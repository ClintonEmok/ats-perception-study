# Milestone v4.0 — ATS Perception Study (Archived 2026-06-30)

## Scope

A controlled within-subjects web experiment comparing Adaptive Temporal Scaling (ATS) vs Uniform timeline perception, built on a dedicated `ats-study` branch. The study is a self-contained Next.js 16 app with a custom React + Zustand + Visx experiment engine (not jsPsych), 6 base datasets, Convex-only persistence, and a formal state machine driving the participant flow.

## Phases

| # | Phase | Plans | Status | Requirements | Success Criteria |
|---|---|---|---|---|---|
| 87 | Infrastructure & Core Logic | 1 | Complete | 4 | 4 |
| 88 | Stimulus Rendering & RT Measurement | 1 | Complete | 3 | 5 |
| 89 | Experiment Flow & Trial Engine | 1 | Complete | 6 | 5 |
| 90 | Deployment / Route Stripping / Pilot | 1 | Complete (build + gates; deploy + pilot deferred) | 6 | 3 |
| 91 | Questionnaire Iteration | 1 | Complete | 1 | 4 |

## Final stats

- 17 commits on `ats-study` between `67635969` (milestone init) and `f841dec` (phase 91 docs)
- 17/19 requirements satisfied; 2 deferred (DEPLOY-04, DEPLOY-05) pending Convex + Vercel credentials
- 72/72 vitest tests pass across 12 files
- 0 import-guard violations across 26 targets
- 0 lint issues on the study surface
- 3 static routes (`/`, `/_not-found`, `/experiment`)
- 0 forbidden modules in the production bundle
- Git range: `7f4e5873..HEAD` → 754 files changed, +6,540 / -112,611

## Outcomes

### Code shipped

- **Convex backend** (`convex/`) — schema with `studySessions`, `studyTrials` (status enum: started/responded/completed/abandoned/timeout), `studyResponses`, `studyQuestionnaires`; typed mutations/queries; hand-written `_generated/server.ts` + `dataModel.ts` + `api.ts` stubs that mirror `npx convex dev` output.
- **Stimulus engine** (`src/lib/ats-study/`) — geometry, stimulus layout, ATS interval mapping (deterministic local-density weight inversion), 6 seeded base datasets with Uniform + ATS variants, counterbalancing Latin square, scoring helpers, export (CSV/JSON), import guard, questionnaire helpers.
- **Participant UI** (`src/components/study/`) — TimelineStimulus (pure SVG), TrialRunner with per-trial confidence anchors, 3 task choice components, PracticeFeedback, ParticipantFlow orchestrator, PostStudyQuestionnaire with 5-pt preference scale, DebriefPanel with download-my-responses.
- **State machine** (`src/store/useExperimentStore.ts`) — Zustand FSM with `persist` middleware, two-phase write API, navigation guard hook, full test coverage.
- **Deployment artifacts** — `vercel.json`, `convex.json`, `.env.example`, `docs/DEPLOYMENT.md`, `docs/PILOT.md`, `scripts/check-import-guard.mjs`, `scripts/check-bundle.mjs`.
- **Repository** — Pushed to a new public repo `ClintonEmok/ats-perception-study` on the `ats-study` branch.

### Architectural decisions

- v4.0 is a custom React + Zustand + Visx experiment engine, not jsPsych.
- Study persistence is Convex-only; no DuckDB on the study branch.
- Counterbalancing uses a committed Latin square with sequential participant assignment.
- Practice trials plus post-study preference/free-text questionnaire are in scope.
- Phase 87: Convex hand-written `_generated/server.ts` stub keeps imports stable without `npx convex dev`; replace when the Convex project is provisioned.
- Phase 87: ATS interval mapper applies min/max-width bounds after the final span rescale, not before, so dense-region emphasis survives normalisation.
- Phase 88: Timing source requires an explicit `performance` argument in tests; the `useStimulusTiming` hook owns Page Visibility wiring for the participant UI.
- Phase 88: `TimelineStimulus` is a pure SVG component (no canvas) so it survives Playwright screenshot comparisons and keeps the bundle small.
- Phase 89: `ConvexClientProvider` ships as a stub that does not import `convex/react`; replaced with the real provider in Phase 90 after `pnpm add convex`.
- Phase 89: `useExperimentStore` persists everything except the `convexWrites` reference, so a refresh resumes mid-trial and we never serialize a function.
- Phase 90: All prototype routes and dependencies are physically removed from the study branch; the only runtime surface is the landing page, `/experiment`, and the Convex client.
- Phase 90: Three deployment gates enforced — import-guard, bundle-check, and the production build.
- Phase 91: Per-trial confidence uses published Likert-5 anchors; 5-point preference scale added alongside the 3-way fallback; free-text has a 10-character soft minimum; debrief offers participant download.

### What is NOT done

- DEPLOY-04 (Vercel + Convex production deployment) — needs `VERCEL_TOKEN`, `CONVEX_DEPLOY_KEY`, and a Convex team.
- DEPLOY-05 (2-3 participant pilot) — blocked on DEPLOY-04.

## Audit

`.planning/v4.0-MILESTONE-AUDIT.md` — 17/19 satisfied, 2 unsatisfied (operational, not code).
