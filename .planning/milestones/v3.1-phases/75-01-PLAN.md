# Phase 75 Plan: Presentation Cleanup

## Goal
Remove presentation deck language, academic jargon, and confusing copy so the dashboard reads as a working tool.

## Changes

1. **"Showcase" → "Generate"** — `DashboardDemoShell.tsx`
   - Button text renamed from "Showcase" to "Generate"
   - Loading text from "Setting up…" to "Generating…"
   - Toast messages updated ("Showcase generation failed" → "Generation failed", etc.)
   - Variable `showcaseLoading` renamed to `generateLoading`, handler `handleShowcase` → `handleGenerate`

2. **"Catalyst cue" → plain language** — `DemoStatsPanel.tsx`
   - Removed academic jargon. Replaced with direct language: "Start with the peak period and dominant crime type."
   - Empty state: "Catalyst cue: No crime records…" → "No crime records…"

3. **"Choose a slice from Inspect" → "Select a slice to compare"** — `DemoInspectPanel.tsx`
   - Empty comparison slot text was confusing (user is already in Inspect panel).

4. **"Left comparison" / "Right comparison" → "Left" / "Right"** — `DemoInspectPanel.tsx`
   - Shortened comparison slot labels for conciseness.

## Files touched
- `src/components/dashboard-demo/DashboardDemoShell.tsx`
- `src/components/dashboard-demo/DemoStatsPanel.tsx`
- `src/components/dashboard-demo/DemoInspectPanel.tsx`
- `src/app/dashboard-demo/page.shell.test.tsx`
