---
phase: quick
plan: mp9
type: execute
wave: 1
depends_on: []
files_modified:
  # Route removals
  - src/app/dashboard/page.tsx
  - src/app/dashboard/page.shell.test.tsx
  - src/app/dashboard-v2/page.tsx
  - src/app/dashboard-v2/page.stkde.test.ts
  - src/app/dashboard-v2/page.flow-consolidation.test.tsx
  # Shared logic relocation
  - src/app/dashboard-v2/hooks/useDashboardStkde.ts
  - src/app/dashboard-v2/hooks/useDashboardStkde.test.ts
  - src/components/stkde/lib/useDashboardStkde.ts
  - src/components/stkde/lib/useDashboardStkde.test.ts
  - src/components/stkde/DashboardStkdePanel.tsx
  # Demo preset migration
  - src/components/demo/DemoPresetSelect.tsx
  - src/components/dashboard-demo/DemoPresetSelect.tsx
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/layout/TopBar.tsx
  - src/lib/demo/preset-windows.ts
  - src/app/dashboard-demo/page.shell.test.tsx
  # Docs / onboarding / fallback tests
  - src/components/onboarding/OnboardingTour.tsx
  - src/app/docs/page.tsx
  - docs/GETTING-STARTED.md
  - src/lib/adaptive/route-binning-mode.test.ts
  - src/app/timeslicing-algos/lib/mode-intent.test.ts
  - src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
autonomous: true

must_haves:
  truths:
    - "`/dashboard-demo` is the only remaining dashboard route and still renders the demo workspace shell."
    - "Selecting a demo preset on `/dashboard-demo` updates demo route state, not legacy `/dashboard` stores."
    - "`/dashboard` and `/dashboard-v2` no longer exist, but shared non-route primitives remain intact unless still needed elsewhere."
    - "The evaluation training tour still starts from the `gsd:start-evaluation-training-tour` event, while the removed `/dashboard*` auto-tour path is gone."
    - "Docs and tests no longer describe `/dashboard` or `/dashboard-v2` as active routes, and fallback-path coverage uses a non-dashboard route."
  artifacts:
    - path: "src/app/dashboard-demo/page.tsx"
      provides: "sole dashboard route entry"
      contains: "DashboardDemoShell"
    - path: "src/components/dashboard-demo/DemoPresetSelect.tsx"
      provides: "demo-only preset control wired to demo stores"
      contains: "useDashboardDemoCoordinationStore"
    - path: "src/components/stkde/lib/useDashboardStkde.ts"
      provides: "route-agnostic STKDE hook formerly nested under dashboard-v2"
      exports: ["useDashboardStkde", "selectAppliedGeneratedSlices"]
    - path: "src/components/onboarding/OnboardingTour.tsx"
      provides: "event-driven evaluation onboarding only"
      contains: "gsd:start-evaluation-training-tour"
  key_links:
    - from: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      to: "src/components/dashboard-demo/DemoPresetSelect.tsx"
      via: "rail render"
      pattern: "DemoPresetSelect"
    - from: "src/components/dashboard-demo/DemoPresetSelect.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "setBrushRange + setTimeScaleMode"
      pattern: "useDashboardDemoCoordinationStore"
    - from: "src/components/stkde/DashboardStkdePanel.tsx"
      to: "src/components/stkde/lib/useDashboardStkde.ts"
      via: "import"
      pattern: "components/stkde/lib/useDashboardStkde"
    - from: "src/app/docs/page.tsx"
      to: "src/app/dashboard-demo/page.tsx"
      via: "docs route links"
      pattern: "href: \"/dashboard-demo\"|Link href=\"/dashboard-demo\""
---

<objective>
Remove the legacy `/dashboard` and `/dashboard-v2` routes, keep `/dashboard-demo` as the permanent final dashboard route without renaming it, migrate the demo preset control into the demo rail with demo-store wiring, and clean up onboarding/docs/tests so nothing active still points at removed dashboard routes.

Purpose: retire obsolete route surfaces without accidentally deleting shared infrastructure, preserve the evaluation training tour, and leave a clean implementation path for a later execution pass.
</objective>

<context>
Locked decisions:
- `/dashboard-demo` stays permanent. Do not rename it back to `/dashboard`.
- Remove `/dashboard`.
- Remove `/dashboard-v2`.
- Keep only the event-driven evaluation training tour in `OnboardingTour`; remove only the legacy `/dashboard*` auto-tour effect.
- Rehome `DemoPresetSelect` into dashboard-demo and wire it to the dashboard-demo coordination/time model, not `useCoordinationStore` / `useTimeStore`.
- Preserve shared primitives/stores still used elsewhere; route removal alone is not justification for deleting shared code.
- Update docs/comments/tests away from `/dashboard` and `/dashboard-v2`, but preserve fallback-path coverage using another non-dashboard route.
- Verification must be baseline-relative: compare against current failures and do not require unrelated zero-failure repo state.
</context>

<tasks>

<task type="auto">
  <name>Migrate DemoPresetSelect into the dashboard-demo rail and rewire it to demo stores</name>
  <files>
    - src/components/demo/DemoPresetSelect.tsx
    - src/components/dashboard-demo/DemoPresetSelect.tsx
    - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    - src/components/layout/TopBar.tsx
    - src/lib/demo/preset-windows.ts
    - src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Create a demo-owned preset control at `src/components/dashboard-demo/DemoPresetSelect.tsx` by porting the current preset dropdown UI and preset metadata usage out of the legacy `src/components/demo/DemoPresetSelect.tsx` location.

    Implementation requirements:
    1. Replace legacy store usage with dashboard-demo wiring only:
       - read timeline bounds/loading state from `useTimelineDataStore` as today,
       - write brush selection and scale mode through `useDashboardDemoCoordinationStore` (`setBrushRange`, `setTimeScaleMode`),
       - if reset/full-range behavior needs a range reset, use `useDashboardDemoTimeStore` only where necessary for demo-local behavior; do not reintroduce `useCoordinationStore` or `useTimeStore`.
    2. Mount the migrated preset control inside `DashboardDemoRailTabs.tsx` near `GlobalWarpControls` so it becomes part of the permanent demo surface rather than the removed legacy top bar.
    3. Remove the legacy `DemoPresetSelect` import/render from `TopBar.tsx`; keep the rest of `TopBar` intact because shared legacy UI may still be used by non-removed surfaces.
    4. Update comments in `src/lib/demo/preset-windows.ts` and the migrated component so they describe `/dashboard-demo` as the consumer route and stop talking about `/dashboard` as the target shell.
    5. Update `src/app/dashboard-demo/page.shell.test.tsx` to assert the demo route now includes the migrated preset control and no longer depends on the legacy top-bar placement.
    6. After the new component is live, delete `src/components/demo/DemoPresetSelect.tsx` or reduce it to a short transitional re-export only if needed for a clean move; the final state should leave dashboard-demo as the canonical owner.
  </action>
  <verify>
    - Baseline first: run `pnpm vitest run src/app/dashboard-demo/page.shell.test.tsx` and record the starting result.
    - After edits: rerun `pnpm vitest run src/app/dashboard-demo/page.shell.test.tsx`.
    - `rg -n "useCoordinationStore|useTimeStore" src/components/dashboard-demo/DemoPresetSelect.tsx` returns no matches.
    - `rg -n "DemoPresetSelect" src/components/layout/TopBar.tsx` returns no matches.
  </verify>
  <done>
    - `DashboardDemoRailTabs` renders the preset control.
    - The preset control mutates demo-route state only.
    - The old top-bar preset placement is gone.
  </done>
</task>

<task type="auto">
  <name>Remove `/dashboard` and `/dashboard-v2` while relocating the STKDE hook out of the route tree</name>
  <files>
    - src/app/dashboard/page.tsx
    - src/app/dashboard/page.shell.test.tsx
    - src/app/dashboard-v2/page.tsx
    - src/app/dashboard-v2/page.stkde.test.ts
    - src/app/dashboard-v2/page.flow-consolidation.test.tsx
    - src/app/dashboard-v2/hooks/useDashboardStkde.ts
    - src/app/dashboard-v2/hooks/useDashboardStkde.test.ts
    - src/components/stkde/lib/useDashboardStkde.ts
    - src/components/stkde/lib/useDashboardStkde.test.ts
    - src/components/stkde/DashboardStkdePanel.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Remove only route-owned artifacts, not shared primitives.

    Implementation requirements:
    1. Delete the route entry files/tests for `src/app/dashboard/*` and `src/app/dashboard-v2/*` listed above.
    2. Before deleting the `dashboard-v2` tree, relocate `useDashboardStkde.ts` and its test into a route-agnostic shared path such as `src/components/stkde/lib/useDashboardStkde.ts` so `DashboardStkdePanel.tsx` no longer imports from a removed route folder.
    3. Update `DashboardStkdePanel.tsx` to import the relocated hook/tested selector from the new shared path with no behavior change.
    4. Do not delete shared files merely because the removed routes used them previously. Specifically preserve `DashboardLayout.tsx`, `TimelinePanel.tsx`, `StudyControls.tsx`, `ContextualSlicePanel.tsx`, `TopBar.tsx`, and other shared stores unless a separate active import graph proves they are truly obsolete and out of scope for this quick task.
    5. Update `src/app/dashboard-demo/page.shell.test.tsx` to stop reading `../dashboard/page.tsx` as a route-separation oracle. Replace that coverage with a non-dashboard fallback route assertion (for example `/stats` and/or `/timeslicing`) so the test still proves dashboard-demo is isolated from other active surfaces.
  </action>
  <verify>
    - Baseline first: run `pnpm vitest run src/app/dashboard-v2/hooks/useDashboardStkde.test.ts src/app/dashboard-demo/page.shell.test.tsx` and record the result.
    - After edits: run `pnpm vitest run src/components/stkde/lib/useDashboardStkde.test.ts src/app/dashboard-demo/page.shell.test.tsx`.
    - `test ! -e src/app/dashboard/page.tsx && test ! -e src/app/dashboard-v2/page.tsx` succeeds.
    - `rg -n "app/dashboard-v2/hooks/useDashboardStkde|../dashboard/page\.tsx|dashboard-v2 flow consolidation|/dashboard shell" src` returns no active-source matches.
  </verify>
  <done>
    - `/dashboard` and `/dashboard-v2` route files/tests are gone.
    - `DashboardStkdePanel` imports a shared hook path, not a removed route path.
    - Shared non-route primitives remain untouched unless explicitly needed for the relocation.
  </done>
</task>

<task type="auto">
  <name>Clean onboarding, docs, and fallback tests for the new single-dashboard reality</name>
  <files>
    - src/components/onboarding/OnboardingTour.tsx
    - src/app/docs/page.tsx
    - docs/GETTING-STARTED.md
    - src/lib/adaptive/route-binning-mode.test.ts
    - src/app/timeslicing-algos/lib/mode-intent.test.ts
    - src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx
    - src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Update remaining active docs/comments/tests to reflect that `/dashboard-demo` is final and the legacy dashboards are gone.

    Implementation requirements:
    1. In `OnboardingTour.tsx`, remove the legacy pathname-based `/dashboard*` auto-tour effect and its related comments/constants while preserving the event-driven evaluation training tour exactly. The component should still listen for `gsd:start-evaluation-training-tour` and build the dark-themed evaluation tour when requested.
    2. In `src/app/docs/page.tsx`, remove `/dashboard` and `/dashboard-v2` from `routeDocs`, feature-group route lists, and CTA links. Promote `/dashboard-demo` as the main dashboard entry without renaming the route.
    3. In `docs/GETTING-STARTED.md`, remove the `/dashboard` and `/dashboard-v2` route rows and rewrite the `/dashboard-demo` description to describe the final dashboard experience rather than a temporary handoff shell.
    4. Update fallback/helper tests so they stop using removed dashboard routes as the “unrelated path” case:
       - `src/lib/adaptive/route-binning-mode.test.ts`: replace `/dashboard` fallback assertions with another active non-dashboard path such as `/stats`.
       - `src/app/timeslicing-algos/lib/mode-intent.test.ts`: replace the `/dashboard` non-algos assertion with another active route such as `/stats`.
       - `src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx`: remove explicit `/dashboard-v2` wording and keep the stronger contract that the header exposes no route navigation links.
       - `src/app/dashboard-demo/page.shell.test.tsx`: update remaining expectations/comments so they no longer describe `/dashboard` as the stable route; use an active non-dashboard route for separation coverage instead.
    5. Verification is baseline-relative: if typecheck/lint already has known unrelated failures, the executor must capture the baseline first and confirm no new failures are introduced by these edits.
  </action>
  <verify>
    - Baseline first: run `pnpm vitest run src/lib/adaptive/route-binning-mode.test.ts src/app/timeslicing-algos/lib/mode-intent.test.ts src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx src/app/dashboard-demo/page.shell.test.tsx` and record failures, if any.
    - After edits: rerun the same suite and confirm no new failures beyond the recorded baseline.
    - `rg -n 'href="/dashboard"|/dashboard-v2|startsWith\("/dashboard"\)' src/app/docs/page.tsx src/components/onboarding/OnboardingTour.tsx docs/GETTING-STARTED.md src/lib/adaptive/route-binning-mode.test.ts src/app/timeslicing-algos/lib/mode-intent.test.ts src/components/dashboard/DashboardHeader.flow-consolidation.test.tsx src/app/dashboard-demo/page.shell.test.tsx` returns no matches.
  </verify>
  <done>
    - Active onboarding/docs/tests no longer describe `/dashboard` or `/dashboard-v2` as live surfaces.
    - The evaluation training tour remains available via event trigger.
    - Fallback route coverage still exists, using a non-dashboard path.
  </done>
</task>

</tasks>

<verification>
1. Capture a baseline before edits for the targeted vitest suites and, if used, `pnpm tsc --noEmit` / `pnpm lint`.
2. Execute the three tasks in order: preset migration -> route removal/hook relocation -> onboarding/docs/tests cleanup.
3. After edits, rerun the targeted vitest suites plus `pnpm tsc --noEmit` and/or `pnpm lint` only if they were part of the baseline.
4. Accept success when removed-route files stay deleted, dashboard-demo remains the only dashboard route, and no new failures appear relative to the recorded baseline.
</verification>

<success_criteria>
- `/dashboard-demo` is the only dashboard route left in `src/app/`.
- The demo preset selector lives under `src/components/dashboard-demo/` and writes only to demo stores.
- `DashboardStkdePanel` no longer imports from `src/app/dashboard-v2/**`.
- `OnboardingTour` contains only the event-driven evaluation training behavior.
- Active docs/tests/comments stop referencing `/dashboard` and `/dashboard-v2`, while non-dashboard fallback coverage remains in place.
</success_criteria>
