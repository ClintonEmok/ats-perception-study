---
id: 260803-ud3
description: "Hide the Fixed 3 km hotspot matching option from dashboard-demo Inspect."
status: planned
mode: quick
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
must_haves:
  truths:
    - "Dashboard-demo Inspect visibly offers Adaptive server cell matching and no Fixed 3 km choice."
    - "The Adaptive server cell control remains wired to the existing dashboard matching setter."
    - "The internal fixed matching type, state, setter, and API compatibility remain unchanged."
    - "Standalone /stkde-3d code is untouched."
  artifacts:
    - path: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      provides: "Inspect matching control showing only Adaptive server cell."
    - path: "src/app/dashboard-demo/page.shell.test.tsx"
      provides: "Source-contract regression coverage for hidden Fixed UI and preserved matching wiring."
  key_links:
    - from: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      to: "src/store/useDashboardDemoCoordinationStore.ts"
      via: "Existing hotspotMatchingMode subscription and setHotspotMatchingMode callback"
      pattern: "setMatchingMode\('adaptive'\)"
    - from: "src/app/dashboard-demo/page.shell.test.tsx"
      to: "src/components/dashboard-demo/DemoInspectPanel.tsx"
      via: "Source assertions for visible Adaptive label and absent Fixed label"
      pattern: "not.toMatch.*Fixed 3 km"
---

# Quick Task 260803-ud3: Hide Fixed hotspot matching in dashboard Inspect

<objective>
Remove only the visible Fixed 3 km hotspot matching option from dashboard-demo Inspect while keeping Adaptive server cell visible and functional.

Purpose: The dashboard already defaults to adaptive matching, so Inspect should not present the legacy fixed choice; internal fixed matching contracts remain available for compatibility.

Output: A one-control Inspect UI and focused source-contract coverage, with no store or standalone-route changes.
</objective>

<execution_context>
@.planning/STATE.md
@AGENTS.md
@src/components/dashboard-demo/DemoInspectPanel.tsx
@src/app/dashboard-demo/page.shell.test.tsx
@src/store/useDashboardDemoCoordinationStore.ts
</execution_context>

<scope_guard>
- In `DemoInspectPanel`, render only the existing `Adaptive server cell` matching choice; keep it visibly selectable and wired to the existing setter.
- Preserve `DemoHotspotMatchingMode`, the `'fixed'` union member, `hotspotMatchingMode`, `setHotspotMatchingMode`, and all internal fixed matching behavior in `useDashboardDemoCoordinationStore.ts`.
- Do not change defaults, reset behavior, renderer/legend wiring, APIs, or any `/stkde-3d` files.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Hide Fixed matching from dashboard Inspect and lock the compatibility contract</name>
  <files>
    src/components/dashboard-demo/DemoInspectPanel.tsx,
    src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Replace the Inspect matching-options rendering that maps over `fixed` and `adaptive` with a single visible `Adaptive server cell` control. Keep the `hotspotMatchingMode` subscription and existing `setHotspotMatchingMode` store API; the remaining control must retain pressed-state behavior and call the setter with `'adaptive'`. Do not remove or modify the store's fixed type/state/setter/API, the intensity legend, or any standalone `/stkde-3d` implementation.

    Update the existing dashboard shell source-contract test to require the Adaptive label and matching wiring, reject `Fixed 3 km` in `DemoInspectPanel`, and explicitly assert that the coordination store still contains the fixed/adaptive matching type and `setHotspotMatchingMode` API.
  </action>
  <verify>
    Run `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx`, `pnpm exec eslint --max-warnings 0 src/components/dashboard-demo/DemoInspectPanel.tsx src/app/dashboard-demo/page.shell.test.tsx`, and `pnpm typecheck`. Inspect the diff to confirm only the two planned dashboard files changed and no `/stkde-3d` file was touched.
  </verify>
  <done>
    Dashboard-demo Inspect shows and activates only Adaptive server cell matching; the Fixed 3 km text is absent from that panel; the store's fixed matching compatibility contracts remain present; focused test, targeted lint, and typecheck pass.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx` passes.
- Targeted ESLint and `pnpm typecheck` pass.
- The final diff contains no store or standalone `/stkde-3d` changes.
</verification>

<success_criteria>
The dashboard-demo Inspect UI no longer exposes Fixed 3 km, Adaptive server cell remains visible and functional, internal fixed matching compatibility is preserved, and all requested focused checks pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260803-ud3-hide-the-fixed-3-km-hotspot-matching-opt/260803-ud3-SUMMARY.md` with the implementation decision and verification results.
</output>
