---
phase: quick
plan: 260806-js2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-demo/lib/demo-warp-map.ts
  - src/components/dashboard-demo/lib/demo-warp-map.test.ts
autonomous: true
must_haves:
  truths:
    - "Slice-authored warp allocates display span as linear duration multiplied by the slice's effective warp weight, then normalizes the full weighted domain to the fixed map axis."
    - "All weights equal to 1 produce the identity linear map at every supported exaggeration value, and uniformly weighted full-domain coverage remains linear after normalization."
    - "Overlapping slices use the maximum effective covering weight, while uncovered gaps use neutral weight 1."
    - "Warp exaggeration multiplies deviation from neutral with `1 + (weight - 1) * exaggeration`, and the effective weight is clamped to a positive floor."
    - "The returned map is finite, monotonic, sampled at the requested count, and anchored to both map-domain endpoints."
  artifacts:
    - path: "src/components/dashboard-demo/lib/demo-warp-map.ts"
      provides: "Duration-times-weight piecewise-linear implementation of buildDemoSliceAuthoredWarpMap while preserving existing helper exports and its public signature."
    - path: "src/components/dashboard-demo/lib/demo-warp-map.test.ts"
      provides: "Regression coverage for neutral, weighted-duration, overlap, gap, exaggeration, normalization, and invalid-input semantics."
  key_links:
    - from: "buildDemoSliceAuthoredWarpMap"
      to: "resolveSliceRange and normalized map-domain segments"
      via: "Convert slice ranges from sliceDomain, clip/build boundaries in mapDomain, and fill uncovered segments with weight 1."
    - from: "TimeSlice.warpWeight"
      to: "effective segment weight"
      via: "clampComparableWarpWeight followed by deviation-based exaggeration and positive-floor clamping."
    - from: "weighted segment durations"
      to: "Float32Array warp map"
      via: "Cumulative weighted lengths normalized to mapSpan and linearly interpolated per sample."
---

<objective>
Replace the rejected literal axis-share model for slice-authored warp with duration-times-weight semantics, preserving the already-accepted warp-intensity plumbing and all unrelated helpers.

Purpose: Make authored slices behave like temporal warp multipliers: neutral authored weights stay linear, overlaps resolve predictably, and exaggeration controls deviation rather than exponentiating or assigning literal axis shares.
Output: Updated `demo-warp-map.ts` implementation and focused tests in `demo-warp-map.test.ts`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/dashboard-demo/lib/demo-warp-map.ts
@src/components/dashboard-demo/lib/demo-warp-map.test.ts

Preserve the separate accepted Warp intensity changes in `adaptive-warp-utils.ts`, `warp-contract.ts`, `useDashboardDemoCoordinationStore.ts`, `GlobalWarpControls.tsx`, `DemoDualTimeline.tsx`, and `Demo3dSpatialView.tsx`; this plan modifies only the two files listed in `files_modified`.

Keep `applySliceMultipliersToDensityMap`, `buildDemoSliceAuthoredWarpAllocation`, `buildSampleWarpMapFromComparableWarp`, and `resolveDensityDerivedSliceWeights` intact. `buildDemoSliceAuthoredWarpMap` must retain `(slices, _densityMap, sliceDomain, sampleCount, mapDomain = sliceDomain, exaggeration = 1)`; `_densityMap` is intentionally unused for this mode.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace literal-share allocation with weighted-duration segment mapping</name>
  <files>src/components/dashboard-demo/lib/demo-warp-map.ts</files>
  <action>
    Remove the `buildSliceShareWarpMap` literal display-budget implementation and replace it with a piecewise-linear segment builder used by `buildDemoSliceAuthoredWarpMap`. Resolve visible, warp-enabled slice ranges through the existing `resolveSliceRange`/normalized domains, clip them to the normalized `mapDomain`, add map endpoints and slice endpoints as boundaries, and assign each segment the maximum effective weight of the slices covering that full segment; assign uncovered segments weight 1. For each slice, first use `clampComparableWarpWeight(slice.warpWeight ?? 1, MIN_WARP_WEIGHT, MAX_WARP_WEIGHT)`, then compute `1 + (clampedWeight - 1) * safeExaggeration`, clamping the result to a small positive floor such as `0.01` so no segment is zero or negative. Use linear duration times effective weight for each segment, normalize cumulative weighted lengths to the fixed map span, and sample/interpolate the resulting map at `Math.floor(sampleCount)` with exact start/end anchors and monotonic clamping. Preserve null behavior for invalid domains/sample counts or no valid enabled ranges, preserve all existing helper exports and signatures, and do not modify the accepted Warp intensity files or reapply the density map.
  </action>
  <verify>Run `pnpm exec vitest run src/components/dashboard-demo/lib/demo-warp-map.test.ts` and `pnpm typecheck` after the implementation; inspect the diff to confirm only the intended function/model changed in the source file and existing helper declarations remain present.</verify>
  <done>`buildDemoSliceAuthoredWarpMap` produces a finite monotonic normalized cumulative map from weighted segment durations, with max-overlap, neutral-gap, deviation-exaggeration, endpoint, and sample-count behavior implemented without changing the public contract or preserved helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Replace literal-share tests with duration-warp regression coverage</name>
  <files>src/components/dashboard-demo/lib/demo-warp-map.test.ts</files>
  <action>
    Remove assertions that expect a slice weight to consume a literal percentage of the axis or that exaggeration exponentiates weights. Keep the existing allocation, density-derived, multiplier, and input-order coverage that still applies, then add deterministic map assertions using a sufficiently sampled domain (for example 101 samples): all weights 1 are identity at exaggeration 1 and 3; a partial interval's mapped span matches duration multiplied by its effective weight after whole-domain normalization; overlapping intervals use the maximum covering weight rather than summing or selecting the wrong slice; gaps remain neutral; uniform weights across full-domain coverage cancel through normalization and remain linear; exaggeration moves weighted segments according to the deviation formula; invalid/very light weights still yield finite monotonic output due to the positive floor; and endpoints remain exactly anchored. Include comments/test names documenting that uniform weights cancel, as this is expected semantics rather than a special case.
  </action>
  <verify>Run `pnpm exec vitest run src/components/dashboard-demo/lib/demo-warp-map.test.ts`; run `pnpm typecheck`; optionally run `pnpm exec eslint src/components/dashboard-demo/lib/demo-warp-map.ts src/components/dashboard-demo/lib/demo-warp-map.test.ts` to verify formatting and lint rules.</verify>
  <done>The focused suite rejects the old literal-share behavior and proves all locked semantics: identity baseline, weighted duration normalization, max overlaps, neutral gaps, deviation-based exaggeration, positive clamping, monotonicity, and anchored endpoints.</done>
</task>

</tasks>

<verification>
Run the focused Vitest file, TypeScript typecheck, and targeted ESLint command. Confirm the tests exercise both `mapDomain === sliceDomain` and a scoped/different map domain where relevant, and confirm no accepted Warp intensity plumbing files were reverted or modified by this quick task.
</verification>

<success_criteria>
The old literal axis-share implementation and tests are gone; the slice-authored map obeys duration-times-weight semantics with max-weight overlaps, neutral gaps, deviation-multiplier exaggeration, all-one linear identity, positive effective weights, monotonic sampling, and exact map-domain anchors. Existing exported helpers and callers continue to typecheck.
</success_criteria>

<output>
After completion, create `.planning/quick/260806-js2-rework-slice-authored-warp-to-duration-t/260806-js2-SUMMARY.md`.
</output>
