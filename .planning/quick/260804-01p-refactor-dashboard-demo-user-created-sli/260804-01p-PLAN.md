---
id: 260804-01p
description: "Refactor dashboard-demo user-created slice flows into one draft-review-apply pipeline and finalize the five-tab rail."
status: planned
mode: quick
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoCoordinationStore.test.ts
  - src/lib/study/resetTargets.ts
  - src/lib/study/resetTargets.test.ts
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/components/dashboard-demo/StkdeAnalysisPanel.tsx
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/app/dashboard-demo/page.shell.test.tsx

must_haves:
  truths:
    - "Detect-generated bins, manual range drafts, and 3D canvas-created ranges all enter pendingGeneratedBins before becoming active slices."
    - "Case-study presets remain diagnostic shortcuts outside the normal draft-review-apply pipeline."
    - "The dashboard rail has exactly Overview, STKDE, Slices, Inspect 3D, and Compare tabs."
    - "The Slices tab owns both draft generation and review/apply, while Overview does not render hotspot evolution."
    - "The STKDE intensity legend is rendered once by the main 3D visualization stage and is absent from the STKDE and Inspect control panels."
    - "The Compare tab remains a control/status surface while comparison maps remain in DemoCompareStage."
  artifacts:
    - path: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      provides: "Final five-tab rail with Overview and combined Slices workflow."
    - path: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      provides: "Single main-stage intensity legend while retaining STKDE scene and draft creation."
    - path: "src/store/useDashboardDemoCoordinationStore.ts"
      provides: "Overview-aware rail state with existing STKDE/Inspect/Compare semantics."
  key_links:
    - from: "src/components/dashboard-demo/DemoDetectPanel.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "generated bins remain pending until Slices applies them"
      pattern: "generateBurstDraftBinsFromWindows"
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/store/useDashboardDemoTimeslicingModeStore.ts"
      via: "canvas-created ranges use addManualDraftRange before compute/apply"
      pattern: "addManualDraftRange"
    - from: "src/components/dashboard-demo/DashboardDemoRailTabs.tsx"
      to: "src/components/dashboard-demo/DemoSlicePanel.tsx"
      via: "Slices tab renders generation and review/apply panels together"
      pattern: "DemoDetectPanel.*DemoSlicePanel"
---

# Quick Task 260804-01p: Unified slice pipeline and final dashboard rail

<objective>
Make normal dashboard slice authoring one coherent draft-review-apply workflow and finalize the five-tab dashboard configuration.

Case studies are diagnostic presets and remain outside this pipeline. The existing pendingGeneratedBins store and canonical slice-domain apply methods remain the only normal user-created slice path.
</objective>

<scope_guard>
- Do not route case-study presets through pending drafts or change their diagnostic behavior.
- Do not add a second slice store, API, worker, or client-side analytics path.
- Preserve DemoDetectPanel generation, DemoSlicePanel review/apply, and 3D canvas draft creation semantics.
- Preserve Compare tab control semantics and DemoCompareStage main-viewport rendering.
- Do not add HotspotEvolutionCard to Overview.
- Do not leave duplicate STKDE intensity legends in rail panels.
- Keep standalone /stkde-3d behavior unchanged.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the five-tab rail and consolidate normal slice authoring</name>
  <files>
    src/store/useDashboardDemoCoordinationStore.ts,
    src/store/useDashboardDemoCoordinationStore.test.ts,
    src/lib/study/resetTargets.ts,
    src/lib/study/resetTargets.test.ts,
    src/components/dashboard-demo/DashboardDemoRailTabs.tsx,
    src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Add `overview` to the dashboard rail tab contract and make Overview the fresh dashboard default. Keep `scan` as the STKDE tab value so existing evaluation reset behavior remains meaningful, and remove the top-level `detect` tab. Define exactly five visible specs: Overview, STKDE, Slices, Inspect 3D, Compare.

    Mount `DemoStatsPanel` in Overview without mounting `HotspotEvolutionCard`. Mount `DemoDetectPanel` above `DemoSlicePanel` in the Slices tab so generated bins, manual drafts, and apply/clear/review controls share one visible workflow. Leave `DemoPresetSelect` case-study behavior outside this pipeline. Keep Compare as the existing control panel and leave `DemoCompareStage` in the main viewport.

    Update store/reset source contracts and dashboard shell tests for the new tab values, default Overview state, preserved STKDE reset targeting, combined Slices content, and absence of a top-level Detect tab. Assert the normal entry points still use pending draft actions rather than directly adding active slices.
  </action>
  <verify>
    Run `pnpm exec vitest run src/store/useDashboardDemoCoordinationStore.test.ts src/lib/study/resetTargets.test.ts src/app/dashboard-demo/page.shell.test.tsx` and targeted ESLint for the changed rail/store/test files. Inspect source to confirm case-study selectors remain separate and `DemoCompareStage` is still rendered by the main viewport.
  </verify>
  <done>
    The dashboard exposes exactly five tabs, Overview is high-level stats only, Slices owns Detect plus Review/Apply, and all normal user-created entry points continue to converge on pending drafts before active slice application.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the main visualization stage the single intensity-legend owner</name>
  <files>
    src/components/dashboard-demo/Demo3dSpatialView.tsx,
    src/components/dashboard-demo/StkdeAnalysisPanel.tsx,
    src/components/dashboard-demo/DemoInspectPanel.tsx,
    src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Remove `StkdeIntensityLegend` rendering from the STKDE and Inspect rail panels. Add one dashboard 3D-stage legend overlay in `Demo3dSpatialView`, using the existing field-backed renderer state and preserving the STKDE scene, surfaces, trajectories, active events, scrubber/runtime, and controls. Do not add a legend to Overview or the Compare control panel; comparison surfaces remain owned by `DemoCompareStage`.

    Update shell source contracts to require the single main-stage legend, reject duplicate panel legend renders, and preserve the existing STKDE tuning/status, Inspect matching/opacity controls, and main comparison-stage wiring. Do not modify standalone legend or comparison components.
  </action>
  <verify>
    Run `pnpm exec vitest run src/app/dashboard-demo/page.shell.test.tsx src/components/dashboard-demo/Demo3dSpatialView.events.test.ts`, targeted ESLint for the three dashboard components and shell test, `pnpm typecheck`, and `pnpm build`.
  </verify>
  <done>
    The intensity legend appears once on the main 3D visualization stage and is absent from rail tabs, with all analytical rendering and interaction paths intact.
  </done>
</task>

</tasks>

<success_criteria>
- Normal Detect, manual, and canvas slice creation all produce pending drafts and use the existing Slices review/apply path.
- Case studies remain separate diagnostic presets.
- The final rail is Overview, STKDE, Slices, Inspect 3D, Compare.
- Overview excludes hotspot evolution; Compare controls remain separate from main-viewport surfaces.
- One main-stage STKDE intensity legend replaces repeated rail legends.
- Focused tests, lint, typecheck, and build pass without standalone route changes.
</success_criteria>

<output>
After execution, create `.planning/quick/260804-01p-refactor-dashboard-demo-user-created-sli/260804-01p-SUMMARY.md` with implementation decisions and verification results.
</output>
