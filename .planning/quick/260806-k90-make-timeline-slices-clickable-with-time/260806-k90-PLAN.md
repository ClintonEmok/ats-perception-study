---
quick_task: 260806-k90-make-timeline-slices-clickable-with-time
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/timeline/DualTimelineSurface.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/components/dashboard-demo/DemoMapVisualization.tsx
  - src/components/map/MapVisualization.tsx
  - src/components/map/MapEventLayer.tsx
autonomous: true
must_haves:
  truths:
    - "Clicking a rendered dashboard-demo timeline slice makes that slice the active slice and updates the cube's active slice."
    - "Selecting or advancing a cube slice updates the timeline's active highlight through the shared slice stores."
    - "The dashboard-demo map keeps contextual events visible while visually emphasizing events whose timestamps fall inside the active slice."
  artifacts:
    - path: "src/components/timeline/DualTimelineSurface.tsx"
      provides: "Clickable and keyboard-accessible rendered slice bars"
    - path: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      provides: "Cube active-index to active-slice-id reconciliation"
    - path: "src/components/map/MapEventLayer.tsx"
      provides: "MapLibre event styling that distinguishes active-slice events from context events"
  key_links:
    - from: "src/components/timeline/DemoDualTimeline.tsx"
      to: "useSliceDomainStore.setActiveSlice and useDashboardDemoCoordinationStore.setActiveSliceIndex"
      via: "timeline slice click callback using canonical visible range ordering"
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/components/timeline/DemoDualTimeline.tsx"
      via: "shared activeSliceId store, including cube runtime index changes"
    - from: "src/components/dashboard-demo/DemoMapVisualization.tsx"
      to: "src/components/map/MapEventLayer.tsx"
      via: "active slice epoch range passed as a highlight range without filtering away context"
---

<objective>
Make the dashboard-demo's timeline, cube, and map share one usable active-slice interaction.

Purpose: The prototype's core value depends on users being able to move between temporal slices without losing spatial context. Timeline bars must select the same slice the cube understands, cube navigation must visibly reconcile back to the timeline, and the map must emphasize the active interval without hiding the surrounding pattern.
Output: A minimal cross-view sync implementation limited to the listed timeline, dashboard-demo, and map integration files.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/components/timeline/DemoDualTimeline.tsx
@src/components/timeline/DualTimelineSurface.tsx
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/components/dashboard-demo/DemoMapVisualization.tsx
@src/components/map/MapVisualization.tsx
@src/components/map/MapEventLayer.tsx

The working tree contains accepted, unrelated edits in DemoDualTimeline.tsx and Demo3dSpatialView.tsx. Preserve their current content and layer only this task's changes on top; do not revert, reformat, or stage unrelated hunks. Do not modify the slice stores: their setters already provide the intended source of truth.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire timeline slice bars to the shared active-slice stores</name>
  <files>src/components/timeline/DemoDualTimeline.tsx; src/components/timeline/DualTimelineSurface.tsx</files>
  <action>
    Add an optional slice-click callback to DualTimelineSurface and attach it to the rendered `orderedSliceGeometries` bars only (not pending draft/manual preview geometry). Make each real slice bar click-selectable and keyboard-accessible with an accurate label/pressed state, while leaving the existing brush/pointer interaction and visual ordering intact.

    In DemoDualTimeline, implement the callback with `useSliceDomainStore.setActiveSlice` and `useDashboardDemoCoordinationStore.setActiveSliceIndex`. Resolve the coordination index from the same canonical visible, range-slice ordering used by the cube (chronological start/end with stable id tie-breaking), not from `orderedSliceGeometries`, because those geometries are re-sorted for visual stacking. Preserve the existing activeSliceId highlight and use `-1` for a non-cube-compatible point slice rather than assigning an unrelated cube index. Do not change slice creation, brushing, or the existing warp edits.
  </action>
  <verify>Run `pnpm typecheck` and inspect the surface props/render path to confirm every real timeline slice has click and Enter/Space activation while pending previews remain non-interactive.</verify>
  <done>Clicking or keyboard-activating a timeline slice sets its id in the slice-domain store, sets the matching cube coordination index, and immediately re-renders the selected bar without breaking brush selection.</done>
</task>

<task type="auto">
  <name>Task 2: Complete cube-to-timeline active-slice reconciliation</name>
  <files>src/components/dashboard-demo/Demo3dSpatialView.tsx</files>
  <action>
    Preserve the existing `onSliceSelect` behavior that sets both `setActiveSlice(sourceSliceId)` and `setActiveSliceIndex(...)`. Extend the scene runtime's `onActiveIndexChange` path so playback, scrubbing, or other cube-driven index changes also resolve the corresponding `cubeSlices` item back to the canonical `countedSlices` source id and call `setActiveSlice` before/alongside `setActiveSliceIndex`. Reconcile the initial/clamped active index as needed so the timeline is not left with a null or stale `activeSliceId` after the cube initializes or its slice list changes. Keep the source-id mapping stable under brushed cube scopes and do not alter unrelated 3D rendering or the pre-existing working-tree edits.
  </action>
  <verify>Run `pnpm typecheck`; confirm the runtime callbacks have both store writes and that source ids, rather than local brushed indexes, are used for the active slice.</verify>
  <done>Cube click, playback, and index changes all produce the same activeSliceId consumed by the timeline, while cube-local indexes still map correctly to global counted-slice indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Highlight active-slice events on the dashboard map without filtering context</name>
  <files>src/components/dashboard-demo/DemoMapVisualization.tsx; src/components/map/MapVisualization.tsx; src/components/map/MapEventLayer.tsx</files>
  <action>
    Keep DemoMapVisualization's active-slice epoch-range derivation, but pass it as an explicit highlight range rather than as a data-filter range. Update MapVisualization so its regular viewport/filter query data remains available to the event layer, and thread the highlight range to MapEventLayer without changing standalone map callers. In MapEventLayer, retain existing selected-time/type/district/spatial filtering, compute an `inActiveSlice` feature property using epoch timestamps (including the existing columns/data fallback conversions), and use MapLibre paint expressions to make matching points larger/brighter and contextual points subdued while the highlight range is present. Preserve the current category colors when possible, retain the existing appearance when no active range exists, and keep the active-slice label/legend copy accurate. Avoid adding a second data fetch or filtering all non-active points out of the map.
  </action>
  <verify>Run `pnpm typecheck` and `pnpm lint`; verify the active range is passed separately from `records`, the generated GeoJSON includes the active-slice flag, and the paint expressions visibly distinguish matching points without removing context points.</verify>
  <done>Changing the active slice updates the map highlight through the shared stores; points inside the slice range are emphasized, points outside remain visible as subdued context, and existing map filters/overlays continue to work.</done>
</task>

</tasks>

<verification>
Run `pnpm typecheck` and `pnpm lint`. In `/dashboard-demo`, click several timeline slice bars and verify the clicked bar, cube active slice, and map emphasis change together; activate a slice in the cube and verify the timeline highlight follows; advance/play the cube index and verify the map highlight follows without hiding out-of-slice events. Confirm brush dragging still changes the detail range and pending draft bars remain previews rather than selectable slices.
</verification>

<success_criteria>
The dashboard-demo has one source-consistent active slice across timeline and cube, timeline bars are mouse/keyboard selectable, cube-driven changes update the timeline, and the map emphasizes active-slice events while retaining surrounding spatial context. TypeScript and lint checks pass, and no unrelated worktree files are modified or staged.
</success_criteria>

<output>
After completion, create `.planning/quick/260806-k90-make-timeline-slices-clickable-with-time/260806-k90-SUMMARY.md`.
</output>
