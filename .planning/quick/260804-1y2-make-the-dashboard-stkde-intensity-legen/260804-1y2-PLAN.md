---
id: 260804-1y2
description: "Make the dashboard STKDE intensity legend a collapsed icon by default."
status: planned
mode: quick
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/app/stkde-3d/components/StkdeIntensityLegend.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
must_haves:
  truths:
    - "The dashboard 3D stage starts with a small STKDE legend icon instead of the full legend card."
    - "Activating the compact control with click or keyboard expands the full gradient, labels, and domain values with correct accessible state."
    - "Standalone /stkde-3d shared-legend consumers remain full and expanded by default."
  artifacts:
    - path: "src/app/stkde-3d/components/StkdeIntensityLegend.tsx"
      provides: "Explicit compact and default-expanded presentation props without route detection."
    - path: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      provides: "Dashboard-only compact, initially collapsed legend wiring."
    - path: "src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx"
      provides: "Focused interaction coverage for compact collapse/expand and unchanged defaults."
  key_links:
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/app/stkde-3d/components/StkdeIntensityLegend.tsx"
      via: "explicit compact and defaultExpanded={false} props"
    - from: "compact legend button"
      to: "isExpanded state and full legend content"
      via: "native button onClick with aria-expanded and accessible label"

<objective>
Make only the dashboard-demo 3D legend compact and collapsed on first render while keeping the shared legend's existing full presentation as the default everywhere else.

Purpose: Keep screenshot-ready dashboard stages visually clear without duplicating the legend or coupling the shared component to route detection.
Output: An explicit compact/default-expanded legend API, dashboard wiring, and focused regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/app/stkde-3d/components/StkdeIntensityLegend.tsx
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/app/dashboard-demo/page.shell.test.tsx
</context>

<scope_guard>
- Add explicit `compact` and `defaultExpanded` behavior to the shared legend; default them so existing standalone consumers remain expanded and visually unchanged.
- In dashboard-demo, pass `compact` and `defaultExpanded={false}` explicitly and keep the existing field/legacy mode and `[0, 1]` domain wiring intact.
- Render a small native, focusable button for the compact collapsed state; preserve the current full legend markup and appearance after expansion.
- Do not use pathname/route detection, change `/stkde-3d` layout, alter palette/domain calculations, or add dependencies.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit compact legend presentation and dashboard wiring</name>
  <files>
    src/app/stkde-3d/components/StkdeIntensityLegend.tsx,
    src/components/dashboard-demo/Demo3dSpatialView.tsx
  </files>
  <action>
    Extend `StkdeIntensityLegend` with `compact?: boolean` and `defaultExpanded?: boolean`, defaulting to the current non-compact/full behavior (`compact: false`, `defaultExpanded: true`). Initialize the existing local expansion state from `defaultExpanded`. When compact and collapsed, render only a small styled native button/icon with a visible gradient cue, `aria-label`/`title` describing expansion, `aria-expanded={false}`, and the existing focus-visible treatment; clicking or keyboard-activating it must reveal the current full legend. Keep the expanded header toggle, gradient, sparse/hot labels, domain values, mode, and palette behavior unchanged. In `Demo3dSpatialView`, pass `compact` and `defaultExpanded={false}` on the shared legend instance and align its wrapper so the collapsed control remains a small overlay rather than a full-width card.
  </action>
  <verify>
    Inspect the diff for no pathname checks or standalone component changes, then confirm the dashboard call contains the explicit compact/default-expanded props and the component retains `aria-expanded`, a native button, and the existing gradient/domain rendering.
  </verify>
  <done>
    Dashboard-demo initially shows a compact clickable legend icon; expanding it restores the complete existing legend, while calls without props (including `/stkde-3d`) remain expanded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add focused compact/default regression coverage</name>
  <files>
    src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx,
    src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Add a focused React test-renderer test for `StkdeIntensityLegend`: compact plus `defaultExpanded={false}` starts with an accessible collapsed button, activating that button changes `aria-expanded` and the accessible label and exposes the sparse/hot/domain content, and the no-prop/default configuration starts expanded. Extend the existing dashboard shell source contract to require the dashboard legend call uses explicit compact/default-expanded props and the shared component declares the accessible toggle/defaults, while retaining the existing single-dashboard-legend assertions.
  </action>
  <verify>
    Run `pnpm exec vitest run src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx src/app/dashboard-demo/page.shell.test.tsx`; all focused tests pass.
  </verify>
  <done>
    Tests prove the dashboard-only wiring, compact interaction, accessible expansion state, and unchanged expanded default without requiring route detection.
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm exec vitest run src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx src/app/dashboard-demo/page.shell.test.tsx`.
2. Run `pnpm exec eslint --max-warnings 0 src/app/stkde-3d/components/StkdeIntensityLegend.tsx src/components/dashboard-demo/Demo3dSpatialView.tsx src/app/stkde-3d/components/StkdeIntensityLegend.test.tsx src/app/dashboard-demo/page.shell.test.tsx`.
3. Run `pnpm typecheck`.
4. Confirm the final diff does not modify standalone route/layout files or introduce dependencies.
</verification>

<success_criteria>
- Dashboard 3D legend is compact and collapsed by explicit props, with a keyboard-accessible click target that expands the complete legend.
- Shared legend default remains full/expanded for standalone `/stkde-3d` consumers.
- Focused tests, targeted ESLint, and typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260804-1y2-make-the-dashboard-stkde-intensity-legen/260804-1y2-SUMMARY.md` with implementation decisions and verification results.
</output>
