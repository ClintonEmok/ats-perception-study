---
id: 260803-qm2
description: "Fix dashboard-demo STKDE per-slice event accounting and add screenshot-friendly case-study presets."
status: planned
mode: quick
autonomous: true
files_modified:
  - src/components/dashboard-demo/lib/stkde-slice-accounting.ts
  - src/components/dashboard-demo/lib/stkde-slice-accounting.test.ts
  - src/components/dashboard-demo/lib/useDemoStkde.ts
  - src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.events.test.ts
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/components/dashboard-demo/StkdeAnalysisPanel.tsx
  - src/app/stkde-3d/components/Stkde3DSceneProvider.tsx
  - src/app/stkde-3d/components/StkdeSliceStack.tsx
  - src/app/stkde-3d/components/SliceInspector.tsx
  - src/lib/demo/case-study-presets.ts
  - src/lib/demo/case-study-presets.test.ts
  - src/app/stkde-3d/page.tsx
  - src/app/stkde-3d/page.stkde.test.ts
  - src/components/dashboard-demo/lib/applyDashboardCaseStudy.ts
  - src/components/dashboard-demo/lib/applyDashboardCaseStudy.test.ts
  - src/components/dashboard-demo/DemoPresetSelect.tsx
  - src/components/dashboard-demo/DashboardDemoShell.tsx
  - src/app/dashboard-demo/page.shell.test.tsx
must_haves:
  truths:
    - "Every visible dashboard case-study slice is requested through the server STKDE applied-slices contract and displays its own response sliceResults[id].meta.eventCount; a missing result is shown as unavailable, never as a real zero-events slice."
    - "Selecting Full, Fourth of July, Spring Break, or New Year's replaces the existing applied slice stack with exactly 10 canonical generated-applied range slices covering the shared case-study range."
    - "A case-study selection synchronizes epoch filter time, normalized brush/demo time, coordination analysis time, both dashboard time-scale stores, and STKDE applied-slices scope."
    - "The dashboard case-study workflow remains server-authoritative and does not import the standalone dataset loader or client-side KDE pipeline."
    - "The standalone /stkde-3d route and dashboard-demo consume one shared case-study definition/range source, while the existing protocol study-task presets remain available."
  artifacts:
    - path: "src/lib/demo/case-study-presets.ts"
      provides: "Shared Full/Fourth-of-July/Spring-Break/New-Year's definitions, normalized range conversion, and deterministic ten-slice range builder."
    - path: "src/components/dashboard-demo/lib/stkde-slice-accounting.ts"
      provides: "Source-ID-safe per-slice event accounting that distinguishes an absent server result from an actual zero count."
    - path: "src/components/dashboard-demo/lib/applyDashboardCaseStudy.ts"
      provides: "Pure case-study application contract for replacing slices and synchronizing dashboard stores."
    - path: "src/components/dashboard-demo/DemoPresetSelect.tsx"
      provides: "Visible dashboard-demo case-study selector wired to the canonical application helper."
  key_links:
    - from: "src/components/dashboard-demo/lib/useDemoStkde.ts"
      to: "/api/stkde/hotspots"
      via: "applied-slices request body containing all visible canonical slice descriptors"
      pattern: "filters.*slices"
    - from: "src/components/dashboard-demo/Demo3dSpatialView.tsx"
      to: "src/lib/stkde/contracts.ts"
      via: "sourceSliceId lookup into response.sliceResults for server event counts"
      pattern: "sliceResults.*sourceSliceId"
    - from: "src/components/dashboard-demo/DemoPresetSelect.tsx"
      to: "src/components/dashboard-demo/lib/applyDashboardCaseStudy.ts"
      via: "case-study selection action bag"
      pattern: "applyDashboardCaseStudy"
    - from: "src/app/stkde-3d/page.tsx"
      to: "src/lib/demo/case-study-presets.ts"
      via: "shared case-study definitions instead of local duplicate ranges"
      pattern: "CASE_STUDY_PRESETS"
---

# Quick Task 260803-qm2: Dashboard-demo STKDE accounting and case-study presets

<objective>
Fix dashboard-demo's misleading per-slice STKDE event counts and make the existing standalone case studies a first-class, screenshot-ready dashboard workflow.

Purpose: The dashboard currently renders `sliceResults[id]?.meta.eventCount ?? 0`, but full-viewport STKDE responses have no per-slice results. The fix must preserve one server-authoritative STKDE pipeline while ensuring screenshots show ten real applied intervals with traceable counts rather than fabricated zeroes.

Output: A tested source-ID-safe event-accounting contract, one shared case-study/range definition used by both routes, and a visible dashboard selector that atomically replaces the applied stack and synchronizes every dashboard time surface.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260803-imh-integrate-all-essential-stkde-3d-functio/260803-imh-SUMMARY.md
@src/components/dashboard-demo/lib/useDemoStkde.ts
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/components/dashboard-demo/DemoInspectPanel.tsx
@src/components/dashboard-demo/StkdeAnalysisPanel.tsx
@src/components/dashboard-demo/DemoPresetSelect.tsx
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/store/useSliceDomainStore.ts
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useDashboardDemoFilterStore.ts
@src/store/useDashboardDemoTimeStore.ts
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/lib/dataset-loader.ts
@src/app/stkde-3d/lib/mock-data.ts
</execution_context>

<scope_guard>
- Do not add dependencies, API routes, workers, a second dashboard data path, or client-side KDE computation.
- Do not import `loadStkde3dDataset`, `loadConfiguredMockStkde3dDataset`, `computeSliceKde`, or standalone `sliceEvents` into dashboard-demo. Dashboard surfaces, counts, hotspots, and trajectories remain derived from the existing `/api/stkde/hotspots` response.
- Preserve the existing protocol `DEMO_PRESETS`/study-task behavior. Add the four case studies as a separate selector group; do not replace or rename T1/T2/T3/T4/T8 presets.
- Use the existing canonical `useSliceDomainStore.replaceSlicesFromBins` path for the replacement stack. Do not persist standalone `EvolvingSlice` objects in the dashboard or mutate source slice ranges after creation.
- Keep the known unrelated full-suite source-contract failures documented in STATE.md out of scope.
</scope_guard>

<task_dependencies>
1. **Task 1 — server accounting contract** has no dependency and removes the misleading full-viewport/per-slice assumption.
2. **Task 2 — shared case-study definitions** has no dependency and can run in parallel with Task 1; it extracts the already-proven standalone ranges without changing its KDE behavior.
3. **Task 3 — dashboard application workflow** depends on Tasks 1–2 because it must use the new event-accounting/scope contract and shared range builder while replacing the canonical store stack.

Dependency graph: `Task 1 ─┐`
                      `├─> Task 3`
              `Task 2 ─┘`

Wave 1: Tasks 1 and 2.
Wave 2: Task 3.
</task_dependencies>

<tasks>

<task type="auto">
  <name>Task 1: Make dashboard STKDE per-slice accounting source-ID-safe and applied-slices-only</name>
  <files>
    src/components/dashboard-demo/lib/stkde-slice-accounting.ts,
    src/components/dashboard-demo/lib/stkde-slice-accounting.test.ts,
    src/components/dashboard-demo/lib/useDemoStkde.ts,
    src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts,
    src/components/dashboard-demo/Demo3dSpatialView.tsx,
    src/components/dashboard-demo/Demo3dSpatialView.events.test.ts,
    src/components/dashboard-demo/DemoInspectPanel.tsx,
    src/components/dashboard-demo/StkdeAnalysisPanel.tsx,
    src/app/stkde-3d/components/Stkde3DSceneProvider.tsx,
    src/app/stkde-3d/components/StkdeSliceStack.tsx,
    src/app/stkde-3d/components/SliceInspector.tsx
  </files>
  <action>
    Add a pure accounting helper that resolves a dashboard slice's event count only from `response.sliceResults[sourceSliceId]?.meta.eventCount`. Return `null`/unknown when the response or keyed result is absent, and keep an actual server value of `0` distinct from missing data. Add a small map helper for the ordered canonical slice IDs; never read the top-level response count as a per-slice count and never use array position or `Object.values()` for identity.

    Update `useDemoStkde` and its phase-2 tests so the dashboard's applied-slices request includes every visible range descriptor whenever the dashboard has applied slices. Keep the provider-owned debounce, abort, stale-response, retry, and metadata lifecycle unchanged. Make `applied-slices` the only dashboard-selectable STKDE scope: remove/disable the misleading Full viewport control in `StkdeAnalysisPanel` rather than allowing a top-level-only response to feed slice metrics; retain the intentional no-applied-slices empty shell.

    In `Demo3dSpatialView` and `DemoInspectPanel`, attach the resolved server count to the scene/inspector data through an optional server-event-count field. Renderer-safe numeric fallbacks may be used internally only where an existing numeric prop requires one, but visible labels and inspector values must render `—`/an explicit per-slice-unavailable state when the keyed result is missing. Update the shared scene-provider type, stack interaction payload, and `SliceInspector` so standalone slices still use their existing known `crimeCount`, while dashboard slices prefer the explicit server count and never display a missing result as `0 events/day`.

    Do not change the server request to a standalone local-data path and do not synthesize per-slice KDE/event data. Keep the existing `sourceSliceId`/`sourceSliceIndex` mapping and adaptive temporal runtime intact.
  </action>
  <verify>
    Run `pnpm exec vitest run src/components/dashboard-demo/lib/stkde-slice-accounting.test.ts src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts src/components/dashboard-demo/Demo3dSpatialView.events.test.ts`. Tests must prove: applied-slices bodies contain all requested IDs, a keyed event count of zero remains zero, a missing key is unknown, full/top-level metadata is never used for a slice, and source IDs survive ordering/brushing.

    Run `pnpm exec eslint --max-warnings 0 src/components/dashboard-demo/lib/stkde-slice-accounting.ts src/components/dashboard-demo/lib/useDemoStkde.ts src/components/dashboard-demo/Demo3dSpatialView.tsx src/components/dashboard-demo/DemoInspectPanel.tsx src/components/dashboard-demo/StkdeAnalysisPanel.tsx src/app/stkde-3d/components/Stkde3DSceneProvider.tsx src/app/stkde-3d/components/StkdeSliceStack.tsx src/app/stkde-3d/components/SliceInspector.tsx` and `pnpm typecheck`. Inspect source to confirm no dashboard path imports standalone client KDE/data modules and no `sliceResults[id]?.meta.eventCount ?? 0` remains as a displayed dashboard metric.
  </verify>
  <done>
    Dashboard 3D/Inspect receives per-slice event accounting from the server response keyed by canonical ID; actual zero and missing are distinct; dashboard controls cannot select a top-level-only scope for per-slice metrics; and the existing provider/server-authoritative scene path remains the only analytical source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract shared case-study definitions and preserve standalone behavior</name>
  <files>
    src/lib/demo/case-study-presets.ts,
    src/lib/demo/case-study-presets.test.ts,
    src/app/stkde-3d/page.tsx,
    src/app/stkde-3d/page.stkde.test.ts
  </files>
  <action>
    Create a dependency-free shared module containing the existing `full`, `fourth-of-july`, `spring-break`, and `new-years` definitions, labels/range labels, UTC epoch conversion, dataset limits, an explicit screenshot mode of `adaptive`, and an authoritative `CASE_STUDY_SLICE_COUNT = 10`. Export a deterministic `buildCaseStudySliceRanges(preset, count = 10)` helper that returns ten chronological, contiguous epoch ranges with the preset start on the first range and preset end on the last range; do not derive ranges from client-fetched crime events.

    Replace the local `CASE_STUDY_PRESETS`/`toUtcEpoch` definitions in `/stkde-3d/page.tsx` with imports from the shared module. Keep the standalone route's existing dataset loader, ten real/mock slices, client-side KDE rendering, comparison preset IDs, loading/error/retry lifecycle, and adaptive-time behavior unchanged. The shared module is a definition/range source only; it must not import the standalone loader or KDE code.

    Add pure tests for the four IDs, exact known UTC boundaries/range labels, adaptive screenshot mode, deterministic ten-slice coverage, contiguous boundaries, and safe handling of an invalid/non-positive count. Update the standalone source-contract tests to assert the route consumes the shared module and no longer owns a duplicate case-study range table.
  </action>
  <verify>
    Run `pnpm exec vitest run src/lib/demo/case-study-presets.test.ts src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts` and `pnpm exec eslint --max-warnings 0 src/lib/demo/case-study-presets.ts src/lib/demo/case-study-presets.test.ts src/app/stkde-3d/page.tsx src/app/stkde-3d/page.stkde.test.ts`.

    Inspect the shared module and standalone page to confirm there is one case-study definition source, the four existing labels/ranges are preserved, and no dashboard-facing code imports standalone `sliceEvents` or `computeSliceKde`.
  </verify>
  <done>
    Both routes resolve the same four case-study IDs/ranges, the range builder deterministically produces ten canonical intervals for dashboard use, and `/stkde-3d` retains its existing data/KDE behavior without duplicated definitions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the first-class dashboard case-study selector and replace/synchronize the applied stack</name>
  <files>
    src/components/dashboard-demo/lib/applyDashboardCaseStudy.ts,
    src/components/dashboard-demo/lib/applyDashboardCaseStudy.test.ts,
    src/components/dashboard-demo/DemoPresetSelect.tsx,
    src/components/dashboard-demo/DashboardDemoShell.tsx,
    src/app/dashboard-demo/page.shell.test.tsx
  </files>
  <action>
    Add a pure `applyDashboardCaseStudy` action-bag helper. Given one shared case-study preset and loaded timeline bounds, it must: validate/derive the epoch and normalized ranges; write the epoch range to `useDashboardDemoFilterStore` and `useDashboardDemoCoordinationStore.timeRange`; write the normalized range to `useDashboardDemoCoordinationStore.brushRange` and `useDashboardDemoTimeStore.timeRange`; clamp the current demo time; synchronize both coordination and demo time-store scale modes to the case-study's screenshot mode; set STKDE scope to `applied-slices`; and replace the current applied stack with exactly the ten ranges from `buildCaseStudySliceRanges`.

    In `DemoPresetSelect`, keep the existing protocol preset group and add a clearly labelled `Case studies` group using the shared definitions. Implement the replacement through `useSliceDomainStore.getState().replaceSlicesFromBins` with ten epoch-ms `TimeBin` inputs over the preset domain, preserving `source: 'generated-applied'`, visible range slices, and canonical `TimeSlice` metadata. Clear pending generated drafts before replacement so selecting a case study truly replaces the current applied stack. Reset active slice/comparison selection as needed to avoid stale IDs, but do not reset unrelated STKDE tuning or adaptive rendering settings. Keep all side effects behind the helper's action bag and retain existing toast/error behavior for missing timeline bounds.

    Mount the selector in `DashboardDemoShell`'s dashboard-demo chrome so it is visible on `/dashboard-demo` rather than only existing as an unused component. After a successful case-study selection, switch the shared viewport to 3D and the rail to Inspect 3D for a deterministic screenshot-ready state; preserve the existing map/3D/compare controls and desktop-first layout. Do not call the standalone dataset loader or local KDE implementation.

    Test the helper's atomic action contract, including exact epoch/normalized ranges, all store synchronization calls, applied-slices scope, ten-range replacement, current-time clamping, pending-draft replacement, and no writes when bounds are unavailable. Extend the dashboard shell source contract to pin the visible case-study selector, shared module import, applied-slices request path, ten-slice builder, no standalone loader/KDE import, and preservation of the existing protocol preset helper.
  </action>
  <verify>
    Run `pnpm exec vitest run src/components/dashboard-demo/lib/applyDashboardCaseStudy.test.ts src/components/dashboard-demo/lib/applyDemoPreset.test.ts src/components/dashboard-demo/lib/useDemoStkde.phase2.test.ts src/app/dashboard-demo/page.shell.test.tsx src/lib/demo/case-study-presets.test.ts`.

    Run `pnpm typecheck`, `pnpm exec eslint --max-warnings 0 src/components/dashboard-demo/lib/applyDashboardCaseStudy.ts src/components/dashboard-demo/lib/applyDashboardCaseStudy.test.ts src/components/dashboard-demo/DemoPresetSelect.tsx src/components/dashboard-demo/DashboardDemoShell.tsx src/app/dashboard-demo/page.shell.test.tsx`, and `pnpm build`.

    Browser smoke with `USE_MOCK_DATA=true NEXT_PUBLIC_USE_MOCK_DATA=true pnpm dev --hostname 127.0.0.1`: open `/dashboard-demo` at a desktop screenshot width, select each of the four case studies, confirm the selector/chip and range are visible, exactly ten slices are applied, the 3D/Inspect view is selected, STKDE status is applied-slices, each returned slice shows its server event count (not a fabricated zero), and there is no new console error or horizontal overflow. Confirm an existing protocol task preset still applies through its original six-action helper.
  </verify>
  <done>
    Case studies are a visible dashboard workflow: selection atomically replaces the applied stack with ten canonical slices, synchronizes all dashboard time/scope stores, requests server per-slice STKDE data, and presents a stable screenshot-ready 3D state without reusing standalone client-side KDE data.
</done>
</task>

</tasks>

<verification>
- Focused Vitest coverage passes for event accounting, STKDE request descriptors, shared case-study ranges, case-study application, and dashboard shell contracts.
- Targeted ESLint, `pnpm typecheck`, and `pnpm build` pass.
- Browser smoke demonstrates all four case studies replace the stack with ten applied slices, keep the dashboard 3D workflow server-authoritative, and avoid missing-result-as-zero labels.
- No dependency, API, worker, standalone loader, or client-side KDE changes are introduced to dashboard-demo.
</verification>

<success_criteria>
- No dashboard-visible per-slice metric treats absent `sliceResults[id]` as an actual zero.
- The server receives ten canonical applied slice descriptors for each selected case study and returns ID-aligned per-slice accounting used by the dashboard scene.
- Full/Fourth of July/Spring Break/New Year's definitions and ranges have one shared source consumed by both routes.
- Selecting a case study replaces, rather than appends to, the current stack and synchronizes filter epoch, brush/demo normalized range, coordination epoch, both time-scale stores, and applied-slices scope.
- Existing protocol presets and standalone `/stkde-3d` client behavior remain intact.
</success_criteria>

<output>
After execution, create `.planning/quick/260803-qm2-fix-dashboard-demo-stkde-per-slice-event/260803-qm2-SUMMARY.md` with implementation decisions, focused tests, build/lint/typecheck results, and desktop screenshot/browser smoke evidence.
</output>
