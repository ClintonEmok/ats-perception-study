# Milestones

## v3.4 Burstiness-First Adaptive Timeline (Shipped: 2026-06-30)

**Phases completed:** 7 phases, 19 plans, 23 tasks

**Key accomplishments:**

- A 1024-bin volumetric adaptive warp axis now renders behind the demo 3D slice stack, and applied slices reposition from warped display time instead of fixed vertical spacing.
- The demo 3D slice stack now deselects cleanly, keeps resized applied slices synchronized after resorting, and creates viewport-clamped pending drafts from empty-space double-clicks.
- Selected 3D slices now expose interactive warp-weight and delete controls, and the adaptive timeline renders a matching density strip so cube and timeline distortion cues stay visually synchronized.
- Canonical Phase 80 protocol + audited reset checklist + acknowledged DuckDB-backed study writes that replace the schema-less JSONL logger — before any `/evaluation` UI is built.
- Dedicated `/evaluation` route, researcher header, training gate, floating task card, and per-condition questionnaire overlay on top of the Phase 80 protocol and store from 80-01.
- Complete ✓
- Read-only DuckDB loader + 1d z-score preview on 8.38M events; verdict PASS — data supports the 168-cell approach and Plan 02 can proceed.
- 84-01 (Wave 1 of 3 for Phase 84 — Burstiness Signal Contract + Density Fallback)
- 84-02 (Wave 2 of 3 for Phase 84 — Burstiness Signal Contract + Density Fallback)
- 84-03 (Wave 3 of 3 for Phase 84 — Burstiness Signal Contract + Density Fallback + Contextual z)
- 86-01 (Wave 1 of 1 for Phase 86 — wire up demopresetselect)

---

## v3.2 Visualization Level Up (Shipped: 2026-06-01)

**Phases completed:** 4 phases, 9 plans, 7 tasks

**Key accomplishments:**

- Shared duration-volume settings and normalization helpers for the demo 3D STKDE widget
- Demo 3D widget duration now reads as volume/depth with normalized scaling and active-slice emphasis
- Shared demo 3D temporal state and compact playback controls for active-slice sequencing, scrubbing pause, and speed-driven playback.
- Active-slice anchored KDE interpolation with ghosted aging trails for the demo 3D STKDE widget.

---

## v3.1 Workflow Finalization (Shipped: 2026-05-26)

**Phases completed:** 4 phases, 5 plans

**Key accomplishments:**

- Detect leads the dashboard-demo workflow (FLOW-07) — burst scanning and generation with prerequisite guidance
- Slices is the review/apply surface for pending and manual slices (FLOW-08)
- Inspect opens with immediate active-slice context and inline comparison controls (FLOW-09)
- Map, 3D view, and timeline stay synchronized with the active slice (FLOW-10)
- Presentation cleanup — "Showcase" → "Generate", academic jargon removed, confusing copy clarified

---
