---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - src/app/stkde-3d/page.tsx
  - src/app/stkde-3d/components/StkdeComparisonControls.tsx
  - src/app/stkde-3d/components/StkdeComparisonStage.tsx
  - src/app/stkde-3d/components/StkdeComparisonViewport.tsx
  - src/app/stkde-3d/components/StkdeDifferenceScene.tsx
  - src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx
  - src/app/stkde-3d/components/StkdeIntensityLegend.tsx
  - src/app/stkde-3d/page.stkde.test.ts
  - src/app/stkde-3d/comparison.integration.test.ts
autonomous: false
requirements:
  - BURST-06
must_haves:
  truths:
    - "Automated route and pure-domain checks cover selection, invalidation, view modes, signed scale/sign, preset identity, and camera-link policy while preserving the known unrelated baseline failures."
    - "The UI exposes stable DOM markers for stack, selecting, absolute, difference, loading, ready, empty, error, active slot, camera link, viewport identity, signed field, and active preset."
    - "The comparison rail and stage satisfy the UI-SPEC's desktop-first layout, narrow vertical stacking, accessibility, long-label, reduced-motion, and fixed-height loading requirements."
    - "A human can verify the default stack, A-pending-B flow, absolute linked/unlinked views, difference view, preset repeatability, and loading/empty/error states in a browser."
  artifacts:
    - path: "src/app/stkde-3d/comparison.integration.test.ts"
      provides: "Phase-specific route/source/DOM contract coverage"
      contains: "data-comparison-stage"
    - path: "src/app/stkde-3d/page.tsx"
      provides: "Final stable render/readiness markers and preserved existing controls"
      contains: "data-render-status"
  key_links:
    - from: "src/app/stkde-3d/comparison.integration.test.ts"
      to: "src/app/stkde-3d/page.tsx"
      via: "route contract assertions for locked user-visible markers and no duplicate data path"
      pattern: "Compare intervals|A − B difference|/api/crimes/range"
    - from: "src/app/stkde-3d/components/StkdeComparisonControls.tsx"
      to: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      via: "same comparison mode, selection, preset, camera, and readiness state"
      pattern: "data-comparison-mode"
---

<objective>
Harden the complete comparison flow against the UI-SPEC, add phase-specific regression coverage, and perform the browser verification that WebGL/jsdom cannot provide.

Purpose: Close the phase with observable, accessible state markers and a verification gate that distinguishes real Phase 4 regressions from the four known unrelated full-suite source-contract failures.
Output: Final responsive/accessibility/error-state polish, integration/source contracts, clean targeted/type/lint/build verification, and one human visual/interaction checkpoint.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/IMPLEMENTED-BASELINE.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-04-SUMMARY.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/StkdeComparisonControls.tsx
@src/app/stkde-3d/components/StkdeComparisonStage.tsx
@src/app/stkde-3d/components/StkdeComparisonViewport.tsx
@src/app/stkde-3d/components/StkdeDifferenceScene.tsx
@src/app/stkde-3d/page.stkde.test.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Use the UI-SPEC as the visual source of truth, not a redesign opportunity. Preserve the existing page header, control rail, KDE tuning, adaptive time, renderer, events, trajectories, opacity, matching, inspector, and scrubber. The stage remains vertical at all widths; the rail moves below the stage below `lg`; panes and difference field have real minimum heights. Essential interval identity and signed meaning must be visible in text and accessible names, never conveyed by color alone.

The baseline records four stale unrelated full-suite source-contract failures. Do not “fix” or reclassify those in this phase. Report them separately after the targeted Phase 4 suite, typecheck, lint, and build pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 integration and observability contracts</name>
  <files>src/app/stkde-3d/comparison.integration.test.ts, src/app/stkde-3d/page.stkde.test.ts</files>
  <action>
    Add targeted source/DOM contract coverage following the existing Vitest style. Assert the default stack path and existing controls remain present; Compare/selection/reset/exit copy is exact; A/B selection uses sourceSliceId and does not use dashboard or persistent slice stores; absolute and difference stage markers expose the required values; difference mode includes signed-field and legend semantics while excluding additive/event/trajectory/volume scene mounts; preset identity and exact-pair fields are represented; case-study invalidation and one existing range-fetch lifecycle are wired. Keep tests focused on stable contracts rather than brittle implementation formatting, and preserve the documented four unrelated baseline failures as outside this suite.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/comparison.integration.test.ts src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/lib/comparison-camera.test.ts</verify>
  <acceptance_criteria>The targeted suite fails if any locked state marker, exact copy, sign meaning, source identity path, duplicate-request guard, or heatmap-only restriction is removed, and it passes independently of the four known unrelated full-suite failures.</acceptance_criteria>
  <done>Phase 4 behavior has durable automated coverage spanning pure transitions, analytical math, presets, camera policy, and route-visible contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Harden UI-SPEC states, accessibility, responsive layout, and reduced motion</name>
  <files>src/app/stkde-3d/page.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonViewport.tsx, src/app/stkde-3d/components/StkdeDifferenceScene.tsx, src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx, src/app/stkde-3d/components/StkdeIntensityLegend.tsx</files>
  <action>
    Audit and finish the UI contract without moving state to the dashboard. Ensure the stage root has `data-comparison-stage`, mode is selecting/absolute/difference, readiness is loading/ready/empty/error, selecting exposes `data-selection-slot`, absolute panes expose slot/source IDs and camera-link state, difference exposes `data-difference-field="signed-kde"`, and active built-ins expose `data-comparison-preset-id`. Preserve fixed-size skeleton/veil dimensions for dataset and preset/KDE loading; implement exact empty, one-interval, missing-KDE, load-error, mock-warning, duplicate, invalidation, and preset-error copy with Retry loading where the existing loader can retry. Verify semantic buttons/selects/switch/tabs, `aria-live`, `aria-pressed`, full interval names/title text, visible focus rings, signed text redundancy, disabled heatmap-only controls, 320px pane minimums, `min-h-0 min-w-0`, narrow wrapping, rail scroll, and reduced-motion immediate transitions. Use existing shadcn primitives or native controls and the existing Tailwind tokens; do not add a registry block, dependency, or second styling system.
  </action>
  <verify>pnpm typecheck && pnpm lint</verify>
  <acceptance_criteria>All UI-SPEC screenshot states have stable textual/DOM observability, no page-level horizontal overflow occurs at narrow widths, WebGL parents have non-zero heights, essential identity/sign information is accessible without color or hover, and existing analysis controls remain available with difference restrictions explained.</acceptance_criteria>
  <done>The comparison UI is production-quality for the desktop-first prototype and remains usable in loading, empty, partial, populated, error, overflow, and reduced-motion states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the complete comparison flow in a real browser</name>
  <what-built>Complete Phase 4 `/stkde-3d` comparison mode: default stack, surface/keyboard A/B selection, exact-pair preset, vertical linked absolute panes, independent/relinked cameras, signed A-B difference, invalidation, and edge states.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open `/stkde-3d` at a desktop width. Confirm the default is the existing full stack and all existing controls still work.
    2. Activate `Compare intervals`; confirm playback pauses, the ten surfaces remain visible, `Select interval A` is announced, and the DOM interval picker lists every interval.
    3. Select A by surface and B by picker. Confirm A advances to B, duplicate B is rejected, both panes show exact metadata with A above B, `Absolute` and `Linked cameras` are active, and one shared sequential legend is visible.
    4. Rotate/pan/zoom either pane with linking on and confirm the other mirrors it. Turn linking off and confirm panes diverge; turn it back on and confirm B immediately snaps to A. Activate Reset views and confirm both return to the same front-oblique pose.
    5. Load a built-in exact-pair preset twice. Confirm dataset, A/B interval identity, parameters, renderer/layer, view, camera state, and `data-comparison-preset-id` repeat identically; do not accept a nearest-label fallback.
    6. Switch to `A − B difference`. Confirm exactly one top-down field, `KDE(A) − KDE(B)`, red=A higher, neutral=zero, blue=B higher, and no raw events, trajectories, slabs, burst volume, or temporal-axis cue. Confirm event/trajectory controls are disabled with the heatmap-only helper.
    7. Change case study and confirm stale A/B disappears while the new data loads. Verify empty/one-interval/error/loading markers if the selected dataset or mocked failure path provides them. Resize below `lg` and below `sm`; confirm stage/rail order, vertical A-over-B order, full labels, scrolling, and no horizontal overflow.
  </how-to-verify>
  <resume-signal>Type `approved` if all checks pass, or describe the exact screenshot/interaction/state failure to revise.</resume-signal>
  <acceptance_criteria>A human can observe every required Phase 4 truth in the running browser, including linked-camera snap semantics, truthful signed direction, exact preset repeatability, stale-selection invalidation, and narrow responsive behavior.</acceptance_criteria>
</task>

</tasks>

<verification>
Run the targeted Phase 4 suite, `pnpm typecheck`, `pnpm lint`, and `pnpm build`. Run the full `pnpm test -- --run` only after targeted checks pass; report the four known unrelated source-contract failures from the baseline separately rather than weakening Phase 4 assertions.
</verification>

<success_criteria>
BURST-06 is verifiable end to end: analysts compare two existing intervals as temporary references in matched absolute views or one truthful signed difference view, exact presets reproduce the state, existing stack/data controls remain intact, and all specified edge, accessibility, responsive, loading, empty, error, and transition states are observable and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-05-SUMMARY.md`.
</output>
