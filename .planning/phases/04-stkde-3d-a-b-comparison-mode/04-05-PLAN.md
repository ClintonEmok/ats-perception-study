---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - src/app/stkde-3d/lib/dataset-loader.ts
  - src/app/stkde-3d/lib/dataset-loader.test.ts
  - src/app/stkde-3d/page.tsx
  - src/app/stkde-3d/components/StkdeComparisonControls.tsx
  - src/app/stkde-3d/components/StkdeComparisonStage.tsx
  - src/app/stkde-3d/components/StkdeComparisonViewport.tsx
  - src/app/stkde-3d/components/StkdeDifferenceScene.tsx
  - src/app/stkde-3d/components/StkdeIntensityLegend.tsx
  - src/app/stkde-3d/page.stkde.test.ts
  - src/app/stkde-3d/comparison.integration.test.ts
autonomous: false
requirements:
  - BURST-06
must_haves:
  truths:
    - "Automated route and pure-domain checks cover selection, source-index remapping, invalidation, view modes, signed scale/sign, concrete preset identity, public KDE exports, and camera-link policy while preserving known unrelated baseline failures."
    - "Selecting A/B and changing shared KDE, adaptive-time, renderer, opacity, matching, or inspector settings reuses the loaded dataset and does not issue duplicate `/api/crimes/range` requests; only a case-study/retry load may start a new request lifecycle."
    - "The UI exposes stable DOM markers for stack, selecting, absolute, difference, loading, ready, empty, error, active slot, camera link, viewport identity, signed field, and active preset."
    - "Loading failure is a reachable route-level error with `Retry loading`; configured/baseline mock fallback remains available only as an explicitly labeled `Using mock data` path rather than silently masquerading as live data."
    - "Deterministic zero-, one-, two-, and ten-slice fixtures make no-interval, partial, and ready comparison states testable without relying on live quantile-derived records."
    - "The comparison rail and stage satisfy the UI-SPEC's desktop-first layout, narrow vertical stacking, accessibility, long-label, reduced-motion, and fixed-height loading requirements."
    - "A human can verify the default stack, A-pending-B flow, source-correct absolute overlays, linked/unlinked views, difference view, preset repeatability, request reuse, and loading/empty/error states in a browser."
  artifacts:
    - path: "src/app/stkde-3d/lib/dataset-loader.ts"
      provides: "Single injectable dataset/range loading lifecycle used by the route and fetch-spy tests"
      contains: "fetchImpl"
    - path: "src/app/stkde-3d/lib/dataset-loader.test.ts"
      provides: "Fetch-spy coverage for pagination and loader reuse boundaries"
      contains: "mock"
    - path: "src/app/stkde-3d/comparison.integration.test.ts"
      provides: "Phase-specific route/source/DOM and no-duplicate-request contract coverage"
      contains: "data-comparison-stage"
    - path: "src/app/stkde-3d/page.tsx"
      provides: "Final stable render/readiness/error/retry markers and preserved existing controls"
      contains: "Retry loading"
  key_links:
    - from: "src/app/stkde-3d/comparison.integration.test.ts"
      to: "src/app/stkde-3d/page.tsx"
      via: "route contracts for locked user-visible markers, explicit error/retry, and one dataset effect lifecycle"
      pattern: "Compare intervals|Retry loading|A − B difference|/api/crimes/range"
    - from: "src/app/stkde-3d/lib/dataset-loader.test.ts"
      to: "src/app/stkde-3d/lib/dataset-loader.ts"
      via: "injected fetch spy counts only loader-owned range requests"
      pattern: "fetchImpl|crimes/range"
    - from: "src/app/stkde-3d/components/StkdeComparisonControls.tsx"
      to: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      via: "same comparison mode, selection, preset, camera, readiness, and heatmap-only state"
      pattern: "data-comparison-mode|data-render-status"
---

<objective>
Harden the complete comparison flow against the UI-SPEC, add phase-specific regression and request-reuse coverage, make loading/error/edge states reachable, and perform browser verification that WebGL/jsdom cannot provide.

Purpose: Close the phase with observable, accessible state markers and a verification gate that distinguishes real Phase 4 regressions from the four known unrelated full-suite source-contract failures. The route must fail visibly and retry explicitly instead of hiding a real load failure behind mock data.
Output: Final responsive/accessibility/error-state polish, an injectable single-loader fetch contract, integration/source tests, clean targeted/type/lint/build verification, and one human visual/interaction checkpoint.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/IMPLEMENTED-BASELINE.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-04-SUMMARY.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/StkdeComparisonControls.tsx
@src/app/stkde-3d/components/StkdeComparisonStage.tsx
@src/app/stkde-3d/components/StkdeComparisonViewport.tsx
@src/app/stkde-3d/components/StkdeDifferenceScene.tsx
@src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx
@src/app/stkde-3d/components/StkdeIntensityLegend.tsx
@src/app/stkde-3d/page.stkde.test.ts
@src/app/stkde-3d/lib/comparison-fixtures.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Use the UI-SPEC as the visual source of truth, not a redesign opportunity. Preserve the existing page header, control rail, KDE tuning, adaptive time, renderer, events, trajectories, opacity, matching, inspector, and scrubber. The stage remains vertical at all widths; the rail moves below the stage below `lg`; panes and difference fields have real minimum heights. Essential interval identity and signed meaning must be visible in text and accessible names, never conveyed by color alone.

The current route's loader is embedded in a `useEffect` and its catch block silently calls `generateStkde3dMockData`. Extract the range/pagination loop into `loadStkde3dDataset(preset, fetchImpl)` so a test can inject `vi.fn()` and the route can own one explicit load/retry lifecycle. A real fetch failure must reach `data-render-status="error"` with the exact UI-SPEC copy `Unable to load {case study} data. Retry loading or choose another case study.` and a text-labeled `Retry loading` action. Retain the current mock generator only for the configured/baseline mock path and expose `Using mock data` as a non-blocking warning; do not present an unlabelled mock result as a successful live load.

Request reuse is a required verification, not an inference from source text: the loader/effect test must use a fetch spy or equivalent effect harness to select A/B and change shared settings while asserting the range request count is unchanged. The final browser checkpoint must repeat that check in the network log. Only changing caseStudyPresetId or explicitly retrying may start a new range load.

The signed difference scene and legend are already owned by Plan 04; this plan verifies their inherited heatmap-only and text-redundancy contract while keeping loader/request verification separate from that difference-specific UI implementation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route contracts and fetch-spy coverage for one dataset lifecycle</name>
  <files>src/app/stkde-3d/lib/dataset-loader.ts, src/app/stkde-3d/lib/dataset-loader.test.ts, src/app/stkde-3d/comparison.integration.test.ts, src/app/stkde-3d/page.stkde.test.ts</files>
  <action>
    Extract the current paginated `/api/crimes/range` loop into an injectable `loadStkde3dDataset(preset, fetchImpl)` helper without changing its cursor/limit semantics. Add tests with a `vi.fn()` fetch implementation for one-page and multi-page responses, failed responses, retry invocation, and exactly the expected range-call count. Add an effect-level/focused harness test that applies A/B selection and changes KDE, adaptive-time, renderer, opacity, matching, and inspector state against the same loaded dataset, then asserts the fetch spy count is unchanged; a case-study change or explicit retry must be the only count increase. Add targeted source/DOM contracts asserting the default stack and existing controls remain present; Compare/selection/reset/exit/error/retry copy is exact; A/B selection uses sourceSliceId/sourceSliceIndex and does not use dashboard or persistent slice stores; absolute and difference stage markers expose required values; difference mode excludes additive/event/trajectory/volume mounts; the concrete preset IDs/pairs and public `KdeField` barrel export are represented; the source-contract assertions deferred from Plan 03 remain here; and the route load effect does not depend on A/B or shared settings.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/dataset-loader.test.ts src/app/stkde-3d/comparison.integration.test.ts src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/lib/comparison-source-context.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/lib/comparison-camera.test.ts src/lib/kde/index.test.ts</verify>
  <acceptance_criteria>The targeted suite fails if a shared-setting interaction calls `/api/crimes/range`, if a case-study/retry load does not call the single loader, if any locked state marker/copy/source identity/sign meaning/public export/heatmap-only restriction is removed, or if the route reintroduces a second dataset path. It passes independently of the four known unrelated full-suite failures.</acceptance_criteria>
  <done>Phase 4 has durable automated coverage spanning pure transitions, source-index remapping, analytical math, concrete presets, camera policy, public KDE exports, route-visible contracts, and request reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Harden UI-SPEC states, accessibility, responsive layout, and explicit error/retry behavior</name>
  <files>src/app/stkde-3d/page.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonViewport.tsx, src/app/stkde-3d/components/StkdeDifferenceScene.tsx, src/app/stkde-3d/components/StkdeIntensityLegend.tsx</files>
  <action>
    Audit and finish the UI contract without moving state to the dashboard. Ensure the stage root has `data-comparison-stage`, mode is selecting/absolute/difference, readiness is loading/ready/empty/error, selecting exposes `data-selection-slot`, absolute panes expose slot/source IDs and camera-link state, difference exposes `data-difference-field="signed-kde"`, and active built-ins expose `data-comparison-preset-id`. Preserve fixed-size skeleton/veil dimensions for dataset and preset/KDE loading. Implement a route-level real-load error state with `Retry loading` and exact error copy; keep the configured/baseline mock path visibly labeled `Using mock data`. Use `buildComparisonSliceFixture(0|1|2|10)` in a focused stage/control harness or pure availability tests to make `No intervals available`, `Comparison needs at least two rendered intervals.`, selected-A/B pending, and pair-ready states reachable and deterministic. Cover missing-KDE and preset-error states with the exact UI-SPEC copy. Verify semantic buttons/selects/switch/tabs, `aria-live`, `aria-pressed`, full interval names/title text, visible focus rings, the inherited signed legend's text redundancy, disabled heatmap-only controls, 320px pane minimums, `min-h-0 min-w-0`, narrow wrapping, rail scroll, and reduced-motion immediate transitions. Use existing shadcn primitives or native controls and existing Tailwind tokens; do not add a registry block, dependency, or second styling system.
  </action>
  <verify>pnpm typecheck && pnpm lint && pnpm vitest run src/app/stkde-3d/comparison.integration.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>All UI-SPEC screenshot states have stable textual/DOM observability, error state and Retry loading are reachable without silently substituting mock data, mock fallback is labeled, zero/one/two-slice harness cases are deterministic, no page-level horizontal overflow occurs at narrow widths, WebGL parents have non-zero heights, essential identity/sign information is accessible without color or hover, and existing analysis controls remain available with difference restrictions explained.</acceptance_criteria>
  <done>The comparison UI is production-quality for the desktop-first prototype and remains usable in loading, empty, partial, populated, error, overflow, mock-warning, and reduced-motion states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the complete comparison flow and network reuse in a real browser</name>
  <what-built>Complete Phase 4 `/stkde-3d` comparison mode: default stack, surface/keyboard A/B selection, source-correct focused events and trajectory-capable absolute panes, concrete exact-index presets with runtime interval metadata, vertical linked absolute panes, independent/relinked cameras, signed A-B difference, invalidation, request reuse, and reachable loading/empty/error/mock-warning states.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open `/stkde-3d` at a desktop width. Confirm the default is the existing full stack, `isFocusedView` is false, and all existing controls still work.
    2. Record the settled `/api/crimes/range` request count in the browser network log. Activate `Compare intervals`; confirm playback pauses, `isFocusedView` remains false, the ten surfaces remain visible, `Select interval A` is announced, and the DOM interval picker lists every interval.
    3. Select A by surface and B by picker. Confirm A advances to B, duplicate B is rejected, both panes show actual runtime metadata with A above B, `Absolute` and `Linked cameras` are active, one shared sequential legend is visible, focused events correspond to the selected source interval, and the trajectory toggle/preset renders full-source trajectory epochs without losing source IDs.
    4. Change KDE tuning, adaptive time, renderer, opacity, matching, and inspector-related settings. Confirm both absolute panes update consistently and the network log shows no new `/api/crimes/range` request. Change the case study once and confirm a new load lifecycle occurs, stale A/B disappears, and the request count increases only for that case-study load.
    5. Rotate/pan/zoom either pane with linking on and confirm the other mirrors it. Turn linking off and confirm panes diverge; turn it back on and confirm B immediately snaps to A. Activate `Reset views` and confirm both return to the same front-oblique pose.
    6. Load `full-slice-02-vs-08` and `fourth-of-july-slice-03-vs-09` twice. Confirm the selector exposes the dataset and `Slice 2 vs Slice 8` / `Slice 3 vs Slice 9` pair identities, the actual rendered start/end metadata repeats, `data-comparison-preset-id` is stable, and no nearest-label or missing-preset fallback occurs.
    7. Switch to `A − B difference`. Confirm exactly one top-down field, `KDE(A) − KDE(B)`, red=A higher, neutral=zero, blue=B higher, and no raw events, full trajectories, slabs, burst volume, or temporal-axis cue. Confirm event/trajectory controls are disabled with the heatmap-only helper.
    8. Force a real loader failure (for example, use the browser's offline network mode before selecting `Retry loading`). Confirm `data-render-status="error"`, `Unable to load {case study} data. Retry loading or choose another case study.`, and the text-labeled `Retry loading` action. If the configured/mock path is used instead, confirm the stage is not silently presented as live and visibly says `Using mock data`.
    9. Exercise the deterministic zero/one/two-slice harness or its test route and confirm `No intervals available`, `Comparison needs at least two rendered intervals.`, B-pending, and ready states. Resize below `lg` and below `sm`; confirm stage/rail order, vertical A-over-B order, full labels, scrolling, and no horizontal overflow. Verify `Back to stack` explicitly returns to the full stack and clears temporary A/B state.
  </how-to-verify>
  <resume-signal>Type `approved` if all checks pass, or describe the exact screenshot/interaction/request/state failure to revise.</resume-signal>
  <acceptance_criteria>A human can observe every required Phase 4 truth in the running browser, including source-correct focused overlays, linked-camera snap semantics, truthful signed direction, concrete preset repeatability with runtime bounds, unchanged request count during shared-setting changes, stale-selection invalidation, explicit error/retry/mock labeling, deterministic edge states, and narrow responsive behavior.</acceptance_criteria>
</task>

</tasks>

<verification>
Run the targeted Phase 4 suite including `pnpm vitest run src/app/stkde-3d/lib/dataset-loader.test.ts src/app/stkde-3d/comparison.integration.test.ts src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/lib/comparison-source-context.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/lib/comparison-camera.test.ts src/lib/kde/index.test.ts`, then `pnpm typecheck`, `pnpm lint`, and `pnpm build`. Run the full `pnpm test -- --run` only after targeted checks pass; report the four known unrelated source-contract failures from the baseline separately rather than weakening Phase 4 assertions. Include the browser network verification result in the phase summary.
</verification>

<success_criteria>
BURST-06 is verifiable end to end: analysts compare two existing intervals as temporary references in matched absolute views or one truthful signed difference view, concrete built-in presets reproduce exact dataset/index pairs with runtime bounds, source-aware events/trajectories remain correct, existing stack/data controls remain intact, shared-setting changes reuse loaded data, and all specified edge, accessibility, responsive, loading, empty, error, mock-warning, and transition states are observable and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-05-SUMMARY.md`.
</output>
