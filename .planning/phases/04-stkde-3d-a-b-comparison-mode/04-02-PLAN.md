---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kde/types.ts
  - src/lib/kde/index.ts
  - src/lib/kde/compute-slice-kde.ts
  - src/lib/kde/compute-slice-kde.test.ts
  - src/lib/kde/index.test.ts
  - src/app/stkde-3d/lib/comparison-difference.ts
  - src/app/stkde-3d/lib/comparison-difference.test.ts
  - src/app/stkde-3d/lib/palette.ts
  - src/app/stkde-3d/lib/palette.test.ts
autonomous: true
requirements:
  - BURST-06
must_haves:
  truths:
    - "Absolute A and B values can be interpreted against one shared raw KDE domain rather than two independently normalized maxima."
    - "The difference field subtracts complete aligned raw grids, including values below the existing display threshold."
    - "Positive KDE(A)-KDE(B) is red/A-dominant, zero is neutral, and negative values are blue/B-dominant on a symmetric domain."
    - "Empty, identical, zero, and mismatched inputs have deterministic non-misleading outcomes."
    - "KdeField is available through the public `@/lib/kde` barrel, not only through a private module import."
  artifacts:
    - path: "src/lib/kde/types.ts"
      provides: "Raw complete KDE field metadata alongside backward-compatible sparse display cells"
      contains: "Float32Array"
    - path: "src/lib/kde/index.ts"
      provides: "Public type export for KdeField and existing KDE APIs"
      contains: "KdeField"
    - path: "src/lib/kde/compute-slice-kde.ts"
      provides: "Complete raw field emitted from the existing KDE computation"
      contains: "field"
    - path: "src/app/stkde-3d/lib/comparison-difference.ts"
      provides: "Shared absolute domain, raw signed subtraction, and display-cell conversion"
      contains: "computeSignedKdeDifference"
    - path: "src/app/stkde-3d/lib/palette.ts"
      provides: "Dedicated red-neutral-blue signed palette without changing sequential palettes"
      contains: "A-dominant"
  key_links:
    - from: "src/lib/kde/compute-slice-kde.ts"
      to: "src/lib/kde/types.ts"
      via: "raw grid values/support/grid metadata retained in SliceKdeResult"
      pattern: "SliceKdeResult|KdeField"
    - from: "src/lib/kde/index.ts"
      to: "src/lib/kde/types.ts"
      via: "public barrel type export asserted by a test importing @/lib/kde"
      pattern: "export type.*KdeField"
    - from: "src/app/stkde-3d/lib/comparison-difference.ts"
      to: "src/lib/kde/types.ts"
      via: "same-grid KdeField inputs"
      pattern: "KdeField"
---

<objective>
Preserve the complete KDE fields and establish the tested math/color contracts required for truthful absolute and signed comparison rendering.

Purpose: The current route keeps thresholded normalized cells, which cannot support a shared absolute scale or unbiased subtraction. This plan extends the existing computation in place, without adding an endpoint, second request, or second analytical pipeline.
Output: A backward-compatible raw `KdeField` exposed through the public KDE barrel, pure comparison-domain helpers, a documented signed palette, and targeted unit coverage for threshold, sign, scale, and invalid-input behavior.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@src/lib/kde/types.ts
@src/lib/kde/index.ts
@src/lib/kde/compute-slice-kde.ts
@src/app/stkde-3d/lib/palette.ts
@src/app/stkde-3d/components/StkdeSliceStack.tsx
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
`computeSliceKde` currently computes a complete `Float32Array` and then discards it while emitting sparse normalized `cells`. Add the raw field to the existing result without changing the meaning or threshold behavior of `cells`; existing consumers must continue to compile. The comparison field is small and client-local. Use one absolute maximum for both selected fields and a stable non-zero fallback domain for all-zero differences. Missing fields and grid mismatches are invalid comparison inputs, not neutral data.

Preserve `STKDE_INTENSITY_STOPS`, `LEGACY_STKDE_INTENSITY_STOPS`, and their existing helpers for absolute views. Add a separate signed stop list using the UI-SPEC semantics: `#175CD3`/blue for B higher, `#F4F1EB`/neutral for zero, and `#B42318`/red for A higher. Do not encode signed meaning through the existing cream-to-red sequential palette.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve complete raw KDE fields and expose them through the public barrel</name>
  <files>src/lib/kde/types.ts, src/lib/kde/index.ts, src/lib/kde/compute-slice-kde.ts, src/lib/kde/compute-slice-kde.test.ts, src/lib/kde/index.test.ts</files>
  <action>
    Add a typed `KdeField` containing complete row-major values, support values, gridSize, cell dimensions, and raw max intensity; expose it on `SliceKdeResult` while retaining `cells`, `maxIntensity`, and `meanIntensity` unchanged for current renderers. Populate the field from existing support/intensity arrays before threshold filtering and never reconstruct it from rounded display cells. Update `src/lib/kde/index.ts` to export `KdeField` alongside the current public types. Add computation tests proving below-threshold cells remain in the raw field, ordering/coordinates are stable, empty input is safe, and sparse-cell output remains compatible. Add a barrel test that imports `computeSliceKde` and `KdeField` from `@/lib/kde` (plus a source/export assertion if needed) and verifies the returned `field.values` is a complete `Float32Array`; do not leave comparison code importing a private `types.ts` path when the public barrel is the contract.
  </action>
  <verify>pnpm vitest run src/lib/kde/compute-slice-kde.test.ts src/lib/kde/index.test.ts</verify>
  <acceptance_criteria>A caller can import `KdeField` from `@/lib/kde` and access every computed grid value and support count from one `SliceKdeResult`; thresholding only affects display cells, and all existing result fields retain their prior values for the same inputs.</acceptance_criteria>
  <done>The current KDE pipeline is the sole producer of both display cells and complete raw comparison fields, and the public barrel export is covered by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared absolute and signed comparison math</name>
  <files>src/app/stkde-3d/lib/comparison-difference.ts, src/app/stkde-3d/lib/comparison-difference.test.ts</files>
  <action>
    Add pure helpers that derive `[0, max(a.field.maxIntensity, b.field.maxIntensity)]` with a stable positive fallback, convert a raw field to comparable display cells using that shared maximum, and compute `KDE(A) - KDE(B)` cell-by-cell from complete aligned raw values. Require equal gridSize, equal field length, and valid field metadata; throw or return a typed invalid result for missing fields or mismatches so the UI can show the comparison error rather than drawing neutral output. Return a symmetric signed domain `[-maxAbs, maxAbs]`, with a stable fallback when all differences are zero. Cover positive, negative, zero, thresholded, empty, identical, and mismatched-grid cases.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison-difference.test.ts</verify>
  <acceptance_criteria>Tests demonstrate that a raw value below the display cutoff still contributes to subtraction, equal raw fields produce a valid all-zero neutral field, signs map to the documented A/B meanings, and mismatched grids never silently become zero.</acceptance_criteria>
  <done>The later view layer can derive both shared absolute cells and a truthful signed field entirely from the two already-loaded results.</done>
</task>

<task type="auto">
  <name>Task 3: Add and test the dedicated signed palette</name>
  <files>src/app/stkde-3d/lib/palette.ts, src/app/stkde-3d/lib/palette.test.ts</files>
  <action>
    Add typed red-neutral-blue stops, a signed normalized-value color helper, and a CSS gradient helper beside the existing sequential helpers. Clamp values and domains safely, place zero exactly at the neutral stop, and keep alpha formatting consistent with the existing palette functions. Include text-facing semantic labels or exported constants that allow the legend to state `Red = A higher`, `Neutral = no difference`, and `Blue = B higher`. Do not alter sequential field/legacy outputs or their stop ordering.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/palette.test.ts</verify>
  <acceptance_criteria>Tests assert exact endpoint/neutral color semantics, clamping, gradient generation, and unchanged sequential helper behavior; negative values never use the sequential warm palette.</acceptance_criteria>
  <done>Signed color meaning is centralized, deterministic, and reusable by the difference texture and legend.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run src/lib/kde/compute-slice-kde.test.ts src/lib/kde/index.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/lib/palette.test.ts`, `pnpm typecheck`, and `pnpm lint`. Confirm no API route or fetch path was added, the public `@/lib/kde` barrel exports `KdeField`, and existing imports of `computeSliceKde` still typecheck.
</verification>

<success_criteria>
The existing KDE computation exposes a complete raw field through the public barrel without regressing sparse rendering, and tested helpers provide a shared absolute domain plus a symmetric, red-neutral-blue signed field with explicit invalid-input behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-02-SUMMARY.md`.
</output>
