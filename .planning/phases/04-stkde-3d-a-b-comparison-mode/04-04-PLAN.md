---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - src/app/stkde-3d/lib/comparison-presets.ts
  - src/app/stkde-3d/lib/comparison-presets.test.ts
  - src/app/stkde-3d/lib/comparison-fixtures.ts
  - src/app/stkde-3d/components/StkdeDifferenceScene.tsx
  - src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx
  - src/app/stkde-3d/components/StkdeComparisonStage.tsx
  - src/app/stkde-3d/components/StkdeComparisonControls.tsx
  - src/app/stkde-3d/page.tsx
autonomous: true
requirements:
  - BURST-06
must_haves:
  truths:
    - "A-B replaces the two absolute panes with one top-down signed KDE(A)-KDE(B) field over the shared spatial extent."
    - "Difference mode is heatmap-only: events, trajectories, slabs, burst volumes, adaptive axis, and additive geometry are not mounted or presented as active, even when the selected preset declares a trajectory-capable absolute layer."
    - "The shipped catalog contains the concrete built-ins full-slice-02-vs-08 (dataset full, indices 1 and 7) and fourth-of-july-slice-03-vs-09 (dataset fourth-of-july, indices 2 and 8), with distinct expected labels and tested view/layer/parameter values."
    - "Preset epoch bounds are resolved from the actual rendered scene surfaces at runtime; no unstable production timestamp literals are invented. A deterministic ten-slice fixture validates the shipped catalog indices/labels and rejects missing, mismatched, or nearest-label pairs."
    - "Case-study changes clear stale references, while shared KDE/adaptive/renderer changes recompute the same pair without duplicate requests."
  artifacts:
    - path: "src/app/stkde-3d/lib/comparison-presets.ts"
      provides: "Code-defined concrete preset catalog, declarative index/label references, and strict runtime resolver"
      contains: "full-slice-02-vs-08"
    - path: "src/app/stkde-3d/lib/comparison-fixtures.ts"
      provides: "Deterministic zero/one/two/ten-slice fixture used to validate preset pairs without production epoch literals"
      contains: "buildComparisonSliceFixture"
    - path: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      provides: "Single top-down signed heatmap-only scene"
      contains: "signed-kde"
    - path: "src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx"
      provides: "Text-labeled red-neutral-blue sign legend"
      contains: "B higher"
  key_links:
    - from: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      to: "src/app/stkde-3d/lib/comparison-difference.ts"
      via: "raw aligned field subtraction and symmetric domain"
      pattern: "computeSignedKdeDifference"
    - from: "src/app/stkde-3d/page.tsx"
      to: "src/app/stkde-3d/lib/comparison-presets.ts"
      via: "deferred exact index/label resolution after the selected dataset has produced sceneSlices"
      pattern: "resolveComparisonPreset"
    - from: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      to: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      via: "difference mode branch that renders one field only"
      pattern: "difference"
    - from: "src/app/stkde-3d/lib/comparison-presets.test.ts"
      to: "src/app/stkde-3d/lib/comparison-fixtures.ts"
      via: "deterministic ten-slice catalog validation and runtime metadata resolution"
      pattern: "buildComparisonSliceFixture|sliceIndex"
---

<objective>
Complete the comparison contract with the signed difference view, strict reproducible presets, and dataset/parameter lifecycle handling.

Purpose: Make A-B analytically honest and repeatable. Difference is a spatial field, not a third temporal cube, and presets must identify exact rendered intervals without pretending quantile-derived production epochs are stable literals.
Output: One top-down heatmap-only difference scene, signed legend and controls, concrete exact-index built-in presets with deterministic fixture validation, runtime-resolved interval metadata, and invalidation wiring.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-03-SUMMARY.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/StkdeComparisonStage.tsx
@src/app/stkde-3d/components/StkdeComparisonControls.tsx
@src/app/stkde-3d/lib/comparison-difference.ts
@src/app/stkde-3d/lib/palette.ts
@src/app/stkde-3d/lib/mock-data.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Use the exact effective preset shape in `04-SPEC.md`, but separate declarative catalog identity from runtime-resolved metadata. The shipped catalog must use stable dataset preset IDs, exact zero-based slice indices, expected labels, view/layer/parameter values, and `camera: 'front-oblique'`; it must not contain guessed real-data epoch endpoints because `generateStkde3dRealData` derives ten slice bounds from record quantiles. The resolver output must populate `startEpoch` and `endEpoch` from the actual selected `sceneSlices`, carry their actual sourceSliceId, and show those values in the DOM. A mismatch in dataset ID, index, expected label, source identity, or distinctness is a visible comparison error, never a closest-label or silent fallback.

The concrete initial catalog is locked for this plan:

- `full-slice-02-vs-08`: `datasetPresetId: 'full'`, A `{ sliceIndex: 1, label: 'Slice 2' }`, B `{ sliceIndex: 7, label: 'Slice 8' }`, `view: 'absolute'`, `layer: 'heatmap-with-trajectories'`, experimental KDE parameters, adaptive time on, field renderer, front-oblique camera.
- `fourth-of-july-slice-03-vs-09`: `datasetPresetId: 'fourth-of-july'`, A `{ sliceIndex: 2, label: 'Slice 3' }`, B `{ sliceIndex: 8, label: 'Slice 9' }`, `view: 'difference'`, declared layer `heatmap-with-trajectories` (effective layer is always signed heatmap-only in difference mode), the same explicit analytical/camera fields.

`buildComparisonSliceFixture(0|1|2|10)` must use deterministic labels/source IDs and deterministic synthetic epoch bounds only in tests. Preset tests must assert the exact catalog IDs, dataset IDs, labels, `[1, 7]`/`[2, 8]` pairs, distinctness, and resolved fixture epochs; they must also assert that runtime resolution uses the actual fixture bounds rather than constants embedded in the catalog.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and strictly resolve the concrete built-in preset catalog</name>
  <files>src/app/stkde-3d/lib/comparison-presets.ts, src/app/stkde-3d/lib/comparison-presets.test.ts, src/app/stkde-3d/lib/comparison-fixtures.ts, src/app/stkde-3d/components/StkdeComparisonControls.tsx, src/app/stkde-3d/page.tsx</files>
  <action>
    Add a typed code-defined catalog with exactly the two initial IDs and pairs documented above. Represent each declarative interval with `sliceIndex` and expected `label`; do not invent literal production `startEpoch`/`endEpoch` values. Define `buildComparisonSliceFixture(count)` for deterministic 0/1/2/10 rendered slices and use its ten-slice form to validate every shipped catalog entry. Implement `resolveComparisonPreset(preset, datasetPresetId, sceneSlices)` that checks dataset ID, index bounds, expected label, distinct A/B indices, and source identity, then returns the effective spec-shaped preset/selection with `startEpoch`, `endEpoch`, and sourceSliceId copied from the actual rendered surfaces. Never match by label alone, nearest interval, or stale source ID. Tests must assert the exact IDs, dataset preset IDs, labels, expected index pairs `[1, 7]` and `[2, 8]`, the two resolved fixture endpoints, rejected index/label/dataset mismatches, and no missing catalog entries. Apply a selected preset only after its dataset has loaded and sceneSlices/KDE results exist; expose its exact dataset/pair in the selector and `data-comparison-preset-id` when active.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison-presets.test.ts && pnpm typecheck</verify>
  <acceptance_criteria>The tests fail if either concrete built-in ID, dataset ID, label, or distinct index pair is removed or changed; exact fixture bounds resolve successfully; runtime metadata comes from sceneSlices; endpoint/index/label/dataset mismatch rejects visibly; no closest-label or silent fallback is possible; preset view/layer/parameters/camera survive resolution.</acceptance_criteria>
  <done>Built-in presets can reproduce their declared dataset and exact rendered indices or fail visibly and deterministically without creating a new slice, request, or unstable timestamp contract.</done>
</task>

<task type="auto">
  <name>Task 2: Render the signed heatmap-only difference mode</name>
  <files>src/app/stkde-3d/components/StkdeDifferenceScene.tsx, src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx</files>
  <action>
    Add a single top-down difference scene that consumes the two selected raw fields, computes `KDE(A) - KDE(B)` through the tested helper, uses one symmetric zero-centered domain, and paints the dedicated red-neutral-blue palette over the same spatial extent/map context. Add the DOM strip `A − B difference` and `KDE(A) − KDE(B)`, `data-difference-field="signed-kde"`, and an expanded legend labeled `B higher`, `0 / no difference`, and `A higher` with the copy `Red = A higher · Neutral = no difference · Blue = B higher`. Own the signed legend's final UI-SPEC contract here: keep sign meaning redundant in accessible text, expose exact semantic labels, and never rely on color alone. In the difference branch mount only the geographic base map, signed field, and legend; explicitly omit `StkdeSliceStack`, raw events, full-source trajectories, burst volume, adaptive axis, additive geometry, and temporal-axis cues. Keep A/B interval metadata visible, switch modes through exact `Absolute` and `A − B difference` tabs, and semantically disable event/trajectory controls with `Unavailable in A − B difference view: signed heatmap only.` Render an all-zero field as neutral and missing/mismatched fields as an inline comparison error, never as neutral.
  </action>
  <verify>pnpm typecheck && pnpm vitest run src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>Difference mode has exactly one top-down field, symmetric sign semantics and text redundancy; the signed legend exposes accessible `B higher`, `0 / no difference`, and `A higher` labels without color-only meaning; no temporal/additive/event/trajectory/volume layers are mounted; actual A/B metadata is retained; and zero, missing, and mismatched data follow the UI-SPEC states.</acceptance_criteria>
  <done>The subtraction is presented as a truthful spatial contrast view rather than a 3D difference cube or another ten-slice stack, and trajectory-capable absolute presets do not weaken the difference-only restriction.</done>
</task>

<task type="auto">
  <name>Task 3: Wire preset application, invalidation, and shared-setting recomputation</name>
  <files>src/app/stkde-3d/page.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx</files>
  <action>
    Wire the page to the reusable absolute stage from Plan 03 while integrating the preset lifecycle with the existing case-study effect: clear A/B immediately when caseStudyPresetId changes, prevent stale geometry while the next dataset is loading, and resolve pending catalog entries only after the new sceneSlices and sliceKdeResults are ready. Apply dataset, actual runtime interval metadata, KDE parameters, adaptive-time, renderer, declared/effective layer, view, and front-oblique camera state as one preset transaction. Preserve valid A/B references when shared KDE, adaptive-time, renderer, opacity, matching, or inspector controls change; recompute both absolute panes or the same signed field from the new shared results without adding slices or calling a second data path. If exact resolution fails, keep the pair unselected and show `This comparison could not be resolved. Reset comparison and select two distinct intervals.` Do not allow a preset to silently fall back to a different pair. Keep difference mode's heatmap-only gate effective after every shared-setting update.
  </action>
  <verify>pnpm typecheck && pnpm vitest run src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>Case-study changes cannot render stale A/B intervals; shared analytical changes update both selected views without clearing or duplicating them; preset loading/error states and actual interval metadata are observable; the existing single dataset loading lifecycle remains the only data request path; difference mode never mounts event or trajectory layers.</acceptance_criteria>
  <done>Presets and invalidation are deterministic across asynchronous dataset replacement and ordinary shared-control changes, with exact pair identity preserved and no nearest-pair fallback.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/page.stkde.test.ts`, `pnpm typecheck`, and inspect the route effects for one existing dataset fetch lifecycle. Assert the catalog contains exactly `full-slice-02-vs-08` (`full`, `[1,7]`) and `fourth-of-july-slice-03-vs-09` (`fourth-of-july`, `[2,8]`), that runtime DOM metadata uses actual rendered bounds, and that difference mode does not mount any additive/event/trajectory/volume/axis scene layer.
</verification>

<success_criteria>
The phase supports both concrete exact-index preset absolute comparison and a single signed, heatmap-only A-B field. Preset resolution is strict and reproducible through deterministic fixture validation plus runtime surface metadata, stale selections are cleared on dataset changes, shared settings recompute the current pair without duplicate requests, and all signed/empty/error semantics are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-04-SUMMARY.md`.
</output>
