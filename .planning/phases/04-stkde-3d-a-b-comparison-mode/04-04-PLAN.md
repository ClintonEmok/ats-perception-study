---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - src/app/stkde-3d/lib/comparison-presets.ts
  - src/app/stkde-3d/lib/comparison-presets.test.ts
  - src/app/stkde-3d/components/StkdeDifferenceScene.tsx
  - src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx
  - src/app/stkde-3d/components/StkdeComparisonStage.tsx
  - src/app/stkde-3d/components/StkdeComparisonControls.tsx
  - src/app/stkde-3d/page.tsx
autonomous: true
requirements:
  - BURST-06
must_haves:
  truths:
    - "A-B replaces the two absolute panes with one top-down signed KDE(A)-KDE(B) field over the shared spatial extent."
    - "Difference mode is heatmap-only: events, trajectories, slabs, burst volumes, adaptive axis, and additive geometry are not mounted or presented as active."
    - "A built-in exact-pair preset reproduces dataset, interval identities, KDE parameters, adaptive time, renderer, layer, view, and front-oblique camera state, or fails visibly if exact bounds do not resolve."
    - "Case-study changes clear stale references, while shared KDE/adaptive/renderer changes recompute the same pair without duplicate requests."
  artifacts:
    - path: "src/app/stkde-3d/lib/comparison-presets.ts"
      provides: "Code-defined exact-pair preset contract and strict resolver"
      contains: "datasetPresetId"
    - path: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      provides: "Single top-down signed heatmap-only scene"
      contains: "signed-kde"
    - path: "src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx"
      provides: "Text-labeled red-neutral-blue sign legend"
      contains: "B higher"
  key_links:
    - from: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      to: "src/app/stkde-3d/lib/comparison-difference.ts"
      via: "raw aligned field subtraction and symmetric domain"
      pattern: "computeSignedKdeDifference"
    - from: "src/app/stkde-3d/page.tsx"
      to: "src/app/stkde-3d/lib/comparison-presets.ts"
      via: "deferred exact-pair resolution after the selected dataset has produced sceneSlices"
      pattern: "resolveComparisonPreset"
    - from: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      to: "src/app/stkde-3d/components/StkdeDifferenceScene.tsx"
      via: "difference mode branch that renders one field only"
      pattern: "difference"
---

<objective>
Complete the comparison contract with the signed difference view, strict reproducible presets, and dataset/parameter lifecycle handling.

Purpose: Make A-B analytically honest and repeatable. Difference is a spatial field, not a third temporal cube, and presets must identify exact rendered intervals rather than trusting display labels.
Output: One top-down heatmap-only difference scene, signed legend and controls, exact-pair preset definitions/resolution, and invalidation/loading/error behavior wired into the existing route.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-03-SUMMARY.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/StkdeComparisonStage.tsx
@src/app/stkde-3d/components/StkdeComparisonControls.tsx
@src/app/stkde-3d/lib/comparison-difference.ts
@src/app/stkde-3d/lib/palette.ts
@src/app/stkde-3d/lib/mock-data.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Use the exact preset shape in `04-SPEC.md`. Built-ins must contain literal, distinct `sliceIndex`, `startEpoch`, `endEpoch`, and label values for both intervals. At implementation time, capture the current deterministic endpoints from the route's loaded case-study slices and check them into the preset definitions/tests; do not derive pairs from labels or silently select the closest interval. If a declared pair cannot validate against the current dataset, show the documented comparison error and leave the pair unselected.

Difference mode keeps the geographic base map as context but renders only one signed heatmap and its legend. It must not mount `StkdeSliceStack`, raw events, trajectories, burst volume, adaptive axis, additive overlays, or a temporal-axis cue. Keep A/B metadata in DOM controls. Difference disables event/trajectory controls with `Unavailable in A − B difference view: signed heatmap only.`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and strictly resolve built-in exact-pair presets</name>
  <files>src/app/stkde-3d/lib/comparison-presets.ts, src/app/stkde-3d/lib/comparison-presets.test.ts, src/app/stkde-3d/components/StkdeComparisonControls.tsx, src/app/stkde-3d/page.tsx</files>
  <action>
    Add code-defined presets containing id, label, datasetPresetId, literal A/B index/start/end/label, absolute or difference view, full KDE parameters, adaptive-time flag, field/legacy renderer, absolute layer, and front-oblique camera. Ship a small set of exact distinct pairs from the existing ten-slice case-study output; record the literal endpoint values captured from the current deterministic loader and test those values. Implement a resolver that validates dataset preset, index, sourceSliceId when available, exact start/end epochs, and non-equality before returning `ComparisonSelection`; never match by label or nearest interval. Apply a selected preset only after its dataset has loaded and sceneSlices/KDE results exist, show `Loading comparison preset…` during that wait, and show the exact comparison error instead of falling back when validation fails. Expose the exact dataset/pair in the selector label and `data-comparison-preset-id` when active.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison-presets.test.ts && pnpm typecheck</verify>
  <acceptance_criteria>Tests prove exact pair success, distinctness enforcement, endpoint/index mismatch rejection, parameter/layer/view preservation, deferred dataset resolution, and no closest-label fallback; the preset selector exposes reproducible identity rather than only a nickname.</acceptance_criteria>
  <done>Built-in presets can reproduce a declared pair or fail visibly and deterministically without creating a new slice or request.</done>
</task>

<task type="auto">
  <name>Task 2: Render the signed heatmap-only difference mode</name>
  <files>src/app/stkde-3d/components/StkdeDifferenceScene.tsx, src/app/stkde-3d/components/StkdeSignedDifferenceLegend.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx</files>
  <action>
    Add a single top-down difference scene that consumes the two selected raw fields, computes `KDE(A) - KDE(B)` through the tested helper, uses one symmetric zero-centered domain, and paints the dedicated red-neutral-blue palette over the same spatial extent/map context. Add the DOM strip `A − B difference` and `KDE(A) − KDE(B)`, `data-difference-field="signed-kde"`, and an expanded legend labeled `B higher`, `0 / no difference`, and `A higher` with the copy `Red = A higher · Neutral = no difference · Blue = B higher`. In the difference branch mount only the geographic base map, signed field, and legend; omit focused slabs, events, trajectories, burst volume, adaptive axis, and additive geometry. Keep A/B interval metadata visible, switch modes through exact `Absolute` and `A − B difference` tabs, and semantically disable event/trajectory controls with the required helper copy. Render an all-zero field as neutral and missing/mismatched fields as an inline comparison error, never as neutral.
  </action>
  <verify>pnpm typecheck && pnpm vitest run src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>Difference mode visibly has one top-down field, symmetric sign semantics and text redundancy, no temporal/additive overlays, retains A/B metadata, and handles zero, missing, and mismatched data according to the UI-SPEC states.</acceptance_criteria>
  <done>The subtraction is presented as a truthful spatial contrast view rather than a 3D difference cube or another ten-slice stack.</done>
</task>

<task type="auto">
  <name>Task 3: Wire preset application, invalidation, and shared-setting recomputation</name>
  <files>src/app/stkde-3d/page.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonControls.tsx</files>
  <action>
    Integrate the preset lifecycle with the existing case-study effect: clear A/B immediately when caseStudyPresetId changes, prevent stale geometry while the next dataset is loading, and resolve pending exact pairs only after the new sceneSlices and sliceKdeResults are ready. Apply dataset, interval metadata, KDE parameters, adaptive-time, renderer, layer, view, and front-oblique camera state as one preset transaction. Preserve valid A/B references when shared KDE, adaptive-time, renderer, opacity, matching, or inspector controls change; recompute both absolute panes or the same signed field from the new shared results without adding slices or calling a second data path. Expose `data-render-status` for loading/ready/empty/error, retain fixed stage dimensions during loading, show retry/mock-data warnings through the existing route conventions, and cover zero/one/partial/preset-error states with the exact UI-SPEC copy.
  </action>
  <verify>pnpm typecheck && pnpm vitest run src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>Case-study changes cannot render stale A/B intervals, shared analytical changes update both selected views without clearing or duplicating them, preset loading/error states are observable, and the existing single `/api/crimes/range` loading loop remains the only data request path.</acceptance_criteria>
  <done>Presets and invalidation are deterministic across asynchronous dataset replacement and ordinary shared-control changes.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run src/app/stkde-3d/lib/comparison-presets.test.ts src/app/stkde-3d/lib/comparison-difference.test.ts src/app/stkde-3d/page.stkde.test.ts`, `pnpm typecheck`, and inspect the route effects for one existing dataset fetch lifecycle. Confirm difference mode does not mount any additive/event/trajectory/volume scene layer.
</verification>

<success_criteria>
The phase supports both exact-preset absolute comparison and a single signed, heatmap-only A-B field. Preset resolution is strict and reproducible, stale selections are cleared on dataset changes, shared settings recompute the current pair without duplicate requests, and all signed/empty/error semantics are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-04-SUMMARY.md`.
</output>
