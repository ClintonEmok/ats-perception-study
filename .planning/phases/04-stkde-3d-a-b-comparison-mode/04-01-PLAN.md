---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/stkde-3d/lib/comparison.ts
  - src/app/stkde-3d/lib/comparison.test.ts
  - src/app/stkde-3d/components/StkdeComparisonControls.tsx
  - src/app/stkde-3d/page.tsx
  - src/app/stkde-3d/components/Stkde3DScene.tsx
  - src/app/stkde-3d/components/StkdeSliceStack.tsx
  - src/app/stkde-3d/components/Stkde3DSceneProvider.tsx
  - src/app/stkde-3d/page.stkde.test.ts
autonomous: true
requirements:
  - BURST-06
must_haves:
  truths:
    - "The default /stkde-3d route remains the existing full ten-slice stack and does not create comparison state until Compare intervals is activated."
    - "A first interval click or keyboard selection fills temporary A and automatically prompts for B."
    - "A second selection must be a different rendered interval; after B is selected the pair is locked until Reset comparison."
    - "Changing case study or invalidating the loaded dataset clears both references without adding duplicate slices or issuing comparison fetches."
  artifacts:
    - path: "src/app/stkde-3d/lib/comparison.ts"
      provides: "Typed temporary comparison state machine and identity-checked transitions"
      contains: "ComparisonSelection"
    - path: "src/app/stkde-3d/components/StkdeComparisonControls.tsx"
      provides: "Keyboard-accessible interval picker, A/B metadata, prompts, and reset/exit actions"
      contains: "Select interval A"
    - path: "src/app/stkde-3d/page.tsx"
      provides: "Route-local comparison lifecycle connected to the existing rendered slices"
      contains: "Compare intervals"
  key_links:
    - from: "src/app/stkde-3d/page.tsx"
      to: "src/app/stkde-3d/lib/comparison.ts"
      via: "route-local transition calls using sceneSlices sourceSliceId and epoch bounds"
      pattern: "selectComparisonSlice|resetComparison|invalidateComparison"
    - from: "src/app/stkde-3d/components/StkdeSliceStack.tsx"
      to: "src/app/stkde-3d/page.tsx"
      via: "existing onSliceSelect runtime callback, preserving sourceSliceId"
      pattern: "sourceSliceId"
---

<objective>
Ship the first tracer slice for comparison: a temporary, route-local A/B selection flow over the already rendered ten-slice stack.

Purpose: Prove the locked selection lifecycle without introducing a persistent slice domain, a duplicate slice collection, or a second data request. This plan deliberately leaves the existing stack as the visible stage; later plans add the paired and difference renderers.
Output: Tested comparison state transitions, an accessible interval picker and metadata rail, and stack clicks that assign A then B while preserving the default workflow.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/Stkde3DScene.tsx
@src/app/stkde-3d/components/StkdeSliceStack.tsx
@src/app/stkde-3d/components/Stkde3DSceneProvider.tsx
@src/app/stkde-3d/lib/types.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
The route currently owns activeIndex, playback, case-study loading, ten sceneSlices, and the only scene runtime. Keep comparison state in this route or a route-local helper; do not use Zustand, the dashboard slice domain, a database object, or an API route. `sourceSliceId` plus index and epoch bounds are the identity, not a focused array index. Preserve the existing stack, scrubber, KDE controls, adaptive time, events, trajectories, and renderer controls when comparison is inactive.

The UI contract requires exact copy and semantics: `Compare intervals`, `Select interval A`, `Select interval B`, `Reset comparison`, `Back to stack`, `A/B comparison ready`, an `aria-live="polite"` status, a DOM button for every rendered interval, and explicit zero/one/partial states. Apply the UI-SPEC's 44px narrow hit-area and long-label accessibility rules without redesigning the route.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the temporary comparison state machine</name>
  <files>src/app/stkde-3d/lib/comparison.ts, src/app/stkde-3d/lib/comparison.test.ts</files>
  <action>
    Define the locked `ComparisonSelection` and `Stkde3DComparisonState` contracts in a pure route-local module. Implement initial state, enter/exit comparison, selection, reset, invalidation, and absolute/difference mode transitions. A selection while activeSlot A stores A and advances to B; a B selection is accepted only when its sourceSliceId differs from A, with rendered index as the fallback identity check when an ID is absent. Once both slots exist, transition to absolute and ignore further surface selections until explicit Reset comparison. Reset must clear A and B, return to selecting/A, and invalidate must do the same while preserving no data objects. Return a stable duplicate-selection message key so the UI can announce `Choose a different interval for B.` without mutating state.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison.test.ts</verify>
  <acceptance_criteria>Unit tests cover initial state, A to B auto-advance, duplicate ID and duplicate-index rejection, completed-pair locking, reset, dataset invalidation, explicit difference mode, and preservation of exact source ID/start/end metadata.</acceptance_criteria>
  <done>The pure module can express every selection lifecycle transition without importing React, Zustand, Three.js, fetch, or a slice-domain action.</done>
</task>

<task type="auto">
  <name>Task 2: Add the accessible selection rail and route lifecycle</name>
  <files>src/app/stkde-3d/components/StkdeComparisonControls.tsx, src/app/stkde-3d/page.tsx</files>
  <action>
    Add a route-local comparison section using existing native/shadcn semantics rather than a new UI library. Render a visible `Compare intervals` entry point, selection prompt, pair-ready status, A and B metadata cards, and a scrollable DOM interval picker whose button names include slot eligibility, interval label, full start/end values, and event count. Wire Compare to pause playback and enter selecting/A; wire Reset comparison to the pure transition and announce the exact reset copy; wire Back to stack to leave comparison without changing dataset, KDE settings, or persistent slices. Handle zero intervals by showing `No intervals available` and disabling entry, one interval by showing `Comparison needs at least two rendered intervals.`, and a selected-A/no-B state by retaining the stack and prompting for B. Add an `aria-live="polite"` status that announces selection, duplicate rejection, reset, and dataset invalidation. Keep comparison state temporary and derive all picker entries directly from `sceneSlices`.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/page.stkde.test.ts</verify>
  <acceptance_criteria>The rail exposes the exact locked labels and A/B metadata, keyboard users can assign both slots without touching WebGL, Compare pauses playback, Back to stack restores the ordinary stack flow, and no control creates or persists a new slice.</acceptance_criteria>
  <done>The route has a usable selecting tracer with explicit status and partial/empty behavior while the existing ten rendered surfaces remain the only interval source.</done>
</task>

<task type="auto">
  <name>Task 3: Route stack surface clicks into A/B selection without breaking ordinary slice interaction</name>
  <files>src/app/stkde-3d/components/Stkde3DScene.tsx, src/app/stkde-3d/components/StkdeSliceStack.tsx, src/app/stkde-3d/components/Stkde3DSceneProvider.tsx, src/app/stkde-3d/page.tsx, src/app/stkde-3d/page.stkde.test.ts</files>
  <action>
    Add an explicit comparison-selection prop/runtime branch that lets a rendered surface call the route's comparison transition with its stable sourceSliceId, interval bounds, and event metadata. In selecting mode, highlight the selected source intervals using the existing warm accent treatment, suppress resize handles and focused-camera jumps so a selection click means assignment, and do not let the compact focus index overwrite source identity. In completed absolute/difference modes, ignore new surface assignments until Reset comparison. Leave all existing stack-mode callbacks, activeIndex updates, hover handling, playback interpolation, and resize behavior unchanged when comparison is inactive. Extend the source-contract test to assert the Compare/selection labels, source identity path, and absence of dashboard/Zustand comparison stores.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison.test.ts && pnpm typecheck</verify>
  <acceptance_criteria>A click on an existing surface assigns A then B exactly once, duplicate B leaves A unchanged with the documented message, completed pairs cannot be silently replaced, and ordinary stack clicks still update activeIndex/focus as before.</acceptance_criteria>
  <done>The first end-to-end tracer is verified from rendered surface or keyboard picker to temporary route state, with no duplicate request or persistent slice mutation.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run src/app/stkde-3d/lib/comparison.test.ts src/app/stkde-3d/page.stkde.test.ts` and `pnpm typecheck`. Confirm the existing stack remains the default route output and that the new tests distinguish zero, one, A-pending-B, duplicate, completed-lock, reset, and invalidation states.
</verification>

<success_criteria>
The `/stkde-3d` route has a tested, accessible, temporary A/B selection tracer over the existing rendered intervals. It auto-advances A to B, rejects same-interval pairs, locks completed pairs until Reset comparison, clears on case-study changes, and leaves the normal stack/data-request path unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-01-SUMMARY.md`.
</output>
