---
phase: 04-stkde-3d-a-b-comparison-mode
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/app/stkde-3d/components/Stkde3DScene.tsx
  - src/app/stkde-3d/components/StkdeComparisonStage.tsx
  - src/app/stkde-3d/components/StkdeComparisonViewport.tsx
  - src/app/stkde-3d/components/StkdeSliceStack.tsx
  - src/app/stkde-3d/components/Stkde3DSceneProvider.tsx
  - src/app/stkde-3d/components/HotspotTrajectoryOverlay.tsx
  - src/app/stkde-3d/lib/raw-events.ts
  - src/app/stkde-3d/components/StkdeIntensityLegend.tsx
  - src/app/stkde-3d/lib/comparison-camera.ts
  - src/app/stkde-3d/lib/comparison-camera.test.ts
autonomous: true
requirements:
  - BURST-06
must_haves:
  truths:
    - "A completed pair renders exactly two focused absolute viewports with A above B, each showing one selected surface and the same spatial/analytical context."
    - "Focused surface and event rendering use the selected original source slice index, never the compact local index 0."
    - "When the absolute layer permits trajectories, each pane receives explicit full source-slice trajectory data and renders epoch placement/source IDs correctly instead of suppressing HotspotTrajectoryOverlay in focus mode."
    - "Absolute panes use one shared domain and the current field/legacy sequential palette; there is no additive A+B geometry or second full ten-slice cube."
    - "Linked cameras default on, mirror orientation/target/zoom in either direction, can be disabled for independent inspection, and snap B to A when re-enabled."
    - "The comparison stage remains vertical and usable below lg, with explicit pane heights so R3F never receives an auto-height parent."
  artifacts:
    - path: "src/app/stkde-3d/lib/comparison-source-context.ts"
      provides: "Source-aware focused A/B context separating selected-slice data from full trajectory data"
      contains: "sourceSliceIndex"
    - path: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      provides: "Absolute comparison orchestration and stable comparison-stage markers"
      contains: "data-comparison-mode"
    - path: "src/app/stkde-3d/components/StkdeComparisonViewport.tsx"
      provides: "One focused Canvas and CameraControls surface for a selected A or B interval"
      contains: "CameraControls"
    - path: "src/app/stkde-3d/components/HotspotTrajectoryOverlay.tsx"
      provides: "Optional full-source trajectory rendering in focused absolute panes with source-aware payloads"
      contains: "sourceSliceIndex"
    - path: "src/app/stkde-3d/lib/comparison-camera.ts"
      provides: "Imperative pose-link policy with loop suppression and reset behavior"
      contains: "setLookAt"
  key_links:
    - from: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      to: "src/app/stkde-3d/components/StkdeComparisonViewport.tsx"
      via: "same scene context and separate pane refs for A and B"
      pattern: "interval-slot"
    - from: "src/app/stkde-3d/components/StkdeComparisonViewport.tsx"
      to: "src/app/stkde-3d/lib/comparison-source-context.ts"
      via: "selected source index resolves focused surface, event, and metadata data"
      pattern: "sourceSliceIndex|selectedEvents"
    - from: "src/app/stkde-3d/components/StkdeComparisonViewport.tsx"
      to: "src/app/stkde-3d/components/HotspotTrajectoryOverlay.tsx"
      via: "full source slices/results are supplied separately from the selected focused slice"
      pattern: "trajectory.*source|sourceSlices|sourceSliceResults"
    - from: "src/app/stkde-3d/components/StkdeComparisonViewport.tsx"
      to: "src/app/stkde-3d/lib/comparison-camera.ts"
      via: "imperative CameraControls pose propagation, not per-frame React state"
      pattern: "linkedCameras|setLookAt"
    - from: "src/app/stkde-3d/page.tsx"
      to: "src/app/stkde-3d/components/StkdeComparisonStage.tsx"
      via: "selected source slices, raw KDE results, shared warp/domain, and existing toggles"
      pattern: "StkdeComparisonStage"
---

<objective>
Render the selected pair as matched, vertically stacked focused absolute views with shared data context, source-correct overlays, and linked cameras.

Purpose: Turn the selection tracer into a truthful A/B inspection surface while reusing the current R3F scene primitives and one loaded analytical pipeline. The extraction must be incremental: keep the ordinary `Stkde3DScene` stack path intact and share one map capture rather than mounting two complete map-owning scenes.
Output: Reusable focused viewport/stage components, shared absolute scaling, explicit source-index remapping for surfaces/events/trajectories, preserved source identity, and tested imperative camera synchronization.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-CONTEXT.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-RESEARCH.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-UI-SPEC.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-01-SUMMARY.md
@.planning/phases/04-stkde-3d-a-b-comparison-mode/04-02-SUMMARY.md
@src/app/stkde-3d/page.tsx
@src/app/stkde-3d/components/Stkde3DScene.tsx
@src/app/stkde-3d/components/StkdeSliceStack.tsx
@src/app/stkde-3d/components/Stkde3DSceneProvider.tsx
@src/app/stkde-3d/components/HotspotTrajectoryOverlay.tsx
@src/app/stkde-3d/lib/raw-events.ts
@src/app/stkde-3d/components/StkdeIntensityLegend.tsx
@src/app/stkde-3d/lib/palette.ts
@src/app/stkde-3d/lib/comparison-difference.ts
</read_first>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
The current `Stkde3DScene` owns an invisible MapLibre capture and one Canvas, while `SceneContent` compacts focused slices to index 0. Extract reusable scene layers so a comparison stage owns one captured map texture and passes the same texture into two focused viewport Canvases. A focused pane may use local render coordinates, but every callback and DOM marker must retain the original `sourceSliceId`, original source index, and epoch metadata.

The source-aware overlay decision is deliberate and required: focused A/B surface rendering receives one selected source slice and one selected KDE/event group; it must not read `sliceEvents[0]` merely because the focused render index is zero. Optional absolute trajectory rendering receives the full source slice array and full hotspot result map as a separate prop, maps every snapshot back to its source slice ID/index, and uses the shared epoch resolver for Y placement. Remove the existing `viewMode === 'focus'` early return from `HotspotTrajectoryOverlay`; do not silently restrict a trajectory-capable absolute preset. Difference mode remains heatmap-only and is gated separately in Plan 04.
</context>

<tasks>

  <task type="auto">
    <name>Task 1: Apply source-aware context to focused scene overlays</name>
    <files>src/app/stkde-3d/components/StkdeSliceStack.tsx, src/app/stkde-3d/components/Stkde3DSceneProvider.tsx, src/app/stkde-3d/lib/raw-events.ts, src/app/stkde-3d/components/HotspotTrajectoryOverlay.tsx, src/app/stkde-3d/components/Stkde3DScene.tsx</files>
    <action>
      Consume the tested source-context resolver from Plan 01 rather than recreating source identity in the viewport layer. Update interaction payload types to carry `sourceSliceIndex` wherever focused rendering remaps `index`/`renderedIndex`. Make `RawEventPoints` consume the resolver's selected source event group (or an explicit selected source index) so A/B events cannot read the wrong group. Make `HotspotTrajectoryOverlay` consume the resolver's full source slices/results even when its focused pane has one local surface: map snapshots by source ID, retain epoch bounds, and emit cluster hover/select payloads with the original sourceSliceId and sourceSliceIndex. Remove only the focus-mode suppression; do not duplicate data or fabricate local trajectory IDs. Use the source-aware tests from Plan 01 as the pure context contract, then verify the overlay payload/source-index mapping through the existing raw-event/integration coverage while preserving ordinary stack behavior and existing timestamp/Y resolution.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison-source-context.test.ts src/app/stkde-3d/lib/event-data.test.ts src/app/stkde-3d/lib/raw-events.test.ts && pnpm typecheck</verify>
  <acceptance_criteria>Tests prove source slice 7 still drives focused surface/events when its local render index is 0; trajectory snapshots retain their full-source epoch and ID; hotspot payloads expose the original source index; focused absolute rendering no longer returns an empty trajectory list solely because `viewMode` is focus; stack-mode event/trajectory behavior remains unchanged.</acceptance_criteria>
  <done>Focused A/B data and optional full-source trajectories have an explicit, tested remapping boundary instead of relying on compact array positions.</done>
</task>

  <task type="auto">
    <name>Task 2: Build the vertical absolute comparison stage with source-correct overlays</name>
    <files>src/app/stkde-3d/components/Stkde3DScene.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx, src/app/stkde-3d/components/StkdeComparisonViewport.tsx, src/app/stkde-3d/components/StkdeIntensityLegend.tsx</files>
  <action>
    Add an absolute stage that renders exactly two rows in a vertical CSS grid: A on top and B below. Each row must have a 32px DOM metadata strip with slot, interval label, full start/end, event count, `data-interval-slot`, and `data-source-slice-id`, followed by a focused Canvas with the same map extent, projection, adaptive-time setting, KDE parameters, renderer, and shared absolute domain. Mount one shared sequential STKDE legend in stage chrome, not one legend per pane. Each viewport must pass the source-aware focused context to its surface and event layers and pass the full source slice/results context to `HotspotTrajectoryOverlay` when the absolute layer/toggle enables trajectories; this is the explicit way a trajectory-capable preset reproduces trajectories in focused panes. Do not pass a compact one-item result map to the trajectory layer. Add `data-comparison-stage`, `data-comparison-mode="absolute"`, and readiness markers. Use `min-h-0 min-w-0`, a 20rem desktop row minimum, and a 320px narrow-pane minimum while preserving the existing lg two-column page frame and below-lg vertical rail order.
  </action>
  <verify>pnpm typecheck && pnpm vitest run src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison-source-context.test.ts</verify>
  <acceptance_criteria>With two selections, A and B appear simultaneously in the required order; both panes use shared analytical props/domain and exact runtime metadata; selected source 7 shows source-7 events even though the focused surface is local index 0; a trajectory-enabled absolute pane renders full-source trajectory epochs/IDs; the ordinary stack remains the default before Compare or after Back to stack; no additive A+B geometry or second full cube is mounted.</acceptance_criteria>
  <done>Absolute mode is a matched two-pane view with an explicit source-aware overlay contract, stable screenshot/accessibility markers, and no horizontal overflow at the specified breakpoints.</done>
</task>

<task type="auto">
    <name>Task 3: Implement linked, independent, and reset camera behavior</name>
    <files>src/app/stkde-3d/lib/comparison-camera.ts, src/app/stkde-3d/lib/comparison-camera.test.ts, src/app/stkde-3d/components/StkdeComparisonViewport.tsx, src/app/stkde-3d/components/StkdeComparisonStage.tsx</files>
  <action>
    Define a serializable camera pose and an imperative controller that reads the source CameraControls position/target, propagates to the other pane with transition disabled, and suppresses mirrored update feedback loops using an origin guard or epsilon comparison. Default linkedCameras to true for a completed pair. When link changes false to true, immediately read A and snap B to A before future propagation. When false, leave the last matched pose intact and allow independent interaction. Implement Reset views by applying the exact front-oblique position/target to both controls; use the existing smooth transition unless reduced motion is active. Expose `data-camera-linked="true|false"` and preserve camera poses outside React render state. Camera behavior is absolute-only; the difference stage will have one separate top-down camera.
  </action>
  <verify>pnpm vitest run src/app/stkde-3d/lib/comparison-camera.test.ts && pnpm typecheck</verify>
  <acceptance_criteria>Pure tests cover bidirectional propagation, feedback-loop suppression, unlink preservation, relink snap-to-A, default enabled state, and reset-to-front-oblique; the viewport uses current CameraControls APIs rather than custom pointer math or deprecated damping props.</acceptance_criteria>
  <done>Absolute panes are matched by default, can diverge only when explicitly unlinked, and can be returned to the same known pose without render storms.</done>
</task>

</tasks>

<verification>
Run `pnpm typecheck`, `pnpm vitest run src/app/stkde-3d/page.stkde.test.ts src/app/stkde-3d/lib/comparison-source-context.test.ts src/app/stkde-3d/lib/comparison-camera.test.ts`, and inspect the source for exactly one MapLibre capture owner in comparison mode. Confirm focused event access uses the selected source index, trajectory overlays use the full source context with epoch/source IDs, no focus-mode trajectory suppression remains, no new `/api` request or Zustand comparison state exists, and no difference scene is mounted by this plan.
</verification>

<success_criteria>
Selecting A and B now produces a truthful vertical absolute comparison with one shared analytical/map context, one shared sequential legend/domain, source-correct focused events and surfaces, optional full-source trajectories in both panes, stable source identity, and linked/independent/reset camera behavior covered by pure tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stkde-3d-a-b-comparison-mode/04-03-SUMMARY.md`.
</output>
