---
phase: 81-reduce-dashboard-memory-pressure-by-separating-overview-deta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/lib/queries.ts
  - src/lib/queries/index.ts
  - src/lib/queries/builders.ts
  - src/lib/queries/types.ts
  - src/app/api/crime/meta/route.ts
  - src/app/api/crime/overview/route.ts
  - src/app/api/crime/meta/route.test.ts
  - src/app/api/crime/overview/route.test.ts
autonomous: true
must_haves:
  truths:
    - Dashboard startup summary requests return quickly from persisted DuckDB tables instead of rescanning the CSV on every page load.
    - Opening the dashboard can retrieve filter-aware overview bins and dataset metadata without first materializing full detail rows in the browser.
    - Overview data arrives as pre-binned counts that downstream UI can render directly rather than rebucketing a large raw timestamp payload.
  artifacts:
    - path: src/lib/db.ts
      provides: persisted DuckDB fact/derived table bootstrap with dataset fingerprint invalidation
    - path: src/app/api/crime/meta/route.ts
      provides: metadata and facet reads backed by persisted DuckDB tables
    - path: src/app/api/crime/overview/route.ts
      provides: pre-binned overview counts backed by persisted DuckDB tables
  key_links:
    - from: src/lib/db.ts
      to: src/app/api/crime/meta/route.ts
      via: persisted metadata table query helpers
      pattern: crime_dataset_meta|ensure.*meta
    - from: src/lib/db.ts
      to: src/app/api/crime/overview/route.ts
      via: persisted overview bins table query helpers
      pattern: crime_overview_bins_medium|ensure.*overview
---

<objective>
Build the persisted DuckDB storage foundation Phase 81 needs, then cut the summary API hot paths over to it.

Purpose: Remove repeated CSV scan cost from dashboard startup before any client-side summary/detail refactor begins.
Output: Persisted DuckDB fact + derived summary tables, plus `/api/crime/meta` and `/api/crime/overview` reading from those tables.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-CONTEXT.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-RESEARCH.md
@src/lib/db.ts
@src/lib/queries.ts
@src/app/api/crime/meta/route.ts
@src/app/api/crime/overview/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist the canonical DuckDB fact table and derived summary tables</name>
  <files>src/lib/db.ts, src/lib/queries.ts, src/lib/queries/index.ts, src/lib/queries/builders.ts, src/lib/queries/types.ts</files>
  <action>Extend the existing DuckDB bootstrap beyond `ensureSortedCrimesTable()` so Phase 81 has one persisted canonical fact table plus the derived tables needed for startup summary reads. Keep the current offline DuckDB model; do not introduce Parquet, JSON caches, background services, or a second storage layer. Add a stable row identifier and normalized event-time columns on the persisted base table so later exact paging can sort safely by `(timestamp, rowId)`. Add dataset fingerprint/version tracking so derived tables rebuild once per source-data change, not once per request. Materialize at least one metadata table and one fixed medium-resolution overview-bins table keyed for the locked crime-type and district filters.</action>
  <verify>Run `pnpm exec tsc --noEmit` and confirm the new bootstrap/query helpers type-check. With `USE_MOCK_DATA=false pnpm dev` running, hit one summary endpoint twice and confirm logs show persisted-table reuse on the second request instead of a second CSV rebuild.</verify>
  <done>A persisted DuckDB fact table, metadata table, and overview bins table exist behind query helpers, and rebuilds are tied to dataset change rather than request frequency.</done>
</task>

<task type="auto">
  <name>Task 2: Cut `/api/crime/meta` and `/api/crime/overview` over to persisted summary reads</name>
  <files>src/app/api/crime/meta/route.ts, src/app/api/crime/overview/route.ts, src/app/api/crime/meta/route.test.ts, src/app/api/crime/overview/route.test.ts</files>
  <action>Rewrite both summary routes so they query only the persisted DuckDB tables created in Task 1. Remove request-time `read_csv_auto(...)` work from these hot paths while keeping current route URLs and mock fallback behavior intact. Change the overview contract to server-binned counts plus explicit domain/bin metadata. Ensure the overview route accepts only the locked summary filters (`crimeTypes`, `districts`) and does not incorporate viewport bounds into the aggregate lookup contract.</action>
  <verify>Run `pnpm vitest src/app/api/crime/meta/route.test.ts src/app/api/crime/overview/route.test.ts`. Add assertions proving overview responses no longer expose `timestampsSec[]`, still preserve mock fallback headers, and only vary when crime-type/district filters change.</verify>
  <done>`/api/crime/meta` and `/api/crime/overview` serve persisted DuckDB-backed summary data, no longer rescan CSVs on request, and emit pre-binned overview payloads suitable for direct UI rendering.</done>
</task>

</tasks>

<verification>
- `pnpm vitest src/app/api/crime/meta/route.test.ts src/app/api/crime/overview/route.test.ts`
- `pnpm exec tsc --noEmit`
- Manual spot check with `pnpm dev`: request `/api/crime/meta` and `/api/crime/overview` twice and confirm persisted-table reuse on repeat requests.
</verification>

<success_criteria>
1. Summary startup endpoints are backed by persisted DuckDB tables rather than repeated request-time CSV scans.
2. Overview responses contain pre-binned counts and domain metadata instead of sampled raw timestamps.
3. The persisted base table includes the stable ordering primitives needed for later exact paging work.
</success_criteria>

<output>
After completion, create `.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-01-SUMMARY.md`
</output>
