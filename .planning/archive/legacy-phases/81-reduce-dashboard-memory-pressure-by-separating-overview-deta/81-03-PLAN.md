---
phase: 81-reduce-dashboard-memory-pressure-by-separating-overview-deta
plan: 03
type: execute
wave: 3
depends_on:
  - 81-01
  - 81-02
files_modified:
  - src/lib/queries.ts
  - src/lib/queries/index.ts
  - src/lib/queries/builders.ts
  - src/lib/queries/types.ts
  - src/app/api/crimes/range/route.ts
  - src/app/api/crimes/range/route.test.ts
  - src/types/crime.ts
  - src/hooks/useCrimeData.ts
  - src/hooks/useCrimeData.test.ts
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.ts
  - src/store/useDashboardDemoTimeslicingModeStore.test.ts
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
autonomous: false
must_haves:
  truths:
    - The first exact detail fetch happens only after the user narrows the timeline and targets a bounded working window instead of the whole dataset.
    - Exact detail results page progressively with `hasMore`/`nextCursor` metadata or return a narrowing prompt instead of sampled/truncated results.
    - When multiple slices or detail consumers compete for exact data, the active or visible slice gets populated first so the user sees the focused slice resolve before background comparisons.
  artifacts:
    - path: src/app/api/crimes/range/route.ts
      provides: exact paged range API with guardrails and cursor metadata
    - path: src/hooks/useCrimeData.ts
      provides: client contract for bounded exact working-window fetches
    - path: src/store/useDashboardDemoTimeslicingModeStore.ts
      provides: migrated exact-detail generation flow using the paged range contract
    - path: src/components/dashboard-demo/DemoInspectPanel.tsx
      provides: active-slice-first exact detail consumption
  key_links:
    - from: src/app/api/crimes/range/route.ts
      to: src/lib/queries.ts
      via: exact `LIMIT pageSize + 1` paging over the persisted fact table
      pattern: hasMore|nextCursor|requiresNarrowing
    - from: src/hooks/useCrimeData.ts
      to: /api/crimes/range
      via: explicit `startEpoch`/`endEpoch` + cursor-based requests
      pattern: /api/crimes/range
    - from: src/components/dashboard-demo/DemoInspectPanel.tsx
      to: src/store/useDashboardDemoCoordinationStore.ts
      via: active/visible slice prioritization for exact detail loading
      pattern: activeSlice|selectedSlice|visibleSlices
---

<objective>
Redesign exact detail loading around a paged `/api/crimes/range` contract, then migrate the known dashboard exact-detail consumers to it in a controlled order.

Purpose: Preserve exact slice analysis while preventing broad or redundant detail fetches from rebuilding the dashboard memory problem.
Output: An exact paged range API, migrated dashboard consumers, and explicit proof that active/visible slice detail is prioritized first.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-CONTEXT.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-RESEARCH.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-02-SUMMARY.md
@src/lib/queries.ts
@src/app/api/crimes/range/route.ts
@src/hooks/useCrimeData.ts
@src/store/useDashboardDemoTimeslicingModeStore.ts
@src/components/dashboard-demo/DemoInspectPanel.tsx
@src/components/dashboard-demo/Demo3dSpatialView.tsx
@src/store/useDashboardDemoCoordinationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sampled range-query behavior with exact paged guardrailed reads</name>
  <files>src/lib/queries.ts, src/lib/queries/index.ts, src/lib/queries/builders.ts, src/lib/queries/types.ts, src/app/api/crimes/range/route.ts, src/app/api/crimes/range/route.test.ts, src/types/crime.ts</files>
  <action>Rebuild `/api/crimes/range` around exact persisted-table reads only. Remove the hot-path `queryCrimeCount(...)` call, `sampleStride`, and any sampled/truncated semantics from the exact dashboard path. Require explicit `startEpoch`/`endEpoch`, support optional `crimeTypes`, `districts`, `pageSize`, `cursor`, and `target`, and implement keyset paging with `LIMIT pageSize + 1` so the response exposes `hasMore` and `nextCursor`. Add guardrails that return a structured `requiresNarrowing` response when the requested exact range is broader than product policy allows. Keep scope inside Phase 81: do not turn this into a generic app-wide search API redesign.</action>
  <verify>Run `pnpm vitest src/app/api/crimes/range/route.test.ts`. Add assertions covering exact unsampled responses, `hasMore`/`nextCursor`, absence of `queryCrimeCount(...)`-driven count semantics, and `requiresNarrowing` responses for over-broad windows.</verify>
  <done>`/api/crimes/range` serves exact paged rows from persisted DuckDB tables, removes the count-plus-sampling hot path, and enforces narrowing guardrails before expensive broad queries run.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate known dashboard exact-detail consumers in dependency order and prioritize the active slice first</name>
  <files>src/hooks/useCrimeData.ts, src/hooks/useCrimeData.test.ts, src/store/useDashboardDemoCoordinationStore.ts, src/store/useDashboardDemoTimeslicingModeStore.ts, src/store/useDashboardDemoTimeslicingModeStore.test.ts, src/components/dashboard-demo/DemoInspectPanel.tsx, src/components/dashboard-demo/Demo3dSpatialView.tsx</files>
  <action>Migrate the known Phase 81 dashboard consumers in this order: `useCrimeData`, `DemoInspectPanel`, `Demo3dSpatialView`, then `useDashboardDemoTimeslicingModeStore`. Replace broad `limit=50000` assumptions with bounded working-window requests against the new exact paging contract. Honor `requiresNarrowing` responses instead of falling back to broad loads. Keep exact slice workflows exact. Implement locked decision D-15 by using the coordination state to prioritize the actively selected or currently visible slice window before background slice pages or comparison requests when multiple exact-detail loads are possible.</action>
  <verify>Run `pnpm vitest src/hooks/useCrimeData.test.ts src/store/useDashboardDemoTimeslicingModeStore.test.ts` and add assertions that the first detail request after narrowing uses the active/visible slice window, later pages follow only when `hasMore` is true, and no dashboard consumer expects sampled responses.</verify>
  <done>All known Phase 81 dashboard exact-detail consumers use the new paged contract, avoid broad default loads, and fill the user’s active/visible slice before background detail work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Exact paged detail contract, migrated dashboard consumers, and active-slice-first prioritization</what-built>
  <how-to-verify>
    1. Start the app with real DuckDB data (`USE_MOCK_DATA=false pnpm dev`) and open `/dashboard-demo`.
    2. Change the crime-type filter, then the district filter, and verify the overview bins update each time.
    3. Without changing filters, pan and zoom the map viewport only. Verify the overview bins do not change and no viewport-driven overview refetch alters them. This proves locked decision D-02.
    4. Brush the timeline to a narrow window with multiple visible slices, then open the Inspect and 3D surfaces.
    5. In Network, confirm the first `/api/crimes/range` request targets the active or visible slice window you just focused, not a broad all-time range and not a background comparison slice first. If paging continues, confirm follow-up requests use `nextCursor` only after the first focused window resolves. This proves locked decision D-15.
    6. Deliberately request a too-broad exact range if the UI allows it; verify the product prompts narrowing instead of silently loading a giant query.
    7. Confirm no response is marked or behaves as sampled/truncated exact data.
  </how-to-verify>
  <resume-signal>Type "approved" if filter/viewport behavior and active-slice-first paging match the checks above, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm vitest src/app/api/crimes/range/route.test.ts src/hooks/useCrimeData.test.ts src/store/useDashboardDemoTimeslicingModeStore.test.ts`
- `pnpm exec tsc --noEmit`
- Manual network check with `pnpm dev` proving locked decisions D-02 and D-15 end-to-end.
</verification>

<success_criteria>
1. Exact detail is loaded only after explicit user narrowing and never via eager broad dashboard defaults.
2. `/api/crimes/range` pages exact rows with cursor metadata and narrowing guardrails instead of sampling or count-plus-fetch behavior.
3. Locked decision D-15 is visible in the product: the active/visible slice resolves before background exact-detail work.
</success_criteria>

<output>
After completion, create `.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-03-SUMMARY.md`
</output>
