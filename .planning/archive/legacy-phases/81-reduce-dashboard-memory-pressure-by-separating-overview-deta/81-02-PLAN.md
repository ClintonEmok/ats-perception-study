---
phase: 81-reduce-dashboard-memory-pressure-by-separating-overview-deta
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - .planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-dashboard-consumer-audit.md
  - src/store/useTimelineDataStore.ts
  - src/store/useTimelineDataStore.test.ts
  - src/components/timeline/DemoDualTimeline.tsx
  - src/components/timeline/hooks/useDemoTimelineSummary.ts
  - src/components/viz/CubeVisualization.tsx
  - src/components/dashboard-demo/DemoDetectPanel.tsx
  - src/components/dashboard-demo/DemoInspectPanel.tsx
  - src/components/dashboard-demo/Demo3dSpatialView.tsx
autonomous: true
must_haves:
  truths:
    - Opening `/dashboard-demo` shows a usable overview timeline and summary-backed dashboard state before any full-detail fetch begins.
    - Changing crime-type or district filters updates the overview bins, but panning or zooming the map viewport alone does not change them.
    - Dashboard surfaces that used to assume eager `columns` preload now stay stable in preview mode until the user explicitly narrows for detail.
  artifacts:
    - path: .planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-dashboard-consumer-audit.md
      provides: audited inventory of dashboard-facing `useTimelineDataStore` / `columns` consumers and their migration status
    - path: src/store/useTimelineDataStore.ts
      provides: summary-first store state and explicit detail-intent gating
    - path: src/components/timeline/DemoDualTimeline.tsx
      provides: direct rendering from server overview bins
    - path: src/components/viz/CubeVisualization.tsx
      provides: removal of eager `loadRealData()` dashboard mount preload
  key_links:
    - from: src/store/useTimelineDataStore.ts
      to: src/app/api/crime/overview/route.ts
      via: loadSummaryData overview-bin fetch
      pattern: /api/crime/overview
    - from: src/components/timeline/DemoDualTimeline.tsx
      to: src/store/useTimelineDataStore.ts
      via: overview bins instead of `overviewTimestampSec` rebucketing
      pattern: overviewBins|binResolution
    - from: src/components/viz/CubeVisualization.tsx
      to: src/store/useTimelineDataStore.ts
      via: removal of eager full-detail mount trigger
      pattern: loadRealData
---

<objective>
Cut the dashboard over to a true summary-first startup path and explicitly audit every dashboard-facing `useTimelineDataStore` / `columns` consumer impacted by removing eager full-detail preload.

Purpose: Make Phase 81 safe to execute without hidden breakage from `columns === null` on dashboard mount.
Output: A consumer audit artifact, a summary-first timeline store contract, and preview-safe dashboard consumers that no longer require eager full-detail preload.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-CONTEXT.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-RESEARCH.md
@.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-01-SUMMARY.md
@src/store/useTimelineDataStore.ts
@src/components/timeline/DemoDualTimeline.tsx
@src/components/timeline/hooks/useDemoTimelineSummary.ts
@src/components/viz/CubeVisualization.tsx
@src/components/dashboard-demo/DemoDetectPanel.tsx
@src/components/dashboard-demo/DemoInspectPanel.tsx
@src/components/dashboard-demo/Demo3dSpatialView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all dashboard-facing `useTimelineDataStore` / `columns` consumers affected by preload removal</name>
  <files>.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-dashboard-consumer-audit.md</files>
  <action>Create a concrete migration inventory for the dashboard-facing surfaces in scope for Phase 81 before changing startup behavior. Audit at least `CubeVisualization`, `DemoDualTimeline`, `useDemoTimelineSummary`, `DemoDetectPanel`, `DemoInspectPanel`, and `Demo3dSpatialView`, plus any additional `/dashboard-demo` consumers discovered during grep that depend on `useTimelineDataStore` or `columns`. For each consumer, record whether it must be summary-safe on mount, whether it can stay preview-only until detail intent, and what exact code change Phase 81 will make. Keep this artifact phase-local and implementation-oriented; do not turn it into broad architecture documentation.</action>
  <verify>Confirm `81-dashboard-consumer-audit.md` lists every audited dashboard-facing consumer, its risk if `columns` is null on mount, and its concrete migration status/action.</verify>
  <done>The phase contains an explicit audit artifact covering the dashboard-facing consumers that would otherwise regress when eager preload is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Convert timeline summary state and overview rendering to the server-binned contract</name>
  <files>src/store/useTimelineDataStore.ts, src/store/useTimelineDataStore.test.ts, src/components/timeline/DemoDualTimeline.tsx, src/components/timeline/hooks/useDemoTimelineSummary.ts</files>
  <action>Replace `overviewTimestampSec` assumptions with the persisted server overview-bin contract from Plan 81-01. Keep `loadSummaryData()` as the dashboard mount path and add explicit state for summary-ready vs detail-ready behavior so the store no longer treats missing `columns` as an error. Update `DemoDualTimeline` and `useDemoTimelineSummary` to render overview bins directly, stop client-side rebucketing from large raw timestamp arrays on the summary path, and keep filter updates scoped to crime-type and district rather than viewport changes.</action>
  <verify>Run `pnpm vitest src/store/useTimelineDataStore.test.ts`. Add assertions that `loadSummaryData()` stores overview bins, that changing crime-type/district filters changes the summary request inputs, and that viewport-only changes do not alter overview-bin fetch parameters or derived overview counts.</verify>
  <done>The timeline summary path consumes server bins directly, supports `columns === null` on mount, and encodes locked decision D-02 in tests by separating overview-filter changes from viewport movement.</done>
</task>

<task type="auto">
  <name>Task 3: Remove eager full-detail preload and make audited dashboard surfaces preview-safe</name>
  <files>src/components/viz/CubeVisualization.tsx, src/components/dashboard-demo/DemoDetectPanel.tsx, src/components/dashboard-demo/DemoInspectPanel.tsx, src/components/dashboard-demo/Demo3dSpatialView.tsx</files>
  <action>Remove the dashboard-mount `loadRealData()` trigger from `CubeVisualization`. Use the audit artifact from Task 1 to update each impacted dashboard-facing surface so it tolerates summary-only startup and clearly distinguishes preview mode from exact-detail mode. Do not silently reintroduce eager detail fetches through another component; the dashboard should remain summary-first until explicit user narrowing intent arrives. Keep changes scoped to the audited dashboard-demo surfaces rather than refactoring unrelated pages that also read `useTimelineDataStore` elsewhere in the app.</action>
  <verify>Run `pnpm exec tsc --noEmit`. With `USE_MOCK_DATA=false pnpm dev` running, refresh `/dashboard-demo` and confirm no eager `/api/crime/stream` or broad `/api/crimes/range` request fires on mount while the audited surfaces still render a stable preview state.</verify>
  <done>The dashboard mounts in summary/preview mode without crashing or forcing full-detail preload, and the audited consumer surfaces remain usable until detail is explicitly requested.</done>
</task>

</tasks>

<verification>
- `pnpm vitest src/store/useTimelineDataStore.test.ts`
- `pnpm exec tsc --noEmit`
- Manual startup check with `pnpm dev`: `/dashboard-demo` renders summary state on refresh without eager `/api/crime/stream` or broad `/api/crimes/range` requests.
- Locked decision D-02 proof: automated tests and manual spot checks show crime-type/district filter changes update overview bins, while viewport movement alone does not.
</verification>

<success_criteria>
1. Dashboard mount is genuinely summary-first and does not preload full Arrow detail.
2. Every dashboard-facing `useTimelineDataStore` / `columns` consumer affected by preload removal is explicitly audited and either made preview-safe or documented as detail-gated.
3. Overview-bin behavior matches locked decision D-02: crime-type and district filters affect counts; viewport movement does not.
</success_criteria>

<output>
After completion, create `.planning/phases/81-reduce-dashboard-memory-pressure-by-separating-overview-deta/81-02-SUMMARY.md`
</output>
