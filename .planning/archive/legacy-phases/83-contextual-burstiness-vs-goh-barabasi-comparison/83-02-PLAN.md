---
phase: 83-contextual-burstiness-vs-goh-barabasi-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/tests/test_contextual.py
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
autonomous: true
user_setup: []
requirements:
  - CBP-01

must_haves:
  truths:
    - "A 168-cell (hour×dayOfWeek) baseline of mu_c and sigma_c is computed once on the full dataset, with sigma=0 cells replaced by epsilon=1e-9"
    - "Per-window z-scores are computed for windows 1h/6h/1d/1w with step=window/4, using the linearised per-second form (sum over active cells of mu_c · Δt_cell / (7·24·3600) and sigma² · Δt_cell / (7·24·3600))"
    - "Windows with fewer than 5 events are dropped from the metric"
    - "The module writes output/contextual_metric.parquet with columns (window_start, window_end, hour, dow, z) where z is float64 and hour/dow are int8"
    - "Unit tests on synthetic uniform-rate data produce z-scores within ±0.5 of zero (deterministic)"
    - "Unit tests on synthetic data with a 2x spike in one (h, d) cell produce z > 0 for windows in that cell"
  artifacts:
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py"
      provides: "Baseline + z-score metric"
      exports: ["compute_baseline", "compute_contextual_z_series", "write_contextual_parquet"]
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/tests/test_contextual.py"
      provides: "Unit tests for the metric"
      min_lines: 40
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/contextual_metric.parquet"
      provides: "Per-window z-scores (≥ 4 windows × at least 1h scale; ≥ 1,000 windows at 1d scale on the real dataset)"
  key_links:
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/db.py"
      via: "Consumes a DataFrame with [ts, hour, dow, month] from load_crimes()"
      pattern: "from db import load_crimes|import db"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py"
      to: "data/cache/crime.duckdb"
      via: "DuckDB GROUP BY (hour, dow) for weekly-bucketed mu_c and sigma_c"
      pattern: "GROUP BY.*hour.*dow|GROUP BY EXTRACT\\(HOUR"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py"
      via: "imported and called from main() (extended in Plan 04)"
      pattern: "from metrics import contextual|import metrics\\.contextual"
---

<objective>
Implement Metric A — the contextual (deviation-from-baseline) burstiness measure — and ship its first per-window output. Build the 168-cell (hour × dayOfWeek) baseline once on the full dataset, then compute z-scores for sliding windows at 1h/6h/1d/1w scales with step=window/4. The metric must be reproducible and produce a parquet artifact Plan 04 will compare against Goh-Barabasi.

Purpose: CBP-01 requires a contextual burstiness metric defined as `(observed - expected[hour, dayOfWeek]) / sigma`, with both expected-rate and sigma profiles derived from the same dataset. This plan delivers that metric plus the first artifact that downstream plans (03, 04, 05) will consume.

Output: `metrics/contextual.py` with `compute_baseline()` and `compute_contextual_z_series()` functions, unit tests in `tests/test_contextual.py`, and `output/contextual_metric.parquet` written by the metric's CLI.

**Relationship to Plan 01 exploration.** Plan 01's `explore.py` produces a quick preview using a Poisson-style sigma estimate (`output/baseline_168.csv`). Plan 02's `compute_baseline` is the **canonical** baseline using weekly-bucketed sigma (more rigorous). After `compute_baseline` runs, sanity-check the result against `output/baseline_168.csv` from Plan 01 — the per-cell `mean_per_sec` should agree to within 5% (sigma estimates will differ because Plan 01 uses Poisson and Plan 02 uses empirical weekly). If the per-cell mean diverges by more than 10%, halt and report to user.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-CONTEXT.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/db.py
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-01-PLAN.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-01-SUMMARY.md
@docs/FUTURE-WORK-ADAPTIVE-TIME.md
@scripts/burstiness_sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compute_baseline() and compute_contextual_z_series()</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (Baseline Grid section at L138-157, per-window z-score formulas at L36-91, edge cases at L83-91)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-CONTEXT.md (per-bin z formula at L32: `z = (observed - expected[h, d] · binDuration) / (sigma[h, d] · sqrt(binDuration))`)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/db.py (the loader — read it to know exact column names and dtypes)
    - scripts/burstiness_sweep.py (windowing style: windows_for_size at L120-127, sweep_iei at L138-151)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py
  </files>
  <action>
    Create `metrics/contextual.py` with two public functions and one helper, all fully type-hinted.

    Module docstring: "Contextual burstiness metric (CBP-01). Computes a 168-cell hour×dayOfWeek baseline (mu_c, sigma_c) on the full dataset, then a per-window z-score for each sliding window at 1h/6h/1d/1w with step=window/4. Mirrors the formulation in docs/FUTURE-WORK-ADAPTIVE-TIME.md L57-83 and the locked decision in 83-CONTEXT.md."

    1. Constants:
       - `WINDOWS_SEC = (3600, 21600, 86400, 604800)` — the locked 1h/6h/1d/1w set.
       - `WINDOW_LABELS = {3600: "1h", 21600: "6h", 86400: "1d", 604800: "1w"}` — matches `scripts/burstiness_sweep.py:204-210`.
       - `MIN_EVENTS_PER_WINDOW = 5` — matches `83-RESEARCH.md` Risk #7 floor for fairness with the Goh-Barabasi metric.
       - `SECONDS_PER_WEEK = 7 * 24 * 3600` = 604800.
       - `EPSILON = 1e-9` — for sigma=0 cell substitution per `83-RESEARCH.md` L88.

    2. `def compute_baseline(events: pd.DataFrame) -> tuple[np.ndarray, np.ndarray]:`
       - Input: `events` is a DataFrame with columns `ts:int64, hour:int8, dow:int8, month:int8` (as produced by `load_crimes()`).
       - Output: `(mu, sigma)` each shape `(24, 7)` dtype `float64`.
       - Algorithm:
         a. Compute `week_index = (events["ts"] // SECONDS_PER_WEEK).astype("int64")` — each timestamp maps to an integer week since 1970-01-01.
         b. `weekly = events.assign(week=week_index).groupby(["week", "hour", "dow"]).size().reset_index(name="count")` — for every (week, hour, dow) cell, the number of events that fell in that cell during that week.
         c. `per_cell = weekly.groupby(["hour", "dow"])["count"].agg(["mean", "std"])` — population std (default in pandas).
         d. Build a `(24, 7)` array for `mu` and `sigma`. Initialise with `np.zeros((24, 7), dtype=np.float64)`. For each `(h, d)` in `per_cell.index`, set `mu[h, d] = per_cell.loc[(h, d), "mean"]` and `sigma[h, d] = per_cell.loc[(h, d), "std"]` (NaN-safe — fill NaN with 0.0).
         e. Apply the `EPSILON` substitution: `sigma = np.where(sigma < EPSILON, EPSILON, sigma)`. Count the number of substituted cells and print to stderr: `f"Baseline: {n_substituted}/168 cells had sigma<{EPSILON}, substituted with epsilon"`.
         f. Return `(mu, sigma)`.

    3. `def _z_for_window(window_start: int, window_end: int, mu: np.ndarray, sigma: np.ndarray) -> tuple[int, int, int, float]:`
       - Linearised per-second form. Compute `dt = window_end - window_start` (float seconds). For every (h, d) in the 168 cells, compute the fraction of the week that the window covers in that cell (window-spans-at-most-2-cells is fine — handle the general case by iterating over the 168 cells and adding the contribution only if the window overlaps that cell).
       - Simpler and correct-enough implementation (use this — the per-cell overlap computation in `83-RESEARCH.md` L70-78 is more general but harder to read; for the locked 1h/6h/1d/1w sweep the windows align well enough with the (h, d) grid that the *contributing-cell-only* approximation is sufficient):
         a. The window covers some range of (h, d) cells. For each integer hour `h ∈ {0..23}` and day-of-week `d ∈ {0..6}` that the window *fully* covers, add `mu[h, d] * 3600 / SECONDS_PER_WEEK` to `expected` and `sigma[h, d]**2 * 3600 / SECONDS_PER_WEEK` to `sigma_W_sq`.
         b. For partial overlaps (the first and last hour of the window, if the window doesn't align on hour boundaries), compute the exact overlap seconds `Δt_cell` and add `mu[h, d] * Δt_cell / SECONDS_PER_WEEK` and `sigma[h, d]**2 * Δt_cell / SECONDS_PER_WEEK`.
         c. `expected = sum_mu_per_sec * dt` where `sum_mu_per_sec = sum of (mu[h,d] / SECONDS_PER_WEEK) over overlapping cells weighted by overlap seconds`.
         d. `z = (obs - expected) / sqrt(sigma_W_sq)`. Return `(window_start, window_end, primary_h, primary_d, z)` where `primary_h` and `primary_d` are the (h, d) of the window's midpoint.
       - Edge case: if `sigma_W_sq == 0`, return `z = float("nan")` (caller will filter).

    4. `def compute_contextual_z_series(timestamps: np.ndarray, hours: np.ndarray, dows: np.ndarray, mu: np.ndarray, sigma: np.ndarray, window_sec: int, step_sec: int | None = None) -> pd.DataFrame:`
       - Input: `timestamps` shape `(N,)` int64 epoch seconds (sorted ascending), `hours` and `dows` shape `(N,)` int8 (the hour/dow of each timestamp), precomputed baseline.
       - `step_sec` defaults to `max(1, window_sec // 4)` — matches `scripts/burstiness_sweep.py:282`.
       - Output: DataFrame with columns `[window_start, window_end, hour, dow, z, obs, expected, sigma_w]` and dtypes `[int64, int64, int8, int8, float64, int32, float64, float64]`. (`hour` and `dow` are the midpoint of the window, for indexing back into the heatmap in Plan 04.)
       - Algorithm:
         a. If `len(timestamps) < 2`, return an empty DataFrame with the columns above.
         b. `t_start, t_end = timestamps[0], timestamps[-1]`.
         c. For `ws in range(t_start, t_end - window_sec + 1, step_sec)`:
            - `we = ws + window_sec`.
            - Use `np.searchsorted` for slicing: `lo = np.searchsorted(timestamps, ws, side="left")`, `hi = np.searchsorted(timestamps, we, side="left")`.
            - `obs = hi - lo`. If `obs < MIN_EVENTS_PER_WINDOW`, skip.
            - Compute expected and sigma via `_z_for_window(ws, we, mu, sigma)`.
            - Append `(ws, we, primary_h, primary_d, z, obs, expected, sigma_w)`.
         d. Build and return the DataFrame.

    5. `def write_contextual_parquet(series: pd.DataFrame, output_path: Path) -> None:` — ensure `output_path.parent.mkdir(parents=True, exist_ok=True)`, then write `series.to_parquet(output_path, index=False)`. Print `f"Wrote {len(series):,} windows to {output_path}"` to stdout.

    6. `if __name__ == "__main__":` block:
       ```python
       from db import load_crimes
       from pathlib import Path
       df = load_crimes()
       print("Computing baseline (168 cells)...")
       mu, sigma = compute_baseline(df)
       print(f"Baseline mu range: [{mu.min():.2f}, {mu.max():.2f}]")
       print(f"Baseline sigma range: [{sigma.min():.4f}, {sigma.max():.4f}]")
       output_dir = Path(__file__).parent / "output"
       output_dir.mkdir(parents=True, exist_ok=True)
       output_path = output_dir / "contextual_metric.parquet"
       all_series = []
       for window_sec in WINDOWS_SEC:
           step = max(1, window_sec // 4)
           series = compute_contextual_z_series(df["ts"].to_numpy(), df["hour"].to_numpy(), df["dow"].to_numpy(), mu, sigma, window_sec, step)
           series["window_sec"] = window_sec
           all_series.append(series)
           print(f"  {WINDOW_LABELS[window_sec]}: {len(series):,} valid windows")
       full = pd.concat(all_series, ignore_index=True)
       write_contextual_parquet(full, output_path)
       ```
       This makes the module runnable standalone (`python -m metrics.contextual` or `python metrics/contextual.py`) and writes the parquet.

    7. Imports: `from __future__ import annotations`, `numpy as np`, `pandas as pd`, `from pathlib import Path`, plus stdlib `sys` for the stderr print.
  </action>
  <verify>
    - `grep -F "def compute_baseline(" metrics/contextual.py` matches
    - `grep -F "def compute_contextual_z_series(" metrics/contextual.py` matches
    - `grep -F "WINDOWS_SEC = (3600, 21600, 86400, 604800)" metrics/contextual.py` matches
  </verify>
  <acceptance_criteria>
    - `metrics/contextual.py` contains `def compute_baseline(`, `def compute_contextual_z_series(`, `def write_contextual_parquet(`, and the literal string `WINDOWS_SEC = (3600, 21600, 86400, 604800)`
    - `from metrics.contextual import compute_baseline, compute_contextual_z_series` succeeds (no ImportError)
    - Running `python metrics/contextual.py` standalone completes in < 5 minutes on the 8.5M dataset and creates `output/contextual_metric.parquet`
    - The resulting parquet has ≥ 100,000 rows and 5+ columns including `window_start`, `window_end`, `hour`, `dow`, `z`
    - `Baseline mu range: [50, 500]` (sanity check against the 83-RESEARCH.md L141 prediction of ~50,500 mean per cell across the 8.5M dataset — wide range acceptable since some cells are sparse)
  </acceptance_criteria>
  <done>metrics/contextual.py implements compute_baseline() and compute_contextual_z_series() and writes the contextual_metric.parquet artifact.</done>
  <depends_on></depends_on>
</task>

<task type="auto">
  <name>Task 2: Write unit tests on synthetic data with deterministic expected z-scores</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py (the module being tested — read the current file, not assumptions)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (synthetic-data sanity check at L448-455: "Contextual z-score on a synthetic uniform-rate subset should be ≈ 0 across all windows")
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/tests/test_contextual.py
  </files>
  <action>
    Create `tests/test_contextual.py` with pytest-style tests that run on **synthetic in-memory data** (no DuckDB dependency) so they're fast and deterministic.

    Test cases (each is a `def test_*` function):

    1. `test_compute_baseline_uniform_rate()`:
       - Build a synthetic events DataFrame with 4 weeks of data where every (h, d) cell has exactly 10 events per week, evenly distributed. Use `np.random.default_rng(seed=42)` for determinism. Build a `(4 * 7 * 24 * 10,)` array of timestamps covering the 4-week range.
       - For each event, assign `hour = (i % 24)` and `dow = ((i // 24) % 7)`.
       - Call `compute_baseline(df)`. Assert that `mu` is `(24, 7)`, `sigma` is `(24, 7)`, all finite. Assert that `mu` is close to 10 (within ±0.5) for every cell — the uniform-rate sanity check. Assert that `sigma` is close to 0 (within ±1.0) — uniform cells have low variance; the `EPSILON` substitution means sigma is at least 1e-9.

    2. `test_compute_baseline_with_spike()`:
       - Build a synthetic events DataFrame covering 4 weeks where every (h, d) cell draws count ~ Uniform(9, 11) per week except (h=22, d=6) which draws count ~ Uniform(15, 25) per week. Use `np.random.default_rng(seed=43)` for determinism. Generate timestamps so each week contributes `c_h_d` events evenly spread across the week's 604800 seconds in that (h, d) cell.
       - Call `compute_baseline(df)`. Assert that `mu[22, 6]` is in `[15, 25]` (the spike cell's mean) and `sigma[22, 6]` > 1.0 (the cell has nonzero variance from the ±5 spread).
       - Assert that all other cells have `mu` in `[9, 11]` and `sigma` < 2.0.

    3. `test_contextual_z_uniform_is_near_zero()`:
       - Build a synthetic events DataFrame for 1 year where every (h, d) cell has the SAME constant rate (e.g., 50 events per week, uniform). No day-of-week or hour-of-day variation. (Construct by drawing timestamps from `np.arange(t0, t0 + SECONDS_PER_YEAR, SECONDS_PER_YEAR / (50 * 52 * 168))` and assigning hour/dow from the timestamp.)
       - Call `compute_baseline(df)`, then `compute_contextual_z_series(...)` for the 1d window with step=1d.
       - Assert that the mean of `z` is within ±0.5 of 0 and the std of `z` is < 2.0 (a uniform-rate baseline should produce near-zero z everywhere — the deviation is pure sampling noise).

    4. `test_contextual_z_spike_is_positive()`:
       - Build a synthetic events DataFrame for 1 year where (h=22, d=5) (Sat 10pm) has 5x the rate of all other cells.
       - Call `compute_baseline(df)`, then `compute_contextual_z_series(...)` for a 1h window that falls exactly in (22, 5).
       - Assert that at least one z value in the resulting series is > 1.0 (the spike cell should produce a positive z when the window aligns with it).

    5. `test_write_contextual_parquet_roundtrip()`:
       - Build a tiny synthetic DataFrame with 10 windows. Call `write_contextual_parquet(series, Path("/tmp/test_metric.parquet"))`. Then `pd.read_parquet("/tmp/test_metric.parquet")` and assert the roundtripped DataFrame equals the input.

    6. `test_min_events_filter()`:
       - Build a synthetic DataFrame with 100 events spread over a 10-hour range. Call `compute_contextual_z_series(...)` with `window_sec=3600, step_sec=900`. The empty windows should be dropped (`obs < MIN_EVENTS_PER_WINDOW`). Assert that the returned DataFrame has fewer rows than `10 * 4 = 40` (i.e. some windows were dropped).

    Top of file: `import numpy as np`, `import pandas as pd`, `import pytest`, `from pathlib import Path`, `from metrics.contextual import compute_baseline, compute_contextual_z_series, write_contextual_parquet, WINDOWS_SEC, MIN_EVENTS_PER_WINDOW, SECONDS_PER_WEEK`.

    Each test must be deterministic (no `np.random` without a seed; no `datetime.now()`).
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python -m pytest tests/test_contextual.py -v` exits 0
    - At least 6 tests are collected and all pass
  </verify>
  <acceptance_criteria>
    - File `tests/test_contextual.py` exists and contains the literal strings `def test_compute_baseline_uniform_rate`, `def test_compute_baseline_with_spike`, `def test_contextual_z_uniform_is_near_zero`, `def test_contextual_z_spike_is_positive`, `def test_write_contextual_parquet_roundtrip`, `def test_min_events_filter`
    - `.venv/bin/python -m pytest tests/test_contextual.py -v` collects at least 6 tests and reports `6 passed`
    - No test imports duckdb, matplotlib, or pyarrow (the tests must be self-contained synthetic-data only)
    - All tests run in < 30 seconds total (verifiable via `pytest --durations=10`)
  </acceptance_criteria>
  <done>Unit tests pass for the contextual metric on synthetic data with deterministic expected z-scores.</done>
  <depends_on>Task 1</depends_on>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke test on the real DuckDB dataset</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py (the module being exercised — read the current file)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/db.py (the loader — exact column names and dtypes)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (sanity check at L141: per-cell sample size 50,500 events/cell; baseline mu range [50, 500] expected)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
  </files>
  <action>
    Extend `run.py` so the Plan 02 metric stage runs inside `main()`. This is the first extension — Plan 04 will add the comparison/figure stages and Plan 05 will add the decision gate. Keep the extension minimal at this stage.

    1. Remove the `try/except ImportError` placeholder for `contextual` (it now exists). Import normally: `from metrics import contextual`.

    2. After the existing `df = load_crimes(...)` call, insert:
       ```python
       if contextual is not None:
           t1 = time.perf_counter()
           print("Computing contextual baseline (168 cells)...")
           mu, sigma = contextual.compute_baseline(df)
           print(f"  mu range: [{mu.min():.2f}, {mu.max():.2f}]")
           print(f"  sigma range: [{sigma.min():.4f}, {sigma.max():.4f}]")
           all_z = []
           for window_sec in contextual.WINDOWS_SEC:
               step = max(1, window_sec // 4)
               z_series = contextual.compute_contextual_z_series(
                   df["ts"].to_numpy(),
                   df["hour"].to_numpy(),
                   df["dow"].to_numpy(),
                   mu, sigma,
                   window_sec, step,
               )
               z_series["window_sec"] = window_sec
               all_z.append(z_series)
               print(f"  {contextual.WINDOW_LABELS[window_sec]}: {len(z_series):,} valid windows")
           full_z = pd.concat(all_z, ignore_index=True)
           args.output_dir.mkdir(parents=True, exist_ok=True)
           contextual.write_contextual_parquet(full_z, args.output_dir / "contextual_metric.parquet")
           print(f"Contextual metric: {time.perf_counter() - t1:.1f}s")
       ```

    3. Add `import pandas as pd` to the imports (needed for the `pd.concat`).

    4. Smoke test the extension: from the phase root, with the venv active, run `.venv/bin/python run.py`. Confirm:
       - Exits 0
       - Stdout contains `Computing contextual baseline`, `mu range:`, `sigma range:`, and four `valid windows` lines (one per window size)
       - `output/contextual_metric.parquet` exists and has ≥ 100,000 rows when read back with pandas
       - Total runtime < 10 minutes on the 8.5M dataset (the 1h scale is the bottleneck — ~200k+ windows; expect 1-3 min for the full sweep)

    Note: Plan 02 deliberately does NOT yet write the `goh_barabasi_metric.parquet` or the comparison table. Those arrive in Plans 03 and 04. The `compare` and `decision_gate` imports remain guarded by `try/except ImportError` in Plan 02; Plan 04 and 05 will remove the guards.
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python run.py` exits 0
    - `output/contextual_metric.parquet` exists
    - `.venv/bin/python -c "import pandas as pd; df = pd.read_parquet('output/contextual_metric.parquet'); print(df.shape, sorted(df['window_sec'].unique().tolist()))"` prints shape `(N, ≥5)` and `window_sec` list `[3600, 21600, 86400, 604800]`
  </verify>
  <acceptance_criteria>
    - `output/contextual_metric.parquet` exists and is readable
    - The parquet has columns including `window_start`, `window_end`, `hour`, `dow`, `z`, `window_sec`
    - `window_sec` column contains the values `[3600, 21600, 86400, 604800]` (matches `WINDOWS_SEC`)
    - Row count at the 1d scale (window_sec=86400) is ≥ 1,000 (the real 8.5M dataset produces ~9,000+ 1d windows across ~25 years)
    - The full run.py end-to-end (data + contextual metric) completes in < 10 minutes
  </acceptance_criteria>
  <done>Contextual metric runs end-to-end via run.py on the 8.5M dataset and writes the parquet artifact.</done>
  <depends_on>Task 2</depends_on>
</task>

</tasks>

<verification>
1. `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python -m pytest tests/test_contextual.py -v` — 6+ tests pass in < 30s
2. `.venv/bin/python run.py` — exits 0, writes `output/contextual_metric.parquet` with ≥ 100,000 rows
3. `python -c "import pandas as pd; df = pd.read_parquet('output/contextual_metric.parquet'); print(df['z'].abs().mean())"` — prints a finite number (mean |z| is in the 0.5-3.0 range on a real dataset)
4. The 168-cell baseline `mu` array has the expected hour-of-day and day-of-week pattern (peak in evening hours + weekend > weekday) — verifiable via a quick `print(mu.argmax(), mu.max())` returning a high hour index (typically 18-22)
</verification>

<success_criteria>
- `metrics/contextual.py` implements compute_baseline() (returns (24,7) mu and sigma arrays) and compute_contextual_z_series() (returns per-window z-scores for 1h/6h/1d/1w windows).
- Unit tests cover uniform-rate, spike, sigma-substitution, parquet roundtrip, and min-events filter cases — all deterministic and < 30s total.
- `python run.py` runs end-to-end on the 8.5M DuckDB dataset, completes in < 10 minutes, and writes `output/contextual_metric.parquet` with the 5 locked columns and 4 window sizes.
- The mu pattern matches the expected hour-of-day + day-of-week profile (peak at evening hours, weekend > weekday).
</success_criteria>

<output>
After completion, create `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-02-SUMMARY.md` using the summary template.
</output>

<artifacts_this_phase_produces>
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py`
  - Constants: `WINDOWS_SEC = (3600, 21600, 86400, 604800)`, `WINDOW_LABELS = {...}`, `MIN_EVENTS_PER_WINDOW = 5`, `SECONDS_PER_WEEK = 604800`, `EPSILON = 1e-9`
  - Functions: `compute_baseline(events: pd.DataFrame) -> tuple[np.ndarray, np.ndarray]`, `_z_for_window(window_start, window_end, mu, sigma) -> tuple`, `compute_contextual_z_series(timestamps, hours, dows, mu, sigma, window_sec, step_sec) -> pd.DataFrame`, `write_contextual_parquet(series, output_path) -> None`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/tests/test_contextual.py`
  - Test functions: `test_compute_baseline_uniform_rate`, `test_compute_baseline_with_spike`, `test_contextual_z_uniform_is_near_zero`, `test_contextual_z_spike_is_positive`, `test_write_contextual_parquet_roundtrip`, `test_min_events_filter`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/contextual_metric.parquet`
  - Schema: `[window_start:int64, window_end:int64, hour:int8, dow:int8, z:float64, obs:int32, expected:float64, sigma_w:float64, window_sec:int64]`
  - Cardinality: ≥ 100,000 rows (typically 200k-300k+ across all four window sizes on the 8.5M dataset)
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py` (extended with contextual-metric stage; further extended in Plans 03-05)
</artifacts_this_phase_produces>
