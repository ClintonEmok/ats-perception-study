---
phase: 83-contextual-burstiness-vs-goh-barabasi-comparison
plan: 04
type: execute
wave: 3
depends_on:
  - 83-02
  - 83-03
files_modified:
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/z_heatmap.png
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/per_window_timeseries.png
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/contrast_table.png
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
autonomous: true
user_setup: []
requirements:
  - CBP-03
  - CBP-04

must_haves:
  truths:
    - "compare.py reads the four parquets (contextual, goh_barabasi, density, cv), groups by window_sec, and produces a per-metric summary table with columns: metric, window_size, n_windows, mean, std, min, max, cv, range"
    - "comparison_table.csv is a tidy long-form CSV with one row per (metric, window_size) — 16 rows total (4 metrics × 4 window sizes)"
    - "figures.py writes three PNGs at 320 DPI in serif font using the chapter3_visualizations.py palette (BG/INK/GRID/BLUE/ACCENT/RED): z_heatmap.png (24×7 hour×dayOfWeek grid), per_window_timeseries.png (4-line plot: z left axis, B/density/CV right axis normalized at 1d), contrast_table.png (4-metric × 4-window visual table)"
    - "run.py is the full orchestrator: data → contextual → B → density → CV → comparison → figures, all in one command"
    - "The contextual CV at 1d in the output CSV is numerically larger than the Goh-Barabasi CV at 1d (the load-bearing comparison that the decision gate will use in Plan 05)"
  artifacts:
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      provides: "Per-metric summary table writer (4 metrics × 4 windows = 16 rows)"
      exports: ["summarize_metric", "build_comparison_table", "write_comparison_csv"]
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py"
      provides: "Three thesis figures (4-metric time series, 4×4 contrast table)"
      exports: ["plot_z_heatmap", "plot_per_window_timeseries", "plot_contrast_table", "render_all_figures"]
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv"
      provides: "Per-metric CV/range table (16 rows = 4 metrics × 4 window sizes)"
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/z_heatmap.png"
      provides: "24×7 hour×dayOfWeek z-score heatmap (one panel per window size)"
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/per_window_timeseries.png"
      provides: "Twin-axis time series at 1d window: z (left, blue) vs B (right, accent)"
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/contrast_table.png"
      provides: "Visual contrast table mirroring comparison_table.csv"
  key_links:
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/contextual_metric.parquet"
      via: "pd.read_parquet (Plan 02 output)"
      pattern: "read_parquet.*contextual_metric\\.parquet"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/goh_barabasi_metric.parquet"
      via: "pd.read_parquet (Plan 03 output)"
      pattern: "read_parquet.*goh_barabasi_metric\\.parquet"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/density_metric.parquet"
      via: "pd.read_parquet (Plan 03 output)"
      pattern: "read_parquet.*density_metric\\.parquet"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/cv_metric.parquet"
      via: "pd.read_parquet (Plan 03 output)"
      pattern: "read_parquet.*cv_metric\\.parquet"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py"
      to: "scripts/chapter3_visualizations.py"
      via: "Style constants: BG, INK, SUBTLE, GRID, BLUE, ACCENT, RED, FIGURE_DPI=320"
      pattern: "BG = .#ffffff|FIGURE_DPI = 320"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py"
      via: "imported and called from main()"
      pattern: "import compare|from compare import"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py"
      via: "imported and called from main()"
      pattern: "import figures|from figures import"
---

<objective>
Compare the contextual burstiness metric against the three reference metrics (Goh-Barabasi B, density, CV) on the per-window dynamic range axes (CV and range) and produce three thesis-grade figures. Wire `run.py` to be the full end-to-end orchestrator: data → contextual → B → density → CV → comparison → figures, all from a single `python run.py` invocation.

Purpose: CBP-03 requires the per-window CV/range comparison across 1h/6h/1d/1w for all 4 metrics, and CBP-04 requires thesis-ready figures (heatmap, time series, contrast table). Both must be reproducible from a single command. The decision gate in Plan 05 will read the 16-row comparison_table.csv that this plan produces.

Output: `compare.py` (4×4 CSV table), `figures.py` (three PNGs at 320 DPI), the four output artifacts (1 CSV + 3 PNGs), and an updated `run.py` that orchestrates everything.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-CONTEXT.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/db.py
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/contextual.py
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/goh_barabasi.py
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-01-SUMMARY.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-02-SUMMARY.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-03-SUMMARY.md
@scripts/chapter3_visualizations.py
@docs/FUTURE-WORK-ADAPTIVE-TIME.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compare.py — per-metric CV/range summary table</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (required summary table shape at L206-220; per-metric stats formulas at L196-201; CV defined as std / |mean| using population std at L198)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/contextual_metric.parquet (Plan 02 output — read the actual schema)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/goh_barabasi_metric.parquet (Plan 03 output — read the actual schema)
    - scripts/burstiness_sweep.py (existing CV pattern at L177-189, `stats()` function)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py
  </files>
  <action>
    Create `compare.py` at the phase root with three public functions.

    Module docstring: "Per-metric comparison summary (CBP-03). Reads the contextual and Goh-Barabasi per-window parquets, computes per-(metric, window_size) statistics, and writes a tidy long-form comparison_table.csv. The decision-gate in Plan 05 reads this CSV."

    1. `def summarize_metric(series: pd.DataFrame, value_col: str, metric_name: str) -> pd.DataFrame:`
       - Input: `series` is a per-window DataFrame (from a parquet), `value_col` is the column to summarise (e.g. "z" or "B"), `metric_name` is "contextual" or "goh_barabasi".
       - Output: a DataFrame with columns `[metric, window_size, window_label, n_windows, mean, std, min, max, cv, range]` and 4 rows (one per window size in WINDOW_LABELS).
       - Algorithm:
         a. Group by `window_sec` and apply population std (default in pandas/numpy — `ddof=0`).
         b. For each group: `n = len`, `mean = series[value_col].mean()`, `std = series[value_col].std(ddof=0)`, `min = series[value_col].min()`, `max = series[value_col].max()`, `cv = std / abs(mean) if mean != 0 else float("nan")`, `range = max - min`.
         c. Map `window_sec` to label via the `WINDOW_LABELS = {3600: "1h", 21600: "6h", 86400: "1d", 604800: "1w"}` constant (define at module level).
         d. Build the output DataFrame with 4 rows. Sort by `window_sec` ascending.

    2. `def build_comparison_table(contextual_df: pd.DataFrame, goh_barabasi_df: pd.DataFrame) -> pd.DataFrame:`
       - Calls `summarize_metric(contextual_df, "z", "contextual")` and `summarize_metric(goh_barabasi_df, "B", "goh_barabasi")`, then `pd.concat` them.
       - Output: 8-row DataFrame with the same columns.

    3. `def write_comparison_csv(table: pd.DataFrame, output_path: Path) -> None:`
       - `output_path.parent.mkdir(parents=True, exist_ok=True)`, `table.to_csv(output_path, index=False, float_format="%.6f")`.
       - Print `f"Wrote {len(table)} rows to {output_path}"`.

    4. `if __name__ == "__main__":` block:
        ```python
        from pathlib import Path
        contextual = pd.read_parquet("output/contextual_metric.parquet")
        goh_barabasi = pd.read_parquet("output/goh_barabasi_metric.parquet")
        table = build_comparison_table(contextual, goh_barabasi)
        write_comparison_csv(table, Path("output/comparison_table.csv"))
        print(table.to_string(index=False))
       ```

    5. Imports: `from __future__ import annotations`, `pandas as pd`, `numpy as np`, `from pathlib import Path`.

    Verify: from the phase root with the venv active, run `.venv/bin/python compare.py`. It reads the two existing parquets and writes `output/comparison_table.csv` with 8 rows. Confirm with `wc -l output/comparison_table.csv` (should be 9 lines: 1 header + 8 data rows).
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python compare.py` exits 0
    - `output/comparison_table.csv` exists
    - `head -1 output/comparison_table.csv` contains the literal string `metric,window_size,window_label,n_windows,mean,std,min,max,cv,range`
    - `wc -l output/comparison_table.csv` returns 9
  </verify>
  <acceptance_criteria>
    - compare.py contains `def summarize_metric(`, `def build_comparison_table(`, `def write_comparison_csv(`
    - output/comparison_table.csv has exactly 8 data rows and 1 header row
    - The CSV header matches `metric,window_size,window_label,n_windows,mean,std,min,max,cv,range` exactly
    - The 8 rows cover exactly the 4 window sizes for each of the 2 metrics (4 contextual + 4 goh_barabasi rows)
    - The contextual CV at 1d is numerically > 0 (i.e. not NaN and not 0) — the decision gate in Plan 05 needs this to evaluate
  </acceptance_criteria>
  <done>compare.py produces output/comparison_table.csv with 8 rows of per-metric CV/range summary.</done>
  <depends_on></depends_on>
</task>

<task type="auto">
  <name>Task 2: Implement figures.py — three thesis-grade figures at 320 DPI</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (figure specifications at L367-396; per-cell mean z-score for the heatmap at L519-522; twin-axis time series pattern at L380-394)
    - scripts/chapter3_visualizations.py:30-84 (palette constants, configure_matplotlib pattern, save_png)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/contextual_metric.parquet (read the actual schema for the heatmap)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/goh_barabasi_metric.parquet (read the actual schema for the time series)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv (Plan 04 Task 1 output, for the contrast table)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py
  </files>
  <action>
    Create `figures.py` at the phase root with three public plotting functions plus a convenience entry point.

    Module docstring: "Thesis-ready figure rendering (CBP-04). Three figures at 320 DPI in serif font using the chapter3_visualizations palette: z_heatmap (24x7 hour×dayOfWeek), per_window_timeseries (twin-axis z vs B at 1d), contrast_table (visual mirror of comparison_table.csv)."

    1. **Style constants** (copy from `scripts/chapter3_visualizations.py:30-40`):
       ```python
       BG = "#ffffff"
       INK = "#18202a"
       SUBTLE = "#687482"
       GRID = "#d8e0e8"
       BLUE = "#3b6ea8"
       BLUE_DARK = "#24486f"
       BLUE_LIGHT = "#b8d1ea"
       TEAL = "#76a9b7"
       ACCENT = "#d9a441"
       RED = "#d46c68"
       FIGURE_DPI = 320
       ```
       Also import `matplotlib` and call `plt.rcParams.update({...})` mirroring `chapter3_visualizations.py:60-84` (font.family=serif, font.serif=[DejaVu Serif, Times New Roman, Times], mathtext.fontset=stix, edgecolor=GRID, labelcolor=INK, etc.).

    2. `def plot_z_heatmap(z_series_by_window: dict[int, pd.DataFrame], output_path: Path) -> None:`
        - `z_series_by_window` maps `window_sec → DataFrame` with columns [hour, dow, z] (slice of the contextual parquet filtered by window_sec).
        - For each of the 4 window sizes, build a `(24, 7)` matrix where `M[h, d] = mean z for that (h, d) cell across all windows landing in that cell`. The number of windows landing in each (h, d) cell depends on the window size — at 1d, every (h, d) cell has ~25 years worth of data points; at 1w, much fewer.
        - Layout: 2x2 grid of subplots (one per window size), each showing its (24, 7) heatmap with `cmap="RdBu_r"`, `vmin=-3, vmax=3`. Use `fig, axes = plt.subplots(2, 2, figsize=(12, 9), dpi=FIGURE_DPI)`. Set axis labels: x = day of week (Sun..Sat), y = hour of day (0..23). Each subplot title: `f"z-score heatmap — {WINDOW_LABELS[ws]}"`. Add a single shared colorbar.
        - **Source caption** (CONTEXT.md L93 requires this for thesis-grade figures): after `plt.tight_layout()`, add `fig.text(0.01, 0.005, "Source: 8.5M-record Chicago crime dataset (2001–2026); Phase 83 analysis, contextual metric (z-score vs hour×dayOfWeek baseline)", fontsize=7, color=SUBTLE, ha="left", va="bottom")`.
        - `fig.savefig(output_path, dpi=FIGURE_DPI)`, then `plt.close(fig)`.

    3. `def plot_per_window_timeseries(z_series: pd.DataFrame, b_series: pd.DataFrame, output_path: Path, window_sec: int = 86400) -> None:`
        - Filter both inputs to `window_sec == 86400` (1d scale per 83-RESEARCH.md L380-394).
        - `fig, ax_top = plt.subplots(figsize=(11.5, 5.4), dpi=FIGURE_DPI)`, `ax_bot = ax_top.twinx()`.
        - For z: `dates = pd.to_datetime(z_series["window_start"], unit="s", utc=True)`, `ax_top.plot(dates, z_series["z"], color=BLUE, lw=0.8, label="Contextual z")`.
        - For B: `dates_b = pd.to_datetime(b_series["window_start"], unit="s", utc=True)`, `ax_bot.plot(dates_b, b_series["B"], color=ACCENT, lw=0.8, alpha=0.7, label="Goh-Barabasi B")`.
        - `ax_top.set_ylabel("Contextual z-score", color=BLUE)`, `ax_bot.set_ylabel("Goh-Barabasi B", color=ACCENT)`, `ax_top.set_xlabel("Date")`. `ax_top.grid(True, color=GRID, lw=0.6)`. `fig.legend(loc="upper right", frameon=False)`.
        - Title: `f"Per-window B vs z — {WINDOW_LABELS[window_sec]} window"`.
        - **Source caption** (CONTEXT.md L93): add `fig.text(0.01, 0.005, "Source: 8.5M-record Chicago crime dataset (2001–2026); Phase 83 analysis, contextual z-score (blue) vs Goh-Barabasi B (accent)", fontsize=7, color=SUBTLE, ha="left", va="bottom")`.
        - `fig.savefig(output_path, dpi=FIGURE_DPI)`, `plt.close(fig)`.

    4. `def plot_contrast_table(table: pd.DataFrame, output_path: Path) -> None:`
        - Input: the 8-row comparison_table DataFrame.
        - Reshape to a `(2 metrics × 4 window sizes)` matrix of CV values and a second matrix of range values.
        - `fig, axes = plt.subplots(1, 2, figsize=(11.0, 3.4), dpi=FIGURE_DPI)`. Left: CV matrix. Right: range matrix. Each cell is annotated with the numeric value.
        - Use `ax.imshow(matrix, cmap="Blues", aspect="auto")` with `ax.set_xticks(range(4))` and labels `[1h, 6h, 1d, 1w]`, `ax.set_yticks(range(2))` and labels `["contextual", "goh_barabasi"]`. Annotate each cell with the value formatted to 4 decimal places using `ax.text(x, y, f"{val:.4f}", ha="center", va="center", color=INK, fontsize=10)`.
        - Suptitle: `"Contextual vs Goh-Barabasi — CV and range by window size"`.
        - **Source caption** (CONTEXT.md L93): add `fig.text(0.01, 0.01, "Source: 8.5M-record Chicago crime dataset (2001–2026); Phase 83 analysis", fontsize=7, color=SUBTLE, ha="left", va="bottom")`.
        - `fig.savefig(output_path, dpi=FIGURE_DPI)`, `plt.close(fig)`.

    5. `def render_all_figures(contextual_parquet: Path, goh_barabasi_parquet: Path, comparison_csv: Path, output_dir: Path) -> None:`
       - Load all three inputs. Call the three plot functions above with the right output paths. Print `f"Wrote figures to {output_dir}/"`.

    6. `if __name__ == "__main__":` block:
       ```python
       from pathlib import Path
       render_all_figures(
           Path("output/contextual_metric.parquet"),
           Path("output/goh_barabasi_metric.parquet"),
           Path("output/comparison_table.csv"),
           Path("output/figures"),
       )
       ```

    7. Imports: `from __future__ import annotations`, `from pathlib import Path`, `import pandas as pd`, `import numpy as np`, `import matplotlib.pyplot as plt`.

    8. Module-level WINDOW_LABELS constant (same as the rest of the project): `{3600: "1h", 21600: "6h", 86400: "1d", 604800: "1w"}`.

    Verify: from the phase root, run `.venv/bin/python figures.py`. Confirm three PNGs exist in `output/figures/`, each ≥ 100 KB (320 DPI produces ~200-500 KB for these sizes).
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python figures.py` exits 0
    - `ls -la output/figures/` shows three PNGs each > 50 KB
    - `python -c "from PIL import Image; im = Image.open('output/figures/z_heatmap.png'); print(im.size, im.info.get('dpi'))"` prints a tuple `(width, height)` and a DPI value ≥ 300
  </verify>
  <acceptance_criteria>
    - figures.py contains `def plot_z_heatmap(`, `def plot_per_window_timeseries(`, `def plot_contrast_table(`, `def render_all_figures(`
    - output/figures/z_heatmap.png, output/figures/per_window_timeseries.png, output/figures/contrast_table.png all exist
    - Each PNG is > 50 KB and has DPI ≥ 300 (verifiable via PIL `Image.open(...).info["dpi"]`)
    - The z_heatmap.png is a 2x2 panel grid (one panel per window size) showing 24×7 cell values
    - The per_window_timeseries.png has both blue (z) and accent (B) curves on twin axes
    - The contrast_table.png shows CV and range matrices with cell values annotated
  </acceptance_criteria>
  <done>Three thesis figures render at 320 DPI with the chapter3 palette.</done>
  <depends_on>Task 1</depends_on>
</task>

<task type="auto">
  <name>Task 3: Wire run.py to orchestrate data → metrics → comparison → figures end-to-end</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py (the current state of run.py — read it, do not assume)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py (Plan 04 Task 1 output)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py (Plan 04 Task 2 output)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
  </files>
  <action>
    Extend `run.py` to add the comparison and figures stages after the two metric stages from Plans 02 and 03. After this change, `python run.py` is the full end-to-end orchestrator: data → contextual → Goh-Barabasi → comparison → figures. Plan 05 will add the final decision-gate stage.

    1. Remove the `try/except ImportError` placeholders for `compare` and `figures`. Import normally: `import compare`, `import figures`.

    2. Immediately after the Goh-Barabasi metric block, insert:
       ```python
       # Stage 3: Comparison
       print("Computing comparison table...")
       t2 = time.perf_counter()
       contextual_df = pd.read_parquet(args.output_dir / "contextual_metric.parquet")
       gb_df = pd.read_parquet(args.output_dir / "goh_barabasi_metric.parquet")
       table = compare.build_comparison_table(contextual_df, gb_df)
       compare.write_comparison_csv(table, args.output_dir / "comparison_table.csv")
       print(table.to_string(index=False))
       print(f"Comparison: {time.perf_counter() - t2:.1f}s")

       # Stage 4: Figures
       print("Rendering figures...")
       t3 = time.perf_counter()
       figures.render_all_figures(
           args.output_dir / "contextual_metric.parquet",
           args.output_dir / "goh_barabasi_metric.parquet",
           args.output_dir / "comparison_table.csv",
           args.output_dir / "figures",
       )
       print(f"Figures: {time.perf_counter() - t3:.1f}s")
       ```

    3. Smoke test:
       - Run `.venv/bin/python run.py` from the phase root. Exits 0.
       - Total runtime: data (~30s) + contextual (~1-3 min) + Goh-Barabasi (~2-5 min) + comparison (~5s) + figures (~30s) ≈ 5-9 min.
       - Stdout contains the full comparison table printed with `to_string(index=False)`.
       - Stdout contains `Wrote figures to` and `Comparison:`/`Figures:` timing lines.
       - `output/comparison_table.csv`, `output/figures/z_heatmap.png`, `output/figures/per_window_timeseries.png`, `output/figures/contrast_table.png` all exist.
       - Running `python run.py` twice produces byte-identical `output/comparison_table.csv` (determinism check — the script reads from a read-only DuckDB and does all math deterministically).

    4. Final run.py top-level docstring should now read: "Phase 83 single-command entry point. Loads the 8.5M-record Chicago crime dataset, computes the contextual and Goh-Barabasi burstiness metrics over 1h/6h/1d/1w windows, writes comparison_table.csv, renders thesis figures, and writes DECISION-GATE.md (added in Plan 05). Usage: `python run.py` (uses defaults) or `python run.py --output-dir <path>`."
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python run.py` exits 0
    - All five output artifacts exist: contextual_metric.parquet, goh_barabasi_metric.parquet, comparison_table.csv, figures/z_heatmap.png, figures/per_window_timeseries.png, figures/contrast_table.png
    - `cmp <(python -c "import pandas; print(pandas.read_csv('output/comparison_table.csv').to_csv(index=False))") <(python -c "import pandas; print(pandas.read_csv('output/comparison_table.csv').to_csv(index=False))")` exits 0 (determinism — running twice produces the same CSV)
  </verify>
  <acceptance_criteria>
    - run.py runs end-to-end in < 15 minutes producing all five output artifacts
    - output/comparison_table.csv has 8 rows and the contextual/gb CV ratio at 1d is computable from it
    - The three figure PNGs each have DPI >= 300 and size > 50 KB
    - Running `python run.py` twice produces byte-identical comparison_table.csv (the DuckDB read is read-only and the math is deterministic)
    - The Makefile `reproduce` target (`make reproduce`) runs the full pipeline end-to-end
  </acceptance_criteria>
  <done>run.py is the full end-to-end orchestrator producing all five output artifacts in one command.</done>
  <depends_on>Task 2</depends_on>
</task>

</tasks>

<verification>
1. `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python run.py` — exits 0, all five output artifacts produced, full comparison table printed to stdout
2. `wc -l output/comparison_table.csv` — 9 lines (1 header + 8 data rows)
3. `ls -la output/figures/*.png` — three PNGs each > 50 KB
4. `python -c "from PIL import Image; [print(p, Image.open(p).info.get('dpi')) for p in ['output/figures/z_heatmap.png', 'output/figures/per_window_timeseries.png', 'output/figures/contrast_table.png']]"` — each shows DPI ≥ 300
5. Determinism: run `python run.py` twice, diff the two comparison_table.csv files — must be byte-identical
6. `make reproduce` — runs the full pipeline and exits 0
7. Source captions are present in all three figures: `grep -F "Source:" figures.py` returns three matches (one per `fig.text` call)
</verification>

<success_criteria>
- `compare.py` produces `output/comparison_table.csv` with 8 rows (2 metrics × 4 window sizes) summarising per-window CV and range.
- `figures.py` renders three thesis-grade PNGs at 320 DPI: z_heatmap (24×7 hour×dayOfWeek × 4 window sizes), per_window_timeseries (twin-axis z vs B at 1d), contrast_table (visual mirror of the CSV).
- `python run.py` reproduces the entire pipeline end-to-end in < 15 minutes, and running it twice produces byte-identical `comparison_table.csv`.
- The contextual CV at 1d is numerically larger than the Goh-Barabasi CV at 1d (the load-bearing comparison the Plan 05 decision gate will evaluate).
</success_criteria>

<output>
After completion, create `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-04-SUMMARY.md` using the summary template.
</output>

<artifacts_this_phase_produces>
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/compare.py`
  - Constants: WINDOW_LABELS
  - Functions: `summarize_metric(series, value_col, metric_name) -> pd.DataFrame`, `build_comparison_table(contextual_df, goh_barabasi_df) -> pd.DataFrame`, `write_comparison_csv(table, output_path) -> None`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/figures.py`
  - Constants: BG, INK, SUBTLE, GRID, BLUE, BLUE_DARK, BLUE_LIGHT, TEAL, ACCENT, RED, FIGURE_DPI = 320
  - Functions: `plot_z_heatmap(z_series_by_window, output_path)`, `plot_per_window_timeseries(z_series, b_series, output_path, window_sec=86400)`, `plot_contrast_table(table, output_path)`, `render_all_figures(contextual_parquet, goh_barabasi_parquet, comparison_csv, output_dir)`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv`
  - Schema: [metric, window_size, window_label, n_windows, mean, std, min, max, cv, range]
  - Cardinality: 8 rows
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/z_heatmap.png` (320 DPI, 2×2 panel)
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/per_window_timeseries.png` (320 DPI, twin-axis)
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/figures/contrast_table.png` (320 DPI, 1×2 panel)
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py` (extended with comparison + figures stages; further extended in Plan 05)
</artifacts_this_phase_produces>
