---
phase: 83-contextual-burstiness-vs-goh-barabasi-comparison
plan: 05
type: execute
wave: 4
depends_on:
  - 83-04
files_modified:
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/DECISION-GATE.md
  - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
autonomous: true
user_setup: []
requirements:
  - CBP-05
  - CBP-06

must_haves:
  truths:
    - "decision_gate.py reads comparison_table.csv and applies the locked thresholds: PRIMARY = (contextual CV at 1d >= 2x max(B, density, CV CV at 1d)) AND (contextual range at 1d >= 3x max(B, density, CV range at 1d)); SECONDARY (NEW) = absolute contextual CV at 1d >= 0.10 (avoids the near-zero-denominator false positive)"
    - "The 'max' comparator in the PRIMARY rule means contextual must beat *every* reference metric, not just the average. This is a stricter claim than 'beats Goh-Barabasi' and is the load-bearing thesis claim."
    - "Verdict is one of: 'go' / 'not_yet' / 'no'. Mapping: go = primary AND secondary; not_yet = primary OR secondary fail by a small margin; no = both fail badly"
    - "DECISION-GATE.md is written with: a 4-metric × 4-window summary table, the 1d CV/range ratios vs each reference metric, the verdict, and a 2-3 paragraph justification that pre-empts the 'why 2x and 3x?' thesis-defense question"
    - "DECISION-GATE.md explicitly states whether Phase 84 (BFT-* work, prototype integration) is unlocked, deferred to a 2,016-cell stretch, or closed"
    - "run.py is the final end-to-end orchestrator: data → contextual → B → density → CV → comparison → figures → decision-gate, all from a single `python run.py` command"
    - "Running `python run.py` twice produces byte-identical DECISION-GATE.md (determinism)"
  artifacts:
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py"
      provides: "Threshold evaluator + markdown writer"
      exports: ["evaluate_gate", "write_decision_gate_md"]
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/DECISION-GATE.md"
      provides: "Go/not-yet/no verdict with threshold justification"
    - path: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py"
      provides: "Final end-to-end orchestrator (data + metrics + comparison + figures + decision-gate)"
  key_links:
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv"
      via: "pd.read_csv (Plan 04 output)"
      pattern: "read_csv.*comparison_table\\.csv"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/DECISION-GATE.md"
      via: "Path.write_text"
      pattern: "DECISION-GATE\\.md"
    - from: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py"
      to: ".planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py"
      via: "imported and called from main()"
      pattern: "from metrics import decision_gate|import metrics\\.decision_gate"
---

<objective>
Apply the locked decision-gate thresholds to the 4-metric comparison table and write `DECISION-GATE.md` with a go/not-yet/no verdict and a 2-3 paragraph justification. Make `python run.py` the final end-to-end orchestrator (data → metrics → comparison → figures → decision-gate). CBP-06 is satisfied by the explicit statement in DECISION-GATE.md of whether Phase 84 is unlocked, deferred, or closed.

Purpose: CBP-05 requires a reproducible end-to-end run plus a written decision gate that records whether the new metric should be wired into the prototype. CBP-06 makes the Phase 84 BFT-* work conditional on the gate. The secondary absolute CV floor (>= 0.10) is the research-recommended mitigation for the near-zero-denominator false positive flagged in 83-RESEARCH.md Risk #11 and L461-463. The 4-way comparison (contextual must beat *all* three reference metrics) is the load-bearing thesis claim that justifies the prototype integration.

Output: `metrics/decision_gate.py` (evaluator + markdown writer), `output/DECISION-GATE.md` (4-way verdict + justification), and a final `run.py` that prints the verdict to stdout.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-CONTEXT.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv
@.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@docs/FUTURE-WORK-ADAPTIVE-TIME.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement metrics/decision_gate.py — threshold evaluator + DECISION-GATE.md writer</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-CONTEXT.md (locked decision-gate thresholds at L42, L91: CV ratio >= 2.0, range ratio >= 3.0)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (secondary CV floor at L461-463: "absolute CV floor of 0.10 for the contextual metric"; "why 2x and 3x" justification at L457-466; risk table at L478-489)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/comparison_table.csv (Plan 04 output — read the actual CSV to confirm column names)
    - .planning/REQUIREMENTS.md (CBP-05 and CBP-06 traceability at L34-37, L41-54)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py
  </files>
  <action>
    Create `metrics/decision_gate.py` with two public functions.

    Module docstring: "Decision-gate evaluator (CBP-05) and DECISION-GATE.md writer (CBP-06). Reads comparison_table.csv, applies the locked thresholds (CV ratio >= 2x, range ratio >= 3x, absolute CV floor >= 0.10), and writes a verdict + justification to output/DECISION-GATE.md."

    1. Module-level constants (locked thresholds, no magic numbers):
       ```python
       PRIMARY_CV_RATIO = 2.0       # CONTEXT.md L42, L91
       PRIMARY_RANGE_RATIO = 3.0   # CONTEXT.md L42, L91
       SECONDARY_ABSOLUTE_CV_FLOOR = 0.10  # 83-RESEARCH.md L461-463 (NEW from research)
       TARGET_WINDOW_SEC = 86400    # the decision-gate target is the 1d window
       ```

    2. `def evaluate_gate(comparison_csv: Path) -> dict:`
       - Input: path to the comparison_table.csv.
       - Output: a dict with keys:
         - `verdict`: one of `"go"`, `"not_yet"`, `"no"`
         - `contextual_cv_1d`, `contextual_range_1d`, `gb_cv_1d`, `gb_range_1d`: float
         - `cv_ratio_1d`, `range_ratio_1d`: float (the contextual / gb ratio)
         - `primary_pass`: bool (primary rule result)
         - `secondary_pass`: bool (secondary rule result)
         - `rationale`: str (one-line summary of the verdict)
       - Algorithm:
         a. Load the CSV with `pd.read_csv(comparison_csv)`.
         b. Filter to `window_size == TARGET_WINDOW_SEC` and split into two rows: one with `metric == "contextual"`, one with `metric == "goh_barabasi"`.
         c. Extract `cv`, `range` from each.
         d. `cv_ratio_1d = contextual_cv / gb_cv if gb_cv > 0 else float("inf")`.
         e. `range_ratio_1d = contextual_range / gb_range if gb_range > 0 else float("inf")`.
         f. `primary_pass = (cv_ratio_1d >= PRIMARY_CV_RATIO) and (range_ratio_1d >= PRIMARY_RANGE_RATIO)`.
         g. `secondary_pass = contextual_cv_1d >= SECONDARY_ABSOLUTE_CV_FLOOR`.
         h. Verdict logic:
            - `"go"` if `primary_pass and secondary_pass`
            - `"not_yet"` if `primary_pass or secondary_pass` (one passes, one doesn't — borderline)
            - `"no"` if `not primary_pass and not secondary_pass` (both fail)
         i. `rationale = f"CV ratio {cv_ratio_1d:.2f}x (need >= 2.00x), range ratio {range_ratio_1d:.2f}x (need >= 3.00x), absolute CV {contextual_cv_1d:.3f} (need >= 0.10)"`.
         j. Return the dict.

    3. `def write_decision_gate_md(evaluation: dict, output_path: Path, *, source_csv: Path | None = None) -> None:`
       - Input: the dict from `evaluate_gate`, the path to write to, and (optionally) the source CSV path for traceability.
       - Output: writes the markdown to `output_path`.
       - Markdown structure (use exactly this format — the orchestrator may grep for these headings):
         ```markdown
         # Phase 83 Decision Gate: Contextual Burstiness vs Goh-Barabasi

         **Date:** 2026-06-27
         **Source data:** 8.5M-record Chicago crime dataset (2001-2026)
         **Decision window:** 1d (86400s)

         ## Verdict: **{verdict_upper}**

         {rationale}

         ## Per-metric summary at 1d

         | Metric | CV | Range | n_windows |
         |---|---:|---:|---:|
         | Contextual (z) | {contextual_cv_1d:.4f} | {contextual_range_1d:.4f} | (see comparison_table.csv) |
         | Goh-Barabasi (B) | {gb_cv_1d:.4f} | {gb_range_1d:.4f} | (see comparison_table.csv) |
         | **Ratio (contextual / gb)** | **{cv_ratio_1d:.2f}x** | **{range_ratio_1d:.2f}x** | — |

         ## Threshold rationale

         The 2x CV and 3x range rules guard against: (a) the contextual metric being so noisy that its CV advantage is meaningless, and (b) the contextual metric being so tightly bounded that it can't drive a warp. The 2x threshold is conservative — it is the minimum ratio at which a thesis can credibly claim the new metric is a *material* improvement over the old.

         The absolute CV floor of 0.10 (the secondary criterion, sourced from 83-RESEARCH.md L461-463) is added to handle the documented near-zero-denominator case: Goh-Barabasi B is bounded in [-1, 1] and the prior 100k-sample baseline (docs/FUTURE-WORK-ADAPTIVE-TIME.md:288) shows B CV at 1d = 0.01. A 2x ratio against a denominator of 0.005 is technically a 2x advantage but is still essentially "no signal" — the absolute floor guards against this false positive.

         ## Next steps (CBP-06)

         Based on the verdict **{verdict_lower}**:

         - If **"go"**: Phase 84 (Burstiness Signal Contract + Density Fallback, BFT-01..BFT-12) is **unlocked**. Begin /gsd-plan-phase 84.
         - If **"not_yet"**: The 2,016-cell stretch baseline (hour × dayOfWeek × month, per 83-RESEARCH.md L147-149 + L489) is the recommended next move. Re-run this analysis with the stretch baseline and re-evaluate the gate. If it still fails, fall back to BFT-04..BFT-06 in Phase 84 (density-driven detail timeline) and close the contextual-burstiness integration thread.
         - If **"no"**: Close the contextual-burstiness prototype-integration thread. Fall back to the density-driven detail timeline (BFT-04..BFT-06) in Phase 84 and document the negative result in the thesis discussion.

         ## Artifacts

         - `output/contextual_metric.parquet` — per-window z-scores
         - `output/goh_barabasi_metric.parquet` — per-window B values
         - `output/comparison_table.csv` — per-metric CV/range summary
         - `output/figures/z_heatmap.png` — 24×7 z-score heatmap
         - `output/figures/per_window_timeseries.png` — twin-axis B vs z
         - `output/figures/contrast_table.png` — visual contrast table
         - `output/DECISION-GATE.md` — this document
         ```
       - `verdict_upper` is the verdict title-cased (e.g. "GO"); `verdict_lower` is lowercase.
       - `output_path.parent.mkdir(parents=True, exist_ok=True)` then `output_path.write_text(markdown)`.
       - Print `f"Wrote decision gate ({evaluation['verdict']}) to {output_path}"`.

    4. `if __name__ == "__main__":` block:
       ```python
       from pathlib import Path
       evaluation = evaluate_gate(Path("output/comparison_table.csv"))
       write_decision_gate_md(evaluation, Path("output/DECISION-GATE.md"))
       print(f"Verdict: {evaluation['verdict'].upper()}")
       print(evaluation["rationale"])
       ```

    5. Imports: `from __future__ import annotations`, `pandas as pd`, `from pathlib import Path`.

    Verify: from the phase root, run `.venv/bin/python -m metrics.decision_gate`. Confirm `output/DECISION-GATE.md` exists and starts with the literal string `# Phase 83 Decision Gate: Contextual Burstiness vs Goh-Barabasi`.
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python -m metrics.decision_gate` exits 0
    - `output/DECISION-GATE.md` exists
    - `head -1 output/DECISION-GATE.md` starts with `# Phase 83 Decision Gate:`
    - `grep -E "Verdict: \*\*[A-Z_]+\*\*" output/DECISION-GATE.md` matches (the verdict heading)
  </verify>
  <acceptance_criteria>
    - metrics/decision_gate.py contains `def evaluate_gate(`, `def write_decision_gate_md(`, and the literal strings `PRIMARY_CV_RATIO = 2.0`, `PRIMARY_RANGE_RATIO = 3.0`, `SECONDARY_ABSOLUTE_CV_FLOOR = 0.10`
    - output/DECISION-GATE.md exists and contains the headings `# Phase 83 Decision Gate`, `## Verdict:`, `## Per-metric summary at 1d`, `## Threshold rationale`, `## Next steps (CBP-06)`, `## Artifacts`
    - The verdict is one of `GO`, `NOT_YET`, or `NO` (uppercase, in a `## Verdict: **...**` heading)
    - The "Next steps (CBP-06)" section explicitly maps each verdict to the Phase 84 unlock/defer/close decision
    - The threshold rationale paragraph mentions both the 2x/3x ratio rules AND the absolute CV floor of 0.10
  </acceptance_criteria>
  <done>decision_gate.py evaluates the locked thresholds and writes a verdict-bearing DECISION-GATE.md.</done>
  <depends_on></depends_on>
</task>

<task type="auto">
  <name>Task 2: Extend run.py with the final decision-gate stage + verify reproducibility</name>
  <read_first>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py (the current state — read it, do not assume)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py (Plan 05 Task 1 output)
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-RESEARCH.md (reproducibility requirements at L420-432)
  </read_first>
  <files>
    - .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py
  </files>
  <action>
    Final extension of `run.py`. After this, `python run.py` is the complete end-to-end orchestrator producing all seven output artifacts (2 parquets + 1 CSV + 3 PNGs + 1 markdown).

    1. Remove the `try/except ImportError` placeholder for `decision_gate`. Import normally: `from metrics import decision_gate`.

    2. Immediately after the figures block, insert:
       ```python
       # Stage 5: Decision gate
       print("Evaluating decision gate...")
       t4 = time.perf_counter()
       evaluation = decision_gate.evaluate_gate(args.output_dir / "comparison_table.csv")
       decision_gate.write_decision_gate_md(evaluation, args.output_dir / "DECISION-GATE.md")
       print(f"VERDICT: {evaluation['verdict'].upper()}")
       print(evaluation["rationale"])
       print(f"Decision gate: {time.perf_counter() - t4:.1f}s")
       print(f"Total elapsed: {time.perf_counter() - t0:.1f}s")
       ```

    3. Final run.py docstring: "Phase 83 single-command entry point. Loads the 8.5M-record Chicago crime dataset, computes the contextual and Goh-Barabasi burstiness metrics over 1h/6h/1d/1w windows, writes comparison_table.csv, renders three thesis figures, evaluates the decision gate, and writes DECISION-GATE.md with a go/not-yet/no verdict. Usage: `python run.py` (uses defaults) or `python run.py --output-dir <path>`."

    4. Reproducibility verification (executor MUST run this before claiming the task done):
       a. Run `.venv/bin/python run.py` end-to-end. Capture the verdict printed to stdout.
       b. Copy `output/comparison_table.csv` and `output/DECISION-GATE.md` to `/tmp/run1_*.csv` and `/tmp/run1_*.md`.
       c. Run `.venv/bin/python run.py` a second time. Copy outputs to `/tmp/run2_*.csv` and `/tmp/run2_*.md`.
       d. Run `cmp /tmp/run1_comparison_table.csv /tmp/run2_comparison_table.csv` — must exit 0 (byte-identical).
       e. Run `cmp /tmp/run1_DECISION-GATE.md /tmp/run2_DECISION-GATE.md` — must exit 0 (byte-identical).
       f. Confirm the verdict printed in both runs is the same string (e.g. `VERDICT: GO` or `VERDICT: NOT_YET`).

    5. Total runtime check: the full pipeline (data + 2 metrics + comparison + 3 figures + decision gate) should complete in < 15 minutes on the 8.5M dataset.

    6. Final inventory check: after `python run.py` completes, the following must exist:
       - `output/contextual_metric.parquet`
       - `output/goh_barabasi_metric.parquet`
       - `output/comparison_table.csv`
       - `output/figures/z_heatmap.png`
       - `output/figures/per_window_timeseries.png`
       - `output/figures/contrast_table.png`
       - `output/DECISION-GATE.md`
  </action>
  <verify>
    - `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python run.py` exits 0
    - `output/DECISION-GATE.md` exists and contains a verdict
    - `ls output/ output/figures/` shows all seven artifacts
    - `cmp <(cat output/DECISION-GATE.md) <(cat output/DECISION-GATE.md)` exits 0 (trivially, but the determinism check is running run.py twice — see acceptance_criteria)
  </verify>
  <acceptance_criteria>
    - run.py runs the full pipeline end-to-end in < 15 minutes
    - All seven output artifacts exist after the run: contextual_metric.parquet, goh_barabasi_metric.parquet, comparison_table.csv, figures/z_heatmap.png, figures/per_window_timeseries.png, figures/contrast_table.png, DECISION-GATE.md
    - DECISION-GATE.md has a `## Verdict: **<ONE_OF_GO/NOT_YET/NO>**` heading
    - The decision gate verdict is printed to stdout as `VERDICT: <verdict>` (uppercase)
    - Running `python run.py` twice produces byte-identical comparison_table.csv and DECISION-GATE.md (the executor MUST run this check and include both cmp exit codes in the SUMMARY)
    - The `make reproduce` target runs the full pipeline and exits 0
  </acceptance_criteria>
  <done>run.py is the final end-to-end orchestrator; DECISION-GATE.md is written with a go/not-yet/no verdict; running run.py twice is deterministic.</done>
  <depends_on>Task 1</depends_on>
</task>

</tasks>

<verification>
1. `cd .planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison && .venv/bin/python run.py` — exits 0, all seven output artifacts produced, verdict printed to stdout
2. `head -3 output/DECISION-GATE.md` — shows title, date, and source data
3. `grep -E "^## Verdict: \*\*" output/DECISION-GATE.md` — matches a verdict heading
4. `ls output/ output/figures/` — all seven files present
5. Determinism: run `python run.py` twice, `cmp` the two comparison_table.csv files (must be byte-identical) and the two DECISION-GATE.md files (must be byte-identical)
6. `make reproduce` — runs the full pipeline and exits 0
7. Total runtime < 15 minutes on the 8.5M dataset
</verification>

<success_criteria>
- `metrics/decision_gate.py` applies the locked thresholds (CV ratio >= 2x, range ratio >= 3x, absolute CV floor >= 0.10) and emits a `go` / `not_yet` / `no` verdict.
- `output/DECISION-GATE.md` contains the verdict, a per-metric summary table at 1d, a threshold rationale paragraph, and an explicit Phase 84 unlock/defer/close mapping.
- `python run.py` is the final end-to-end orchestrator that produces all seven output artifacts from a single command, in < 15 minutes, deterministically (byte-identical outputs across runs).
- CBP-06 is satisfied: the DECISION-GATE.md "Next steps" section makes the Phase 84 BFT-* work explicitly conditional on the verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/83-05-SUMMARY.md` using the summary template. The SUMMARY must include the final verdict (GO / NOT_YET / NO) and both cmp results (comparison_table.csv and DECISION-GATE.md byte-identical) as evidence of determinism.
</output>

<artifacts_this_phase_produces>
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/metrics/decision_gate.py`
  - Constants: `PRIMARY_CV_RATIO = 2.0`, `PRIMARY_RANGE_RATIO = 3.0`, `SECONDARY_ABSOLUTE_CV_FLOOR = 0.10`, `TARGET_WINDOW_SEC = 86400`
  - Functions: `evaluate_gate(comparison_csv: Path) -> dict`, `write_decision_gate_md(evaluation: dict, output_path: Path, *, source_csv: Path | None = None) -> None`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/output/DECISION-GATE.md`
  - Sections: `# Phase 83 Decision Gate`, `## Verdict: **<VERDICT>**`, `## Per-metric summary at 1d`, `## Threshold rationale`, `## Next steps (CBP-06)`, `## Artifacts`
- File: `.planning/phases/83-contextual-burstiness-vs-goh-barabasi-comparison/run.py` (final version — orchestrates data + 2 metrics + comparison + 3 figures + decision gate)
- CLI command: `python run.py` (single-command end-to-end reproduction)
- CLI command: `make reproduce` (Makefile shortcut for the same)
</artifacts_this_phase_produces>
