---
phase: 87-burst-volume-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 87-01-PLAN.md
files_modified:
  - src/hooks/useBurstVolumeModel.ts
  - src/hooks/useBurstVolumeModel.test.ts
  - src/components/viz/TimeSlices.tsx
autonomous: true
user_setup: []
requirements:
  - BURST-01
  - BURST-02
---

<objective>
Add a memoized live derivation path that feeds the burst-volume builder from the existing app state without changing the control layer or the cube rendering.

Purpose: BURST-01 and BURST-02 need the model to be computed from real burst-window and STKDE inputs, not only from static fixtures. This plan keeps the model derivation reusable while leaving the visible experience unchanged.

Output: `src/hooks/useBurstVolumeModel.ts` and a focused test suite that prove a stable, derived `BurstVolumeModel` can be read from current app state.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-burst-volume-foundation/87-CONTEXT.md
@.planning/phases/87-burst-volume-foundation/87-01-PLAN.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/useDashboardDemoCoordinationStore.ts
@src/lib/stkde/contracts.ts
@src/lib/stkde/burst-volume.ts
@src/components/viz/TimeSlices.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive burst volumes from live app inputs</name>
  <read_first>
    - src/store/useDashboardDemoCoordinationStore.ts (for `selectedBurstWindows` and `stkdeResponse`)
    - src/lib/stkde/contracts.ts (for `StkdeResponse` and hotspot shape)
    - src/lib/stkde/burst-volume.ts (the builder contract from 87-01)
  </read_first>
  <files>
    - src/hooks/useBurstVolumeModel.ts
    - src/hooks/useBurstVolumeModel.test.ts
    - src/components/viz/TimeSlices.tsx
  </files>
  <action>
    Add a memoized live derivation path that feeds the burst-volume builder from the existing app state without changing the control layer or the cube rendering.

    Implementation intent:
    - read `selectedBurstWindows` and `stkdeResponse.sliceResults` from the coordination store
    - transform the current burst window selection into the builder input shape
    - derive the burst volume model in a dedicated hook so the model can be reused by later phases
    - keep `TimeSlices.tsx` free of burst-volume rendering work; at most, it may consume the hook for debugging or pass-through state in a way that does not affect the current visuals

    The hook should:
    - return a neutral model when no burst window is active
    - remain stable across rerenders when the underlying inputs do not change
    - preserve the current timeline and cube interaction behavior exactly

    Add a focused test that proves the hook or helper computes a consistent model from representative burst-window + STKDE fixtures.
  </action>
  <verify>
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm test -- useBurstVolumeModel` passes
    - `grep -F "selectedBurstWindows" src/hooks/useBurstVolumeModel.ts` matches
    - `grep -F "sliceResults" src/hooks/useBurstVolumeModel.ts` matches
  </verify>
  <acceptance_criteria>
    - A live derivation path exists from current app state to `BurstVolumeModel`
    - The derivation is memoized and side-effect free
    - No timeline controls, map behavior, or cube rendering regress as part of this phase
    - The resulting model is ready for the rendering phase without further shape changes
  </acceptance_criteria>
  <done>Phase 1 can compute burst volumes from live state while leaving the visible experience unchanged.</done>
</task>

</tasks>
