---
phase: 80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - src/app/evaluation/page.tsx
  - src/components/evaluation/EvaluationShell.tsx
  - src/components/evaluation/EvaluationHeader.tsx
  - src/components/evaluation/EvaluationTrainingGate.tsx
  - src/components/onboarding/OnboardingTour.tsx
  - src/components/evaluation/EvaluationTaskCard.tsx
  - src/components/evaluation/EvaluationQuestionnaire.tsx
autonomous: true
must_haves:
  truths:
    - Researcher can run the dedicated `/evaluation` route without modifying the existing `/dashboard-demo` route.
    - The header always shows the current study step and preserves the fixed Welcome → Done sequence from the evaluation store.
    - Training, tasks, and questionnaires appear only at the correct points in the flow, with task order fixed to T4 → T1 → T2 → T3 inside each condition block.
    - Participants never see explicit `Uniform` or `Adaptive` labels in the evaluation shell.
  artifacts:
    - path: src/app/evaluation/page.tsx
      provides: Dedicated evaluation route entry
    - path: src/components/evaluation/EvaluationShell.tsx
      provides: Overlay frame around DashboardDemoShell
    - path: src/components/evaluation/EvaluationHeader.tsx
      provides: Session metadata, stepper, and researcher utilities scaffold
    - path: src/components/evaluation/EvaluationTaskCard.tsx
      provides: Task answer + confidence capture UI
    - path: src/components/evaluation/EvaluationQuestionnaire.tsx
      provides: Per-condition NASA/Likert questionnaire UI
  key_links:
    - from: src/app/evaluation/page.tsx
      to: src/components/dashboard-demo/DashboardDemoShell.tsx
      via: EvaluationShell wrapper import
      pattern: DashboardDemoShell
    - from: src/components/evaluation/EvaluationHeader.tsx
      to: src/store/useEvaluationStudyStore.ts
      via: currentStep + block order + step navigation
      pattern: currentStep|blockOrder|goToStep|nextStep
    - from: src/components/evaluation/EvaluationTaskCard.tsx
      to: src/lib/study/protocol.ts
      via: fixed task definitions and confidence gating
      pattern: T4|T1|T2|T3|confidence
    - from: src/components/evaluation/EvaluationQuestionnaire.tsx
      to: src/lib/study/protocol.ts
      via: NASA + Likert item definitions
      pattern: nasa|likert
---

<objective>
Build the dedicated `/evaluation` shell and route-specific training/task/questionnaire surfaces on top of the protocol and store contract from 80-01.

Purpose: give the researcher a controlled study route while keeping the visualization workspace intact and condition names hidden from participants.
Output: `/evaluation`, header/stepper chrome, training tour entry, floating task card, and per-condition questionnaire overlays.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-CONTEXT.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-UI-SPEC.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-RESEARCH.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-01-PLAN.md
@src/components/dashboard-demo/DashboardDemoShell.tsx
@src/components/onboarding/OnboardingTour.tsx
@src/store/useEvaluationStudyStore.ts
@src/lib/study/protocol.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the `/evaluation` overlay shell, researcher stepper header, and route-specific training entry</name>
  <files>
    src/app/evaluation/page.tsx
    src/components/evaluation/EvaluationShell.tsx
    src/components/evaluation/EvaluationHeader.tsx
    src/components/evaluation/EvaluationTrainingGate.tsx
    src/components/onboarding/OnboardingTour.tsx
  </files>
  <action>
    Add `src/app/evaluation/page.tsx` as a dedicated App Router page that renders `DashboardDemoShell` inside an `EvaluationShell` overlay. Do not fork or regress `/dashboard-demo`. The shell must honor the UI contract: one overlaid header band with session metadata on the left, the 8-step researcher stepper centered, and researcher utilities on the right.

    Bind the header to `useEvaluationStudyStore` so the displayed step labels come from the canonical protocol/order helpers rather than hardcoded strings. The researcher must be able to confirm the participant's A→B or B→A assignment at session start, and the rendered stepper must then show the correct `Tasks-A` / `Questionnaire-A` followed by `Tasks-B` / `Questionnaire-B` progression for that assignment.

    Update the onboarding tour flow so `/evaluation` uses a researcher-triggered 5-step training tour only. Preserve existing driver.js infrastructure, but remove auto-run dependence on the generic `hasSeenTour` path for this route and restyle the popovers per `80-UI-SPEC.md`.
  </action>
  <verify>
    1. `pnpm exec tsc --noEmit && pnpm exec eslint src/app/evaluation src/components/evaluation src/components/onboarding/OnboardingTour.tsx`
    2. Run `/evaluation` locally and confirm there is no double-stacked top bar and the workspace still fills the screen.
    3. Start one A→B session and one B→A session; confirm the researcher stepper labels and current-step text show the correct order for both runs.
  </verify>
  <done>
    `/evaluation` exists as a dedicated overlay route, the researcher can confirm participant order at session start, and the route-specific training flow is wired to the evaluation store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the task card and questionnaire surfaces with strict progression gating</name>
  <files>
    src/components/evaluation/EvaluationTaskCard.tsx
    src/components/evaluation/EvaluationQuestionnaire.tsx
    src/components/evaluation/EvaluationShell.tsx
    src/components/evaluation/EvaluationHeader.tsx
  </files>
  <action>
    Implement the floating non-blocking task card and the centered questionnaire overlay against `useEvaluationStudyStore` and `src/lib/study/protocol.ts`. The task card must always use the fixed order `T4 -> T1 -> T2 -> T3` inside each condition block, require answer capture before showing the confidence slider, and require confidence capture before the flow can advance.

    Implement the per-condition questionnaire shell with exactly 6 NASA-RTLX sliders, 6 interpretability Likert rows, and an optional comment field. Submission stays disabled until every required item is answered. Transition into `Questionnaire-A` and `Questionnaire-B` must come only after all four tasks in that condition block have been completed and saved.

    Keep participant-facing copy blinded: no visible `Uniform` or `Adaptive` text in the task card, questionnaire, or general shell copy.
  </action>
  <verify>
    1. `pnpm exec tsc --noEmit && pnpm exec eslint src/components/evaluation/EvaluationTaskCard.tsx src/components/evaluation/EvaluationQuestionnaire.tsx`
    2. Run a local session and confirm the UI cannot advance from a task without first recording an answer and then a 1–5 confidence rating.
    3. Confirm the flow cannot submit either questionnaire until all 6 NASA and all 6 Likert inputs are filled.
  </verify>
  <done>
    Tasks and questionnaires are wired to the canonical protocol, progression is gated correctly, and participant-facing copy stays condition-blind.
  </done>
</task>

</tasks>

<verification>
- The route shell is only complete when both A→B and B→A sessions render the correct researcher stepper sequence.
- Training is only complete when the tour is researcher-triggered on `/evaluation` and no longer depends on generic dashboard auto-run behavior.
- Task/questionnaire gating is only complete when partial answers cannot advance the flow.
</verification>

<success_criteria>
1. `/evaluation` runs as a dedicated study shell without changing `/dashboard-demo`.
2. The researcher stepper accurately reflects the chosen participant order and fixed phase sequence.
3. Tasks and questionnaires appear only at the correct phases and enforce answer/completion gating.
</success_criteria>

<output>
After completion, create `.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-02-SUMMARY.md`
</output>
