---
phase: 80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/study/protocol.ts
  - src/lib/study/condition-order.ts
  - src/lib/study/resetTargets.ts
  - src/store/useEvaluationStudyStore.ts
  - src/store/useStudyStore.ts
  - src/lib/study/storage.ts
  - src/lib/logger.ts
  - src/app/api/study/log/route.ts
autonomous: true
must_haves:
  truths:
    - Researcher can start a session with an explicit A→B or B→A assignment before any participant task begins.
    - The evaluation flow always advances in the fixed sequence Welcome → Training → Tasks-A → Questionnaire-A → Tasks-B → Questionnaire-B → Interview → Done.
    - A reset for the next participant clears all named participant-visible persisted state instead of relying on a vague global reset.
    - Session lifecycle, trials, questionnaires, condition toggles, and warp-factor events persist as acknowledged DuckDB writes.
  artifacts:
    - path: src/lib/study/protocol.ts
      provides: Canonical Phase 80 task/questionnaire/step sequence definition
    - path: src/lib/study/condition-order.ts
      provides: Explicit participant order assignment and sequencing helpers
    - path: src/lib/study/resetTargets.ts
      provides: Audited reset checklist for persisted participant-visible state
    - path: src/store/useEvaluationStudyStore.ts
      provides: Evaluation session/phase/task/questionnaire store
    - path: src/lib/study/storage.ts
      provides: DuckDB study table bootstrap and insert helpers
    - path: src/app/api/study/log/route.ts
      provides: Acknowledged structured study write endpoint
  key_links:
    - from: src/store/useEvaluationStudyStore.ts
      to: src/lib/study/condition-order.ts
      via: session start assignment + next-step progression
      pattern: assignConditionOrder|nextStep|currentStep
    - from: src/store/useEvaluationStudyStore.ts
      to: src/lib/study/resetTargets.ts
      via: resetForNewSession bootstrap
      pattern: resetForNewSession|resetTargets
    - from: src/lib/logger.ts
      to: src/app/api/study/log/route.ts
      via: acknowledged POST write path
      pattern: fetch\('/api/study/log'
    - from: src/app/api/study/log/route.ts
      to: src/lib/study/storage.ts
      via: validated study insert helpers
      pattern: ensureStudy|insertStudy
---

<objective>
Create the canonical study protocol, order/reset contract, and structured persistence layer before any evaluation UI is built.

Purpose: make Phase 80 execution deterministic — the UI, researcher workflow, and saved data all share one source of truth.
Output: protocol modules, an evaluation flow store, an audited reset checklist, and acknowledged DuckDB-backed study writes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-CONTEXT.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-RESEARCH.md
@src/store/useStudyStore.ts
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useSliceDomainStore.ts
@src/store/useDashboardDemoMapLayerStore.ts
@src/store/useDashboardDemoFilterStore.ts
@src/lib/db.ts
@src/lib/logger.ts
@src/app/api/study/log/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codify protocol, participant order assignment, and audited reset targets</name>
  <files>
    src/lib/study/protocol.ts
    src/lib/study/condition-order.ts
    src/lib/study/resetTargets.ts
    src/store/useEvaluationStudyStore.ts
    src/store/useStudyStore.ts
  </files>
  <action>
    Create `src/lib/study/protocol.ts` as the implementation source of truth for Phase 80. Encode the fixed task order `T4 -> T1 -> T2 -> T3`, the 8-step researcher flow `Welcome -> Training -> Tasks-A -> Questionnaire-A -> Tasks-B -> Questionnaire-B -> Interview -> Done`, the locked 6 NASA-RTLX + 6 interpretability items, and a metadata/version export that explicitly records that the Phase 80 context/UI spec override the outdated questionnaire counts in `EVALUATION_PROTOCOL.md`.

    Add `src/lib/study/condition-order.ts` with explicit researcher-confirmed order helpers. It must support both `A->B` and `B->A`, derive the correct step labels (`Tasks-A`/`Questionnaire-A` then `Tasks-B`/`Questionnaire-B` for the assigned block order), and expose pure helpers that can be tested directly for participant/order correctness.

    Add `src/lib/study/resetTargets.ts` as a named reset checklist instead of vague reset language. Include concrete targets for every participant-visible persisted surface touched by `/evaluation`: `evaluation-study-v1` (sessionStorage), `study-storage`, `slice-domain-v1`, `dashboard-demo-map-layer-store-v1`, `dashboard-demo-filter-presets`, `hasSeenTour`, plus coordination-store normalization actions such as `resetWarp`, `resetAnalysis`, `resetTemporalSettings`, `resetVolumeSettings`, `setActiveRailTab('scan')`, and `setActiveSliceIndex(0)`. Update `useEvaluationStudyStore` and the legacy `useStudyStore` so `resetForNewSession()` executes this checklist and records which targets succeeded or failed.
  </action>
  <verify>
    1. `pnpm exec vitest run src/lib/study/condition-order.test.ts src/lib/study/resetTargets.test.ts`
    2. `pnpm exec tsc --noEmit`
    3. Inspect the reset checklist output/log and confirm it names every reset target instead of reporting a generic reset.
  </verify>
  <done>
    Phase 80 has a single in-repo protocol definition, explicit A/B order helpers, and an audited reset checklist for all participant-visible persisted state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace schema-less study logging with acknowledged DuckDB fact-table writes</name>
  <files>
    src/lib/study/storage.ts
    src/lib/logger.ts
    src/app/api/study/log/route.ts
    src/store/useEvaluationStudyStore.ts
  </files>
  <action>
    Build `src/lib/study/storage.ts` on top of `src/lib/db.ts` with flat DuckDB tables for `study_sessions`, `study_trials`, `study_questionnaire_responses`, and `study_condition_events`. The event table must store both condition toggles and warp-factor adjustments with timestamps, session/participant identifiers, block labels, and the pre/post values needed for later pilot analysis.

    Redesign `src/app/api/study/log/route.ts` to accept validated write intents for session lifecycle, task trial, questionnaire submission, and condition/warp events, returning an explicit success acknowledgement only after persistence completes. Update `src/lib/logger.ts` so critical writes requeue on failure instead of dropping buffered events, and expose per-write save status back to `useEvaluationStudyStore` so later UI plans can block unsafe advancement when persistence fails.
  </action>
  <verify>
    1. `pnpm exec tsc --noEmit`
    2. Start the app and POST one payload each for session start, trial result, questionnaire submission, condition toggle, and warp-factor adjustment to `/api/study/log`; every response must acknowledge success.
    3. Confirm DuckDB contains rows for all five payload classes, including a warp-factor event row distinct from a condition-toggle row.
  </verify>
  <done>
    Study data writes are acknowledged, analytics-ready, and durable enough for the evaluation flow to depend on them.
  </done>
</task>

</tasks>

<verification>
- Order assignment is only complete when both A→B and B→A are test-covered and the store can derive the correct `Tasks-*` / `Questionnaire-*` sequence from the chosen order.
- Reset is only complete when named reset targets are auditable and participant-visible state cannot silently leak across sessions.
- Persistence is only complete when warp-factor adjustments are saved as first-class events, not implicit UI state.
</verification>

<success_criteria>
1. The evaluation store can start a session with an explicit participant order and fixed step sequence.
2. Reset/bootstrap uses a named checklist covering all participant-visible persisted state.
3. DuckDB stores structured session, trial, questionnaire, condition-toggle, and warp-factor event rows through acknowledged writes.
</success_criteria>

<output>
After completion, create `.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-01-SUMMARY.md`
</output>
