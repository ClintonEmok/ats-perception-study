---
phase: 80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us
plan: 03
type: execute
wave: 3
depends_on:
  - 80-01
  - 80-02
files_modified:
  - src/components/evaluation/EvaluationHeader.tsx
  - src/components/evaluation/ResearcherWarpControls.tsx
  - src/components/dashboard-demo/DashboardDemoRailTabs.tsx
  - src/components/dashboard-demo/DemoDetectPanel.tsx
  - src/components/dashboard-demo/DemoSlicePanel.tsx
  - src/components/dashboard-demo/DemoConfigurePanel.tsx
  - src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
  - src/store/useDashboardDemoCoordinationStore.ts
  - src/store/useEvaluationStudyStore.ts
autonomous: false
must_haves:
  truths:
    - Participants can still access the visualization workspace, but evaluation-locked dashboard-demo controls are disabled and removed from keyboard tab flow.
    - Researcher-only warp controls remain available, focusable, and every warp-factor adjustment is logged.
    - The evaluation flow can be piloted end-to-end for both A→B and B→A with the correct Tasks-A/Questionnaire-A then Tasks-B/Questionnaire-B sequencing.
    - `/dashboard-demo` remains usable for unfinished Phase 79 work because evaluation locking is additive and route-scoped.
  artifacts:
    - path: src/components/evaluation/ResearcherWarpControls.tsx
      provides: Researcher-only warp factor adjustment path in the evaluation header
    - path: src/components/dashboard-demo/DemoSlicePanel.tsx
      provides: Evaluation lock handling for participant-visible slice editing controls
    - path: src/components/dashboard-demo/DemoConfigurePanel.tsx
      provides: Evaluation lock handling for condition-revealing setup controls
    - path: src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
      provides: Evaluation lock handling for timeline settings controls
  key_links:
    - from: src/components/evaluation/ResearcherWarpControls.tsx
      to: src/store/useDashboardDemoCoordinationStore.ts
      via: setWarpFactor + setTimeScaleMode researcher path
      pattern: setWarpFactor|setTimeScaleMode|warpFactor|timeScaleMode
    - from: src/components/evaluation/ResearcherWarpControls.tsx
      to: src/lib/logger.ts
      via: explicit warp/condition event logging
      pattern: warp_factor|condition_toggle|log
    - from: src/components/dashboard-demo/DemoSlicePanel.tsx
      to: src/store/useEvaluationStudyStore.ts
      via: evaluation lock state for disabled and non-focusable controls
      pattern: isEvaluationLocked|tabIndex|-1|disabled
    - from: src/components/dashboard-demo/DashboardDemoRailTabs.tsx
      to: src/components/evaluation/EvaluationShell.tsx
      via: shared lock signal without changing standard dashboard-demo behavior
      pattern: evaluation|locked
---

<objective>
Integrate participant lock mode into shared dashboard-demo surfaces, add the researcher-only warp control path, and pilot the full study flow with overlap-safe verification against unfinished Phase 79 work.

Purpose: finish the route-level evaluation hardening without breaking the standard demo or leaving critical sequencing/focus behavior unverified.
Output: additive dashboard-demo locks, researcher-only warp controls with logging, and a final pilot verification pass.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-CONTEXT.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-UI-SPEC.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-01-PLAN.md
@.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-02-PLAN.md
@.planning/phases/79-adaptive-3d-visualization/79-01-PLAN.md
@.planning/phases/79-adaptive-3d-visualization/79-02-PLAN.md
@.planning/phases/79-adaptive-3d-visualization/79-03-PLAN.md
@src/components/dashboard-demo/DashboardDemoRailTabs.tsx
@src/components/dashboard-demo/DemoDetectPanel.tsx
@src/components/dashboard-demo/DemoSlicePanel.tsx
@src/components/dashboard-demo/DemoConfigurePanel.tsx
@src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
@src/store/useDashboardDemoCoordinationStore.ts
@src/store/useEvaluationStudyStore.ts
</context>

<coordination_strategy>
- Treat all Phase 80 changes to shared dashboard-demo files as additive evaluation guards only. Do not remove or redesign standard `/dashboard-demo` behavior that Phase 79 still needs.
- Before editing any shared dashboard-demo file, read its current source plus the relevant Phase 79 plan(s) above. If a Phase 79-owned control already exists in that file, wrap it in the evaluation lock path instead of reverting, duplicating, or moving it.
- Do not touch `src/app/stkde-3d/*` or the Phase 79 timeline-density implementation from this plan unless a compile break requires a minimal compatibility fix.
- Finish with a `/dashboard-demo` smoke pass so Phase 79 work stays runnable outside `/evaluation`.
</coordination_strategy>

<tasks>

<task type="auto">
  <name>Task 1: Add additive evaluation locks to shared dashboard-demo controls</name>
  <files>
    src/components/dashboard-demo/DashboardDemoRailTabs.tsx
    src/components/dashboard-demo/DemoDetectPanel.tsx
    src/components/dashboard-demo/DemoSlicePanel.tsx
    src/components/dashboard-demo/DemoConfigurePanel.tsx
    src/components/dashboard-demo/DemoTimelineSettingsCard.tsx
    src/store/useEvaluationStudyStore.ts
  </files>
  <action>
    Wire a shared evaluation-lock signal from the evaluation store into the dashboard-demo panels that expose participant-editable or condition-revealing controls. Keep all rail tabs visible, but make locked controls disabled, visually muted, and removed from participant tab order with explicit `disabled`, `aria-disabled`, and `tabIndex={-1}` handling where appropriate. Use one helper strip (`Setup locked during evaluation.`) per affected panel instead of repeating helper text on each control.

    This lock path must cover slice creation/deletion/resizing/apply flows, STKDE parameter editing, generation actions, warp source switching, and timeline settings that reveal the manipulation. Do not alter these controls on the standard `/dashboard-demo` route; gate them only when the evaluation shell has activated participant lock mode.
  </action>
  <verify>
    1. `pnpm exec tsc --noEmit && pnpm exec eslint src/components/dashboard-demo/DashboardDemoRailTabs.tsx src/components/dashboard-demo/DemoDetectPanel.tsx src/components/dashboard-demo/DemoSlicePanel.tsx src/components/dashboard-demo/DemoConfigurePanel.tsx src/components/dashboard-demo/DemoTimelineSettingsCard.tsx`
    2. In `/evaluation`, use keyboard Tab/Shift+Tab to confirm locked participant controls are skipped while visible researcher controls in the header remain reachable.
    3. In `/dashboard-demo`, confirm the same controls remain interactive so unfinished Phase 79 work is not blocked.
  </verify>
  <done>
    Evaluation lock mode is route-scoped, participant controls are non-focusable, and the standard demo route still works for normal editing and Phase 79 execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the researcher-only warp factor control path and event wiring</name>
  <files>
    src/components/evaluation/EvaluationHeader.tsx
    src/components/evaluation/ResearcherWarpControls.tsx
    src/store/useDashboardDemoCoordinationStore.ts
    src/store/useEvaluationStudyStore.ts
  </files>
  <action>
    Create `src/components/evaluation/ResearcherWarpControls.tsx` and mount it inside the researcher zone of `EvaluationHeader.tsx`. This surface is the only place in `/evaluation` where warp factor can be adjusted directly. It must stay researcher-only, keyboard reachable, and visually separated from participant-facing UI.

    Wire the control path to `useDashboardDemoCoordinationStore.setWarpFactor` and `setTimeScaleMode`, and log every condition toggle plus every warp-factor adjustment through the acknowledged study event path from 80-01. Include enough payload detail to reconstruct the sequence later: session ID, participant ID, current step, block label, prior value, new value, and timestamp. Keep the participant-visible unlabeled toggle behavior from 80-02 intact, but ensure the deeper warp-factor tuning path is not available in participant-facing panels.
  </action>
  <verify>
    1. `pnpm exec tsc --noEmit && pnpm exec eslint src/components/evaluation/EvaluationHeader.tsx src/components/evaluation/ResearcherWarpControls.tsx`
    2. In `/evaluation`, confirm the researcher can adjust warp factor from the header while participant-locked panels do not expose a competing warp control.
    3. Inspect DuckDB or the acknowledged event response stream and confirm each warp-factor change writes a study event row distinct from the unlabeled condition toggle event.
  </verify>
  <done>
    `/evaluation` has a researcher-only warp-control path, and every warp or condition change is persisted for later analysis.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pilot the full evaluation flow for sequencing, focus isolation, and reset safety</name>
  <what-built>Dedicated `/evaluation` route, explicit order assignment, route-scoped participant locks, researcher-only warp controls, and acknowledged DuckDB study persistence.</what-built>
  <how-to-verify>
    1. Start one participant with A→B and one participant with B→A. For each run, confirm the header shows the correct order before training begins and the stepper advances through `Tasks-A -> Questionnaire-A -> Tasks-B -> Questionnaire-B` in that exact sequence.
    2. During each block, confirm the task card enforces the fixed in-block order `T4 -> T1 -> T2 -> T3`, and that confidence capture is required before the next task.
    3. Use keyboard Tab from the start of the page in `/evaluation`. Confirm the participant-locked controls in the rail panels are skipped entirely, while the researcher header controls (stepper navigation, session controls, researcher warp controls) remain reachable.
    4. Toggle the unlabeled participant-visible time-scale control and separately adjust warp factor from the researcher-only header controls. Confirm both interactions are instantaneous, and verify the saved study events distinguish the condition toggle from the researcher warp-factor adjustment.
    5. End the first session, start a new participant session, and confirm the audited reset checklist clears all named targets: no old answers, no inherited slices, no leftover map-layer state, no stale filter presets applied, no reused session ID, and no leftover training-complete flag.
    6. Open `/dashboard-demo` after the evaluation pilot and confirm normal demo controls still work so unfinished Phase 79 tasks remain executable.
  </how-to-verify>
  <resume-signal>Type `approved` if sequencing, focus isolation, logging, reset safety, and dashboard-demo compatibility all pass; otherwise list the failing checkpoint numbers.</resume-signal>
</task>

</tasks>

<verification>
- Locking is only complete when participant controls are visible-but-disabled and absent from tab order, while researcher controls remain reachable.
- Warp logging is only complete when researcher-only warp-factor adjustments are saved separately from unlabeled condition toggles.
- Coordination is only complete when `/dashboard-demo` still supports Phase 79 work after the evaluation guards land.
</verification>

<success_criteria>
1. Shared dashboard-demo controls are evaluation-locked without regressing the standard demo route.
2. Researcher-only warp controls exist, stay available, and log every adjustment.
3. The full pilot flow passes for both A→B and B→A, including task/questionnaire sequencing, focus isolation, and reset safety.
</success_criteria>

<output>
After completion, create `.planning/phases/80-evaluation-readiness-prepare-dashboard-demo-prototype-for-us/80-03-SUMMARY.md`
</output>
